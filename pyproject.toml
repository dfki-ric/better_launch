[project]
name = "better-launch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click~=8.1.7",
    "docstring-parser~=0.16",
    "osrf-pycommon~=2.0.2",
    "prompt-toolkit~=3.0.50",
    "psutil~=6.0.0",
    "pytest>=9.0.2",
    "pyyaml~=6.0.1",
    "setproctitle~=1.3.4",
]
