{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"better_launch","text":"<p>Welcome to better_launch</p>"},{"location":"paper/","title":"Summary","text":"<p>Today, most robots use middleware in order to manage and connect the various pieces of software they need for fulfilling their tasks. Over the last years, the most popular middleware - the Robot Operating System (ROS) - has received a complete overhaul and been released as ROS2. However, the reception so far has been mixed. While the code base has seen several important improvements, this reinvention has also come with severe downgrades in terms of user friendliness and usability. One of the biggest and most exposed offenders is the launch system, responsible for orchestrating the startup and execution of the robot's software components. The software presented in this paper - better_launch - is a complete replacement for the ROS2 launch system and solves several technical and usability issues. It is lightweight, intuitive, convenient, fully documented, and comes with many examples.</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#statement-of-need","title":"Statement of need","text":"<p>On one hand, being able to write launch files in Python is a nice feature that does away with many of the limitations of the old ROS1 xml launch files. On the other hand, the current implementation tries to use Python as a declarative language, forcing users to dance around unintuitive constructs as none of the values and nodes exist yet at the time of execution. Aside from its wordiness and significant challenge to new users, it comes with several anti patterns and technical issues, too:</p> <ul> <li>execution order of actions is non-deterministic</li> <li>dynamic code execution (e.g., passing code as a string for later execution)</li> <li>resists linting as many arguments have to be passed as strings</li> <li>resists automated code analysis as actions don't have a unified way of exposing internals</li> <li>frequent zombie processes</li> <li>convoluted and obscure internals</li> </ul> <p>There are, of course, good reasons for the way the launch system has been implemented, at least on a superficial level. According to the design document [@ros2_launch_design], the intent is to treat launch files as \"a description of what will happen\" without executing anything. This is so that tools can \"visualize and modify the launch description\". The recently released LaunchMap [@launchmap] is able to do just that. However, given that users may define their own launch actions without a common way of inspecting them, it could be argued that even this use case is not well supported right now. There is also no good argument why the same couldn't be achieved using, e.g., Python's inspect module and/or a non-declarative syntax.</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#existing-remedies","title":"Existing Remedies","text":"<p>The issues in user friendliness have led to the emergence of several packages that simplify writing launch files, e.g., Simple_launch [@simple_launch] and Launch-generator [@launch_generator], while other packages like the popular Generate_parameter_library [@generate_parameter_library] are dedicated to particular aspects of the launch process. However, in the end they all face the same issues mentioned above, as they just represent different ways of generating the launch descriptions.</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#better_launch","title":"better_launch","text":"<p>In order to actually resolve these issues, I have written better_launch, a complete and self-contained replacement for the ROS2 launch system with no dependencies on the existing facilities. Getting started with better_launch is easy as it is well-documented and comes with examples for various common use cases. It enables developers to write simple and intuitive launch files using regular Pythonic syntax (see example below). Crucially, it addresses the above issues as follows:</p> <ul> <li>Any actions taken in the launch files are executed immediately, allowing direct and meaningful interactions with nodes, topics, and services as the launch process develops. This allows, e.g., adjusting one node's parameters based on another node's published topics.</li> <li>Arguments to the launch file are \"declared\" in the form of function arguments that are passed as natural types, enabling common Python syntax like if-else.</li> <li>better_launch launch files are fully compatible with the ROS2 launch system. This means they can be started through <code>ros2 launch</code>, include regular ROS2 launch files, and even get included from regular ROS2 launch files.</li> <li>The entire launch logic is contained within a single, fully documented package, which includes examples. Convenience functions exist for various common tasks like starting a <code>joint_state_publisher</code> or bridging Gazebo topics.</li> <li>better_launch comes with a replacement for <code>ros2 launch</code> called <code>bl</code>, which is faster than its counterpart and provides additional features. For example, <code>bl</code> enables auto completion for launch parameters and uses the launch function's docstring to generate <code>--help</code> text.</li> <li>To improve usability, better_launch reformats and colors terminal output by default. </li> <li>Unless killed with SIGKILL, better_launch will not leave zombie processes behind. </li> <li>better_launch provides an optional and unobtrusive terminal UI, reminiscent of Rosmon [@rosmon], which can be used for stopping and restarting nodes, triggering life cycle transitions, listing a node's subscribed topics, dynamically adjusting the logging level, and more.</li> </ul> <p></p> <p>We consider better_launch mature enough for general use in research applications, with performance similar or better than <code>ros2 launch</code> (benchmarks in repo) [@py-spy; @memray; @psutil]. It is under active development and can be downloaded for free from https://github.com/dfki-ric/better_launch. We hope that better_launch will advance the state of ROS2 in a meaningful way.</p> <p>\\newpage</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#example","title":"Example","text":"<p>The ROS2 tutorials provide an example [@ros2_substitution_example] for running a turtlebot simulation. This launch file creates a node, then calls one of its services and updates a parameter. Ironically, due to the asynchronous execution, the parameter update usually fails because the node has not come up yet. better_launch does not have this issue, and can in addition express the same launch file with only 28 instead of 73 lines - including documentation!</p> <pre><code>#!/usr/bin/env python3\nfrom better_launch import BetterLaunch, launch_this\n\n@launch_this\ndef great_atuin(\n    turtlesim_ns: str = \"turtlesim1\", \n    use_provided_red: bool = True, \n    new_background_r: int = 200,\n):\n    \"\"\" This docstring will also be used to create text for --help!\"\"\"\n    bl = BetterLaunch()\n\n    with bl.group(turtlesim_ns):\n        turtle_node = bl.node(\n            package=\"turtlesim\",\n            executable=\"turtlesim_node\",\n            name=\"sim\",\n            params={\"background_r\": 120},\n        )\n\n    bl.call_service(\n        topic=f\"/{turtlesim_ns}/spawn\",\n        service_type=\"turtlesim/srv/Spawn\",\n        request_args={\"x\": 2.0, \"y\": 2.0, \"theta\": 0.2},\n    )\n\n    if use_provided_red:\n        turtle_node.is_ros2_connected(timeout=None)\n        turtle_node.set_live_params({\"background_r\": new_background_r})\n</code></pre> <p>\\newpage</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#ai-usage-disclosure","title":"AI usage disclosure","text":"<p>No generative AI tools were used in the development of this software, the writing of this manuscript, or the preparation of supporting materials.</p>","tags":["ROS2","launch","Python"]},{"location":"paper/#references","title":"References","text":"","tags":["ROS2","launch","Python"]},{"location":"about/differences/","title":"What are the differences?","text":"<p>Because better_launch does not use the ROS2 launch system, some aspects work differently from what you may be used to.</p>"},{"location":"about/differences/#action-immediacy","title":"Action immediacy","text":"<p>In ROS2 launch, launchfiles create tasks that are then passed to an asynchronous event loop. This is the reason why e.g. checking for launch parameter values is so incredibly weird - they simply don't exist yet by the time you define the actions. In better_launch however, all actions are taken immediately: if you create a node, its process is started right away; if you include another launchfile, its contents will be handled before the function returns. </p> <p>The only exception to this is adding ROS2 launch actions, e.g. by including regular ROS2 launchfiles. Since these still rely on the ROS2 launch system, they need to be turned into asynchronous tasks and passed to the event loop. Usually a ROS2 <code>LaunchService</code> sub-process is started the first time a ROS2 action is passed to better_launch. From then on this process will handle all ROS2 actions asynchronously in the background. </p> Info <p>While the output of the ROS2 launch service process (and its nodes) is captured and formatted by better_launch just like for all other nodes, these will usually appear and behave as one single <code>launch_service</code> unit in the TUI.</p>"},{"location":"about/differences/#lifecycle-nodes","title":"Lifecycle nodes","text":"<p>Lifecycle nodes differ from regular nodes in that they don't become fully active after their process starts. Instead you have to call one of their lifecycle management services, usually via additional code in your launchfile or the <code>ros2 lifecycle</code> CLI. However, in the end they are still just nodes.</p> <p>better_launch makes no distinction between regular and lifecycle nodes. Instead, all \"lifecyclable\" objects (e.g. nodes and components) provide a <code>LifecycleManager</code> object via their <code>lifecycle</code> member. This will be <code>None</code> if the object has not been identified (yet) as a lifecycle-thing - otherwise you can use it to manage the object's lifecycle. </p> Note <p>All objects that turn out to be lifecyclable will transition to their ACTIVE state by default, unless you pass a different target state on instantiation.</p>"},{"location":"about/differences/#type-checking","title":"Type checking","text":"<p>When passing arguments to a node in ROS2, it is ultimately passed as a stringified command line argument. So why bother with overly strict type checking? Why do I have to turn half the parameters into strings myself? better_launch does not impose a flawed type sytem on you and will happily accept <code>int</code>, <code>string</code>, <code>float</code>, etc. where appropriate. In addition, sensible and unsurprising types have been chosen for all arguments you may provide (e.g. remaps are defined as a <code>dict[str, str]</code>, floats are happy to accept ints, launch arguments are never required to be strings, etc.).</p>"},{"location":"about/differences/#declaring-launch-arguments","title":"Declaring launch arguments","text":"<p>Simply put: you don't. better_launch will check the signature of your launch function and turn all arguments into launch arguments. For example, if your launch function has an <code>enable_x</code> argument, you will be able to pass it with <code>--enable_x &lt;value&gt;</code> from the command line. Under the hood better_launch is using click, so every launchfile you write comes with proper CLI support. </p> Tip <p>Tip: try adding a docstring to your launch function and call your launchfile with <code>--help</code>!</p>"},{"location":"about/differences/#parameter-files","title":"Parameter files","text":"<p>You do not have to put <code>ros__parameters</code> in your configs anymore when using <code>BetterLaunch.load_params</code>. Hooray! You still can do so of course if you feel slightly masochistic. In fact, better_launch supports the full param syntax for mapping params to nodes, including namespace wildcards. See the load_params documentation for details.</p>"},{"location":"about/differences/#logging","title":"Logging","text":"<p>Just like ROS2 launch, better_launch takes care of managing loggers and redirecting everything where it belongs (in fact that part is largely copied from ROS2 launch). However, I did away with the in my opinion not very useful separation between a node's <code>stdout</code> and <code>stderr</code>, since nodes apparently write their log output to <code>stderr</code> by default. </p> <p>I also added a reformatting layer so that colors and nicer screen output are possible. The format can be customized by passing your own logging format strings to the launch_this decorator. However, it is recommended to set the <code>BL_SCREEN_LOG_FORMAT</code> and <code>BL_FILE_LOG_FORMAT</code> environment variables instead.</p>"},{"location":"about/differences/#abandoned-processes","title":"Abandoned processes","text":"<p>ROS2 launch has a bad reputation of leaving stale and abandoned processes behind after terminating. In my testing so far this has never been an issue with better_launch yet - except when you hard kill (-9) the better_launch process.</p>"},{"location":"about/features/","title":"Okay, what can I do with it?","text":"<p>Everything! better_launch  can do everything ROS2 launch can do and some more. It's also faster, more reliabel, and comes with complete API documentation and many examples.</p> <ul> <li>create nodes, composers, components, namespaces, etc.</li> <li>create subscribers, publishers, services, etc. on the fly</li> <li>predictable execution allows you to interact with nodes right after starting them</li> <li>include other better_launch launchfiles</li> <li>include and be included from regular ROS2 launchfiles</li> <li>manage lifecycle stages</li> <li>remap topics using simple dicts</li> <li>launch arguments passed directly to your function</li> <li>easily locate files and load configs</li> <li>manage nodes from other launch files</li> <li>manage your node using a nice terminal UI reminiscent of rosmon</li> <li>write TOML launchfiles with ROS1-style substitutions</li> <li>all of this and more with as few as only 2 imports</li> </ul> <p></p> <p>For a quick comparison, bravely unfold the sections below:</p> ROS2 <pre><code># Taken from https://docs.ros.org/en/jazzy/Tutorials/Intermediate/Launch/Using-Substitutions.html\nfrom launch_ros.actions import Node\n\nfrom launch import LaunchDescription\nfrom launch.actions import DeclareLaunchArgument, ExecuteProcess, TimerAction\nfrom launch.conditions import IfCondition\nfrom launch.substitutions import LaunchConfiguration, PythonExpression\n\n\ndef generate_launch_description():\n    turtlesim_ns = LaunchConfiguration('turtlesim_ns')\n    use_provided_red = LaunchConfiguration('use_provided_red')\n    new_background_r = LaunchConfiguration('new_background_r')\n\n    turtlesim_ns_launch_arg = DeclareLaunchArgument(\n        'turtlesim_ns',\n        default_value='turtlesim1'\n    )\n    use_provided_red_launch_arg = DeclareLaunchArgument(\n        'use_provided_red',\n        default_value='False'\n    )\n    new_background_r_launch_arg = DeclareLaunchArgument(\n        'new_background_r',\n        default_value='200'\n    )\n\n    turtlesim_node = Node(\n        package='turtlesim',\n        namespace=turtlesim_ns,\n        executable='turtlesim_node',\n        name='sim'\n    )\n    spawn_turtle = ExecuteProcess(\n        cmd=[[\n            'ros2 service call ',\n            turtlesim_ns,\n            '/spawn ',\n            'turtlesim/srv/Spawn ',\n            '\"{x: 2, y: 2, theta: 0.2}\"'\n        ]],\n        shell=True\n    )\n    change_background_r = ExecuteProcess(\n        cmd=[[\n            'ros2 param set ',\n            turtlesim_ns,\n            '/sim background_r ',\n            '120'\n        ]],\n        shell=True\n    )\n    change_background_r_conditioned = ExecuteProcess(\n        condition=IfCondition(\n            PythonExpression([\n                new_background_r,\n                ' == 200',\n                ' and ',\n                use_provided_red\n            ])\n        ),\n        cmd=[[\n            'ros2 param set ',\n            turtlesim_ns,\n            '/sim background_r ',\n            new_background_r\n        ]],\n        shell=True\n    )\n\n    return LaunchDescription([\n        turtlesim_ns_launch_arg,\n        use_provided_red_launch_arg,\n        new_background_r_launch_arg,\n        turtlesim_node,\n        spawn_turtle,\n        change_background_r,\n        TimerAction(\n            period=2.0,\n            actions=[change_background_r_conditioned],\n        )\n    ])\n</code></pre> better_launch (python) <pre><code>from better_launch import BetterLaunch, launch_this\nfrom rclpy import Timer\n\n@launch_this\ndef my_start(\n    # Launch arguments in function signature\n    turtlesim_ns: str = \"turtlesim1\", \n    use_provided_red: bool = False, \n    new_background_r: int = 200,\n):\n    bl = BetterLaunch()\n\n    # Pythonic AF\n    with bl.group(turtlesim_ns):\n        turtle_node = bl.node(\n            package=\"turtlesim\",\n            executable=\"turtlesim_node\",\n            name=\"sim\",\n            # Pass parameters directly \n            params={\"background_r\": 120}\n        )\n\n        # Convenient API for common tasks\n        bl.call_service(\n            topic=f\"/{turtlesim_ns}/spawn\",\n            service_type=\"turtlesim/srv/Spawn\",\n            # No weird types like passing dicts as strings\n            request_args={\"x\": 2.0, \"y\": 2.0, \"theta\": 0.2},\n        )\n\n        if new_background_r == 200 and use_provided_red:\n            turtle_node.is_ros2_connected(timeout=None)\n            turtle_node.set_live_params({\"background_r\": new_background_r})\n</code></pre>"},{"location":"about/performance/","title":"Performance","text":"<p>I am not an expert on profiling code. Even though better_launch uses synchronous calls (or classic threads if necessary), and does some additional work to reformat output from nodes, it was able to achieve similar performance to <code>ros2 launch</code>. The scripts and results from the benchmarks can be found under benchmarks. This section will only show the most relevant parts.</p> Note <p><code>bl</code> is just a script to locate the launch file and then run it, so I decided to not use <code>bl</code> for these benchmarks and instead run the launch file directly; otherwise the resources used by the launch file will not be visible to most profilers.</p> memray <p>memray reports that better_launch uses about 30% less memory than <code>ros2 launch</code>.</p> better_launch ros2 launch allocations 48196 60943 peak memory usage 6.6 MiB 9.7 MiB details link link psutil <p>psutil shows that better_launch uses more CPU in the beginning and more memory in total compared to <code>ros2 launch</code>. The memory reported is the unique set size (see the previous link). I'm not sure how these results relate to the memray statistics above. </p> <p></p> <p></p> py-spy <p>I use py-spy to see where better_launch is using resources that can still be optimized. The speedscope files can be visualized on speedscope.app.</p> <p></p> <p></p>"},{"location":"about/ros2/","title":"Why not improve ROS2 launch?","text":"<p>Because I think it is beyond redemption and no amount of refactoring and REPs (ROS enhancement proposals) will turn the sails. While tools like simple_launch or launch-generator exist, they still use ROS2 launch under the hood and so inherit much of its clunkiness. Rather than fixing an inherently broken solution, I decided to make a RAP - a ROS abandonment proposal :)</p> <p>Here is a \"simple\" launch file from the official documentation that does nothing but include another launch file:</p> <pre><code>from launch_ros.substitutions import FindPackageShare\n\nfrom launch import LaunchDescription\nfrom launch.actions import IncludeLaunchDescription\nfrom launch.launch_description_sources import PythonLaunchDescriptionSource\nfrom launch.substitutions import PathJoinSubstitution, TextSubstitution\n\n\ndef generate_launch_description():\n    colors = {\n        'background_r': '200'\n    }\n\n    return LaunchDescription([\n        IncludeLaunchDescription(\n            PythonLaunchDescriptionSource([\n                PathJoinSubstitution([\n                    FindPackageShare('launch_tutorial'),\n                    'launch',\n                    'example_substitutions_launch.py'\n                ])\n            ]),\n            launch_arguments={\n                'turtlesim_ns': 'turtlesim2',\n                'use_provided_red': 'True',\n                'new_background_r': TextSubstitution(text=str(colors['background_r']))\n            }.items()\n        )\n    ])\n</code></pre> <p>I think we can agree that this is not exactly elegant - including another launch file should be doable within a single line, not 10 plus 5 imports. Other terrible decisions within ROS2 launch include, but are not limited to:</p> <ul> <li>a weird fetish for unintuitive import statements (see above)</li> <li>unneccesarily strict type checking (why use python if I have to verify everything?)</li> <li>nonsensical argument types (e.g. remaps are a list of tuples instead of simply a dict)</li> <li>using asyncio may be slightly faster, but prevents normal variable interactions (ever wondered why you always see these weird <code>Condition</code> classes instead of a simple <code>if my_arg:</code>?)</li> <li>horrendous API for starting lifecycle nodes (also, why the hell are there two completely separate base interfaces?)</li> <li>the list goes on...</li> </ul> <p>For comparison, here is what the above launch file will look like in better_launch:</p> <pre><code>from better_launch import BetterLaunch, launch_this\n\n@launch_this\ndef main(turtlesim_ns = \"turtlesim2\", use_provided_red = True, new_background_r = 200):\n    bl = BetterLaunch()\n\n    bl.include(\n        \"launch_tutorial\", \n        \"example_substitutions.launch.py\",\n        pass_all_args=True,  # or pass as keyword arguments\n    )\n</code></pre> <p>Overall, ROS2 launch seems like a system architect's wet fever dream, and I don't enjoy it.</p>"},{"location":"about/why/","title":"Why better_launch?","text":"<p>better_launch is what I wish <code>ROS2 launch</code> would be: intuitive to use, simple to understand, easy to remember. It is not yet another abstraction layer over ROS2 launch; it is a full replacement with no required dependencies on the existing launch system.</p> <p>Instead of dozens of imports and class instances for even the most basic tasks, your launch files could look as simple and beautiful as this:</p> <pre><code>from better_launch import BetterLaunch, launch_this\n\n@launch_this(ui=True)\ndef my_main(enable_x: bool = True):\n    \"\"\"\n    This is how nice your launch files could be!\n    \"\"\"\n    bl = BetterLaunch()\n\n    if enable_x:\n        bl.node(\n            \"examples_rclpy_minimal_publisher\",\n            \"publisher_local_function\",\n            \"example_publisher\",\n        )\n\n    # Include other launch files, even regular ROS2 launch files!\n    bl.include(\"better_launch\", \"ros2_turtlesim.launch.py\")\n</code></pre> <pre><code>$&gt; bl my_package my_launch_file.py --enable_x True\n</code></pre> <p>Do I have your attention? Read on to learn more!</p>"},{"location":"howto/python/","title":"Python Launchfiles","text":"Tip <p>better_launch comes with many example launchfiles that will cover all the essentials and some advanced use cases. They can be found in the repo. This chapter will serve as a more general introduction.</p>"},{"location":"howto/python/#header","title":"Header","text":"<p>A better_launch python launchfile will usually start with the basic imports followed by your decorated main launch function: </p> <pre><code>from better_launch import BetterLaunch, launch_this\n\n@launch_this\ndef my_launch_function():\n    bl = BetterLaunch()\n    ...\n</code></pre> <p>The @launch_this decorator will start the launch process and run your function, or run it in an already running launch process in case the launchfile got included. It will also expose a <code>generate_launch_description</code> function in case the launchfile got included or run from ROS2. In this case, your launch function will be wrapped in an <code>OpaqueCoroutine</code>, so better_launch  will still be used for execution.</p> <p>You may name the decorated function however you like, its name serves no purpose beyond self-documentation. Your function should start by creating an instance of BetterLaunch. This instance will provide you access to all of the launch functions, like creating nodes, calling services, etc. It is also a singleton, so trying to instantiate it multiple times will always return the same instance.</p>"},{"location":"howto/python/#launch-arguments","title":"Launch Arguments","text":"<p>To add launch arguments, simply add them as function arguments. You should provide a type hint and/or default value so that passed arguments are resolved to the correct type on execution. If no default is provided the argument will be required.</p> <pre><code>from better_launch import BetterLaunch, launch_this\n\n@launch_this\ndef my_launch_function(i_like_haikus: bool):\n    bl = BetterLaunch()\n    print(i_like_haikus, type(i_like_haikus))\n    ...\n</code></pre> <p>If you provide a docstring to your launch function it will be used to create a help text and argument descriptions for your launchfile. better_launch uses numpy style docstrings, as I think it is the most concise one, but all common docstring formats are supported.</p> <pre><code>@launch_this\ndef my_launch_function(i_like_haikus: bool):\n    \"\"\"Launchfiles sprawl in knots / \n    One clean cut to banish chaos / \n    Silence, then motion.\n\n    Parameters\n    ----------\n    i_like_haikus : bool\n        I mean, who doesn't?\n    \"\"\"\n    bl = BetterLaunch()\n    print(i_like_haikus, type(i_like_haikus))\n    ...\n</code></pre> <pre><code>\u276f bl ./haiku.launch.toml --help\nUsage: bl ./haiku.toml [OPTIONS]\n\n  Launchfiles sprawl in knots / One clean cut to banish chaos / Silence, then motion\n\nOptions:\n  --i_like_haikus BOOLEAN      I mean, who doesn't?  [default: True]\n</code></pre>"},{"location":"howto/python/#doing-things","title":"Doing Things","text":"<p>Most launch actions will be executed through the BetterLaunch instance you acquire in the beginning. One of the most common launch patterns is loading a config file, then passing it to a node. </p> <pre><code>from better_launch import BetterLaunch, launch_this\n\n@launch_this\ndef my_launch_function():\n    bl = BetterLaunch()\n\n    params = bl.load_params(\"my_package\", \"my_config.yaml\")\n    with bl.group(\"my_namespace\"):\n        my_node = bl.node(\"my_package\", \"my_node.py\", params=params)\n        print(my_node.get_subscribed_topics())\n</code></pre> <p>Simple as that! Notice how we also acquired a reference to the node we started? This is possible because better_launch will execute all actions synchronously. Once the <code>bl.node</code> call returns the node's process is already underway. This allows you to do some advanced actions that are not possible in ROS2 launchfiles:</p> <ul> <li>Query a node's live parameters.</li> <li>Check if it is a lifecycle node and change its lifecycle state.</li> <li>Terminate or restart nodes. </li> <li>List its topics, services, etc.</li> <li>Get a node's process ID.</li> <li>etc.</li> </ul> Note <p>ROS2 (mildly) pushes the philosophy that the launch logic should be kept separate from runtime logic. It is up to you to decide what's appropriate for your launchfile/system.</p>"},{"location":"howto/python/#common-helpers","title":"Common Helpers","text":"<p>Loading configs and running nodes are general enough to be considered building blocks. There are also some common patterns that build upon these Legos. These are collected in the convenience and gazebo modules.</p> <pre><code>from better_launch import BetterLaunch, launch_this, convenience\n\n@launch_this\ndef my_launch_function():\n    # Still need to create the instance first\n    bl = BetterLaunch()\n\n    convenience.joint_state_publisher()\n    convenience.record_topics()\n</code></pre>"},{"location":"howto/python/#launching-launchfiles","title":"Launching Launchfiles","text":"<p>Astute readers may have noticed that the above example launchfile was run using the <code>bl</code> command. While you can in fact run better_launch launchfiles using the good ol' <code>ros2 launch</code>, this will deprive you of some quality of life features:</p> <ol> <li><code>bl</code> offers auto-completion for launch arguments.</li> <li>It provides auto-completion even for regular ROS2 launchfiles.</li> <li>It resolves and starts all launchfiles much faster than <code>ros2 launch</code>.</li> <li>better_launch becomes the main launch process instead of ROS2's <code>LaunchService</code>, ensuring clean termination.</li> </ol>"},{"location":"howto/settings/","title":"Settings","text":""},{"location":"howto/settings/#launch-options","title":"Launch Options","text":"<p>better_launch has a few settings that can be set from launchfiles or externally. </p> <p>Every option listed here can be set in the launchfile, as an environment variable, or passed as a command line argument. The priority order is always <code>CLI &gt; env &gt; launchfile</code>. Their names slightly change depending on where you define them:</p> <ul> <li>In python launchfiles, pass them in lowercase to the @launch_this decorator (e.g. <code>ui=True</code>).</li> <li>For TOML lanuchfiles, define them as global launch arguments and add a <code>bl_</code> prefix (so <code>ui</code> becomes <code>bl_ui</code>).</li> <li>As environment variables they become uppercase and get a <code>BL_</code> prefix (so <code>ui</code> becomes <code>BL_UI</code>).</li> <li>On the command line they get a <code>--bl-</code> prefix and underscores become dashes (so <code>ui</code> becomes <code>--bl-ui</code>). </li> </ul> ui (bool) <p>Enables or disables the terminal UI.</p> colormode (string) <p>How to apply colors to terminal logging output.</p> <ul> <li>default: uniform highlight for node names, colored severity levels.</li> <li>severity: colored severity levels.</li> <li>source: colored node names.</li> <li>none: no colors.</li> <li>rainbow: colored node names, colored severity levels.</li> </ul> print_limit (int) <p>Limits the character length of messages printed to the terminal. No limit if &lt;= 0.</p> screen_log_level (int) <p>Only print messages to the terminal if they have at least this severity (based on python's logging module). , as defined in python's logging module (10 = DEBUG, 20 = INFO, etc).</p> file_log_level (int) <p>Only write messages to log files if they have at least this severity (see above).</p> screen_log_format (string) <p>Overrides the format for messages logged to the terminal. Check the PrettyLogFormatter for valid syntax.</p> file_log_format (string) <p>Overrides the format for messages logged to log files, following the same format as the screen log format.</p> use_sim_time (bool) <p>Changes the default <code>use_sim_time</code> setting of the root group. All nodes will use this setting unless one of their ancestor groups makes a different override. Note that this parameter does not have the <code>bl-</code> prefix on the command line (env is as usual) - it felt more intuitive this way.</p>"},{"location":"howto/settings/#tui-shortcuts","title":"TUI Shortcuts","text":"<p>On some systems the default TUI shortcuts may be inconvenient, e.g. due to conflicts with system or terminal shortcuts. These can be changed by placing a special string in the <code>BL_TUI_KEYBINDS</code> environment variable. For example, to change the Nodes action to <code>ctrl-n</code> and the Log Level action to <code>ctrl-l</code> you would specify</p> <pre><code>export BL_TUI_KEYBINDS=\"nodes: c-n; loglevel: c-l\"\n</code></pre> <p>The syntax is always <code>&lt;action&gt;:&lt;keys&gt;</code> separated by <code>;</code>. Valid actions are <code>exit</code>, <code>mute</code>, <code>nodes</code>, <code>loglevel</code>, <code>cancel</code>, <code>enter</code>, <code>next</code>, <code>previous</code>. For valid key specifiers see the prompt_toolkit documentation. </p> Warning <p>Take special note of how the quirks around <code>ctrl-s</code>, <code>ctrl-q</code>, and the Alt/Meta/Option keys. The former cannot be bound in most terminals without additional setup, while combinations with the meta key are treated as two separate key presses. This too is documented on the prompt_toolkit website.</p>"},{"location":"howto/toml/","title":"TOML Launchfiles","text":"<p>The discussions over at ros discourse revealed that there was a need for launchfiles that are not code-based but can be parsed offline. TOML launchfiles provide exactly that: the full power of better_launch while limiting the complexity that would be achievable through python.</p> Tip <p>This page will give you a general overview. Have a look at the examples for more details!</p> Feedback welcome! <p>The TOML format is an entirely new way of writing launchfiles. While I have faith in the general approach, it may not quite meet the needs yet. Feedback is very much welcome!</p>"},{"location":"howto/toml/#call-tables","title":"Call Tables","text":"<p>In TOML launchfiles most tables are so-called call tables. These are dictionaries with a <code>func</code> key referring to one of the public BetterLaunch member functions (the convenience and gazebo modules are also supported). Most other attributes are treated as keyword arguments to that function. Just like with python launchfiles, call tables are executed in order of appearance, and their return values are stored under the table\u2019s name.</p> <p>For example, the following call table will call bl.find, pass <code>better_launch</code> as the package and <code>cube.sdf</code> as the filename, then store the returned value under <code>a_simple_cube</code>.</p> <pre><code>[a_simple_cube]\nfunc = \"find\"\npackage = \"better_launch\"\nfilename = \"cube.sdf\"\n</code></pre> Warning <p>Every call table must have a unique name even if the call doesn't have a result or the result is not used!</p>"},{"location":"howto/toml/#substitutions","title":"Substitutions","text":"<p>Substitutions take heavy inspiration from those found in ROS1 and should be familiar to many. However, as the TOML launchfile format is much more powerful, only the following substitutions were  deemed necessary for now:</p> <ul> <li><code>${&lt;K&gt;}</code>: value of launch argument or call table <code>&lt;K&gt;</code></li> <li><code>${param &lt;N&gt; &lt;P&gt;}</code>: query node <code>&lt;N&gt;</code> for its ROS2 parameter <code>&lt;P&gt;</code></li> <li><code>${env &lt;E&gt; &lt;D&gt;}</code>: environment variable <code>&lt;E&gt;</code> (default <code>&lt;D&gt;</code>)</li> <li><code>${eval &lt;X&gt;}</code>: evaluate Python expression <code>&lt;X&gt;</code> (disabled by default)</li> </ul> <p>Substitutions may be nested (inner ones resolve first). The following call table will log the path of the cube we previously located:</p> <pre><code>[print_cube]\nfunc = \"log\"\nseverity = \"info\"\nmessage = \"A simple cube can be found at ${a_simple_cube}\"\n</code></pre> Important <p><code>eval</code> poses a security risk and is therefore disabled by default. This can be changed by creating a global launch argument <code>bl_eval_mode</code> and setting it to either <code>full</code> (regular <code>eval</code>) or <code>literal_eval</code> (only parses literal values).</p>"},{"location":"howto/toml/#conditions","title":"Conditions","text":"<p>Call tables can be made conditional by adding an <code>if</code> or <code>unless</code> parameter. The table will only run if this value evaluates to true (or false for <code>unless</code>) according to python truthiness. Be aware that non-empty strings are also considered true. If a call table doesn't run or has no return, <code>None</code> will be stored under its name.</p>"},{"location":"howto/toml/#launch-arguments","title":"Launch Arguments","text":"<p>Launch arguments can be defined as global parameters before the first table. Their values can be used in substitutions just like any call table result. In addition, you may set the <code>bl_allow_kwargs</code> so arbitrary launch arguments can be passed to the launchfile (i.e. for nodes that consume the additional inputs).</p> <p>In order to support required launch arguments, better_launch makes a small extension to TOML: when defining your launch arguments, instead of assigning them a value you may also assign them one of python's primitive types without quotes (e.g. <code>my_arg = bool</code>). The launchfile will then require this argument to be passed and handle it as the specified type.</p>"},{"location":"howto/toml/#namespaces-components","title":"Namespaces &amp; Components","text":"<p>When using functions like bl.group and bl.compose that create a python context, you have to add the elements belonging to them as children using the following syntax:</p> <pre><code>[my_composer]\nfunc = \"compose\"\n\n[my_composer.children.talker]\nfunc = \"component\"\npackage = \"composition\"\nplugin = \"composition::Talker\"\n</code></pre> <p>You may also use TOML table arrays. In this case the children will get assigned a key according to their index (order of appearance). These two patterns cannot be mixed.</p> <pre><code># key will be my_composer.children.0\n[[my_composer.children]]\n...\n\n# key will be my_composer.children.1\n[[my_composer.children]]\n...\n</code></pre>"},{"location":"howto/toml/#convenience-gazebo","title":"Convenience &amp; Gazebo","text":"<p>By default, call tables will lookup the function to run on the BetterLaunch instance. To run functions from the convenience or gazebo modules, you may specify a <code>context</code> parameter and pass it either <code>convenience</code> or <code>gazebo</code>. This cannot  be used to run code from arbitrary modules - only these values are supported.</p> <p>For example, the following call table will start a rosbag recording:</p> <pre><code>[start_recording]\ncontext = \"convenience\"\nfunc = \"record_topics\"\n</code></pre>"},{"location":"howto/toml/#comments-docstrings","title":"Comments &amp; Docstrings","text":"<p>While TOML has support for comments (using <code>#</code>), existing parsers either don't preserve them, make no association between comments and values, or simply don't work for all cases. The TOML parser in better_launch treats any block of comments immediately preceding a key-value pair as belonging to it. This is relevant for any launch arguments you define, as their associated comments will be turned into help text for the command line. In addition, a comment block at the top of the file followed by a blank line will be treated as the launchfile's docstring.</p> <pre><code># Launchfiles sprawl in knots / \n# One clean cut to banish chaos / \n# Silence, then motion\n\n# I mean, who doesn't?\ni_like_haikus = True\n</code></pre> <pre><code>\u276f bl ./haiku.launch.toml --help\nUsage: bl ./haiku.toml [OPTIONS]\n\n  Launchfiles sprawl in knots / One clean cut to banish chaos / Silence, then motion\n\nOptions:\n  --i_like_haikus BOOLEAN      I mean, who doesn't?  [default: True]\n</code></pre>"},{"location":"howto/tui/","title":"The TUI","text":"<p>better_launch comes with a sneaky, unobstrusive TUI (terminal user interface) based on prompt_toolkit, which will hover below the log output. You can start it by either passing <code>ui=True</code> to the <code>launch_this</code> wrapper, or by adding <code>--bl-ui true</code> on the command line. Use \\ to switch between menu items. <pre><code># Run this line to see it in action!\nbl better_launch 02_ui.launch.py\n</code></pre> <p></p> <p>See the single line at the bottom of the terminal? That's the TUI, and it will never take up more than 3 lines! Despite its simplicity, the TUI allows you a comfortable degree of control over all nodes managed by the better_launch process it is running in:</p> list nodes and their status <p></p> control nodes (start, stop, lifecycle, ...) <p></p> change a node's log level <p></p> see a node's services and topics <p></p> Tip <p>It is possible to change the key bindings. The settings page contains further details.</p>"},{"location":"howto/tui/#foreign-nodes","title":"Foreign nodes","text":"<p>The TUI is also able to manage nodes started from different shells and processes, even if they have been started by ROS2 or other means. To do so, pass the <code>manage_foreign_nodes</code> flag to the wrapper or command line. Be aware though that this will not capture their output - to get their output you will have to use the takeover action from the TUI, which will terminate and restart the node process with the original arguments.</p> Note <p>Foreign node processes are identified by having one of the following parameters in their arguments: <code>__ns</code>, <code>__name</code>, <code>__node</code>, <code>--ros-args</code>. This is always true for nodes started from launch files, but fails when they were started via <code>ros2 run</code> or other means. As far as I'm aware, there is no better way right now.</p>"},{"location":"installation/installation/","title":"Installation","text":"<p>I'm working on getting a proper distro package up and running, but it's a surprisingly non-fun process. Until then you may follow the steps below!</p> Tip <p>better_launch is a regular ROS2 package, which means you can install it in your workspace and then use it in all launch files within that workspace. </p> <p>ROS2 is slowly moving towards pixi as the main python3 environment, but I have not tested it yet. However, by now all the dependencies have been added into rosdep, so the following should get you up and running:</p> <pre><code># Get better_launch into your workspace src folder\ncd &lt;your/ros2/workspace&gt;/src\ngit clone https://github.com/dfki-ric/better_launch.git\n</code></pre> <pre><code># Install the dependencies\nsudo apt update\nrosdep update\nrosdep install --from-paths src --ignore-src -y\n</code></pre> Using a python venv <p>If you prefer a python virtual environment instead of installing system packages, here is a setup that worked for us in the past:</p> <pre><code># Install some prerequisites\nsudo apt install python3-pip python3-venv\n\n# Create a virtual environment for your workspace\ncd your/ros2/workspace/\nmkdir venv\npython3 -m venv ./venv --system-site-packages --symlinks\ntouch venv/COLCON_IGNORE\n\n# Activate the venv\nsource ./venv/bin/activate\n\n# Activate your ROS2 workspace\nsource ./install/setup.bash\n\n# Install the dependencies into your venv\npip install -r path/to/better_launch/requirements.txt\n</code></pre> <p>Once all the dependencies are installed you should build your workspace.</p> <pre><code># Build the better_launch package\ncd &lt;your/ros2/workspace&gt;\ncolcon build --packages-up-to better_launch\nsource install/setup.bash\n</code></pre> <pre><code># Verify installation\nbl --help\n</code></pre> The devel branch <p>If you are the experimental type, better_launch also has a <code>devel</code> branch where I merge new features for testing. I try to keep it functional (since I'm using it myself), although there might be the occasional hiccup.</p>"},{"location":"reference/better_launch/","title":"better_launch","text":""},{"location":"reference/better_launch/#better_launch","title":"better_launch","text":""},{"location":"reference/better_launch/#better_launch.BetterLaunch","title":"BetterLaunch","text":"<p>This should be all you need to create beautiful, simple and convenient launch files!</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.group_root","title":"group_root  <code>property</code>","text":"<pre><code>group_root: Group\n</code></pre> <p>The root group (\"/\").</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.group_tip","title":"group_tip  <code>property</code>","text":"<pre><code>group_tip: Group\n</code></pre> <p>The most recent group.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.is_shutdown","title":"is_shutdown  <code>property</code>","text":"<pre><code>is_shutdown: bool\n</code></pre> <p>Whether better_launch has shutdown.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.launch_args","title":"launch_args  <code>property</code>","text":"<pre><code>launch_args: dict[str, Any]\n</code></pre> <p>All key-value pairs that have been passed to the launch function.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.launchfile","title":"launchfile  <code>property</code>","text":"<pre><code>launchfile: str\n</code></pre> <p>The path of the (main) better_launch launchfile being executed.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.ros_adapter","title":"ros_adapter  <code>property</code>","text":"<pre><code>ros_adapter: ROSAdapter\n</code></pre> <p>Contains and runs a shared ROS2 node to interact with topics, services, etc. The adapter is instantiated lazily as it brings a major performance hit (about 6 MiB memory and a high CPU spike).</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.shared_node","title":"shared_node  <code>property</code>","text":"<pre><code>shared_node: Node\n</code></pre> <p>A ROS2 node instance that can be used for creating publishers, services, etc.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.__init__","title":"__init__","text":"<pre><code>__init__(name: str = None, launch_args: dict[str, Any] = None, root_namespace: str = '/', *, short_unique_names: bool = False)\n</code></pre> <p>Note that BetterLaunch is a singleton: only the first invocation to <code>__init__</code> will succeed. All subsequent calls will return the previous instance. If you need access to the BetterLaunch instance outside your launch function, consider using one of the following classmethods instead:</p> <ul> <li>BetterLaunch.instance</li> <li>BetterLaunch.wait_for_instance</li> </ul> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of this instance, will default to the launchfile's filename.</p> <code>None</code> <code>launch_args</code> <code>dict</code> <p>Override the launch arguments BetterLaunch has access to. By default this will be the launch function's arguments. These will mainly be used for passing to included launch files.</p> <code>None</code> <code>root_namespace</code> <code>str</code> <p>The namespace of the root group.</p> <code>'/'</code> <code>short_unique_names</code> <code>bool</code> <p>If True, use short random hex strings for unique names instead of random words.</p> <code>False</code>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.action_client","title":"action_client","text":"<pre><code>action_client(topic: str, action_type: str | type, timeout: float = 5.0, qos_profile: QoSProfile = None) -&gt; RosActionClient\n</code></pre> <p>Create a ROS2 action client to execute long-running actions.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic namespace on which the action interface is provided.</p> required <code>action_type</code> <code>str | type</code> <p>The type of actions the action server handles. Strings must follow the pattern <code>&lt;package&gt;/action/&lt;message&gt;</code>.</p> required <code>timeout</code> <code>float</code> <p>Time to wait for the action server to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the action server handles connections and retains data.</p> <code>None</code> <p>Returns:</p> Type Description <code>ActionClient</code> <p>The action client object. Although not required for Jazzy and below, it is recommended to keep a reference.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the action server did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.action_server","title":"action_server","text":"<pre><code>action_server(topic: str, action_type: str | type, callback: Callable[[Any], Any], qos_profile: QoSProfile = None) -&gt; RosActionServer\n</code></pre> <p>Create a ROS2 action server using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic namespace to provide the action interface on.</p> required <code>action_type</code> <code>str | type</code> <p>The type of the actions to be handled. Strings must follow the pattern <code>&lt;package&gt;/action/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>A function that will handle incoming action requests. The type of the requests will be of type <code>rclpy.action.server.ServerGoalHandle</code> and contain an <code>action_type.Goal</code>.</p> required <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the action server handles connections and retains data.</p> <code>None</code> <p>Returns:</p> Type Description <code>ActionServer</code> <p>The action server object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.add_shutdown_callback","title":"add_shutdown_callback","text":"<pre><code>add_shutdown_callback(callback: Callable[[], Any]) -&gt; None\n</code></pre> <p>Adds a callback which will be called when better_launch shuts down.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>The callback to call on shutdown.</p> required"},{"location":"reference/better_launch/#better_launch.BetterLaunch.all_ros2_node_names","title":"all_ros2_node_names","text":"<pre><code>all_ros2_node_names() -&gt; list[str]\n</code></pre> <p>Returns a list of all currently registered node's full names (namespace + name).</p> <p>This list is guaranteed to be complete as far as ROS2 is concerned. If you require a node object you can actually interact with consider using query_node or get_nodes instead.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of all running nodes' full names.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.call_service","title":"call_service","text":"<pre><code>call_service(topic: str, service_type: str | type, request_args: dict[str, Any] | Any = None, *, timeout: float = 5.0, qos_profile: QoSProfile = None, call_async: bool = False) -&gt; Any\n</code></pre> <p>Makes a single service request and returns the result. The client is destroyed once the request has been handled. If you plan to make additional requests, use service_client instead.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The service topic to post requests on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>request_args</code> <code>dict[str, Any] | Any</code> <p>The keyword arguments from which the request message will be constructed. May also be an instance of <code>service_type.Request</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Time to wait for the service to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <code>call_async</code> <code>bool</code> <p>If True, make the service call async and return a <code>rclpy.task.Future</code> instead.</p> <code>False</code> <p>Returns:</p> Type Description <code>Any</code> <p>A <code>rclpy.task.Future</code> if <code>call_async</code> is True, otherwise the result of the service call of type <code>service_type.Request</code>.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.component","title":"component","text":"<pre><code>component(package: str, plugin: str, name: str = None, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, anonymous: bool = False, hidden: bool = False, use_intra_process_comms: bool = True, ros_waittime: float = 3.0, lifecycle_waittime: float = 0.01, lifecycle_target: LifecycleStage | str = LifecycleStage.ACTIVE, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, **extra_composer_args: dict[str, Any]) -&gt; Component\n</code></pre> <p>Create a component and load it into an existing compose context.</p> <p>If you instead want to load components without a <code>compose</code> context, you should instantiate Component objects directly, then load them via Component.start or Composer.load_component. See the examples for more details.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the component implementation.</p> required <code>plugin</code> <code>str</code> <p>The name the component is registered as, typically of the form <code>&lt;package&gt;::&lt;Name&gt;</code>.</p> required <code>name</code> <code>str</code> <p>The name the instantiated component should be known as. If <code>None</code>, a name will be derived from <code>package</code> and <code>plugin</code>, and <code>anonymous</code> will be set to True. Pass an empty string instead if you really want to use the node's default name - just know that you'll make a cute kitten really sad.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>anonymous</code> <code>bool</code> <p>If True, the composer name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the composer name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any ROS parameters you want to pass to the component. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>use_intra_process_comms</code> <code>bool</code> <p>If True, ask the composer node to enable intra-process communication, i.e. share memory between components when passing messages instead of serializing and deserializing.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the component to register with ROS. This should cover the time between the process starting and the component initializing itself. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>lifecycle_waittime</code> <code>float</code> <p>How long to wait for the component's lifecycle management to come up. This should cover the time between the component initializing itself (see <code>ros_waittime</code>) and creating its additional topics and services. While neglible on modern computers, slower devices and embedded systems may experience a noticable delay here. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>0.01</code> <code>lifecycle_target</code> <code>LifecycleStage | str</code> <p>The lifecycle stage to bring the component into after starting. Has no effect if <code>autostart_process</code> is False or if the component does not appear to be a lifecycle component after waiting <code>ros_waittime + lifecycle_waittime</code>.</p> <code>ACTIVE</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Returns:</p> Type Description <code>Component</code> <p>The component that has been loaded into the current compose context.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If this is called outside a compose context.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.compose","title":"compose","text":"<pre><code>compose(name: str = None, language: str = 'cpp', variant: Literal['normal', 'multithreading', 'isolated'] = 'normal', *, reuse_existing: bool = True, component_remaps: dict[str, str] = None, anonymous: bool = False, hidden: bool = False, use_sim_time: bool = None, autostart_process: bool = True, ros_waittime: float = 3.0, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN) -&gt; Generator[Composer, None, None]\n</code></pre> <p>Creates a composer node which can be used to load Components. Components can be instantiated directly, or preferably via component. Only components can reside within a composer.</p> <p>Existing composers can be reused even if they have been created outside of better_launch. See Composer for further details.</p> <p>This method should be used as a context, e.g.</p> <p>.. code:: python</p> <pre><code>bl = BetterLaunch()\nwith bl.compose(\"my-composer\"):\n    bl.component(\"my_package\", \"mystuff:TheComponentOfDreams\", \"normal-component\")\n</code></pre> <p>Note that new nodes created by a component are using the composer's remaps. This for example applies to transform listeners (but not transform publishers). See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name you want the composer to be known as. <code>anonymous</code> will be set to True if no name is provided. Pass an empty string if you really want to use the composer node's default name.</p> <code>None</code> <code>language</code> <code>str</code> <p>The implementation of the standard composer you want to use. Ignored if <code>reuse_existing</code> is True and a matching node is found.</p> <code>'cpp'</code> <code>variant</code> <code>Literal['normal', 'multithreading', 'isolated']</code> <p>ROS2 provides special composers for components that need multithreading or should be isolated from the rest. Ignored if <code>reuse_existing</code> is True and a matching node is found.</p> <code>'normal'</code> <code>reuse_existing</code> <code>bool</code> <p>If True and a node matching the current namespace and provided name is found, it will be used instead of creating a new node. This will even work for composers not created through better_launch, although in that case it won't be possible to stop them.</p> <code>True</code> <code>component_remaps</code> <code>dict[str, str]</code> <p>Any remaps you want to apply to all components loaded into this composer.</p> <code>None</code> <code>anonymous</code> <code>bool</code> <p>If True, the composer name will be appended with a unique suffix to avoid name conflicts. <code>reuse_existing</code> will be set to False in this case.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the composer name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>use_sim_time</code> <code>bool</code> <p>If set decides whether the node should use simulated time. Otherwise uses the setting from the current BetterLaunch.group_tip. Only effective when a new composer is created. By ROS2 design, all componentens added to this composer will inherit this setting.</p> <code>None</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the composer process before returning from this function. Note that setting this to False for a composer will make it unusable as a context object, since you won't be able to load any components.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the composer to register with ROS. This should cover the time between the process starting and the composer initializing itself. Set negative to wait indefinitely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Yields:</p> Type Description <code>Generator[Composer, None, None]</code> <p>Sets the composition flag and yields the composer.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If you try to create a composer within a compose context.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.does_node_exist","title":"does_node_exist","text":"<pre><code>does_node_exist(name: str, namespace: str = None) -&gt; bool\n</code></pre> <p>Checks whether the specified node is known to ROS2.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the node.</p> required <code>namespace</code> <code>str</code> <p>If given, a pattern to match against the node's namespace. Supports path-like patterns, e.g. <code>*</code> denotes a wildcard (see <code>fnmatch</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if ROS2 knows the node, False otherwise. Note that there are rare circumstances where a crashed (terminated) node is still reported by ROS. To get an actual handle use query_node instead.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.exec","title":"exec  <code>classmethod</code>","text":"<pre><code>exec(cmd: str | list[str]) -&gt; str\n</code></pre> <p>Run the specified command, await its termination and return its output. Bare commands are resolved using <code>shutil.which</code>.</p> <p>For long-lived commands that you don't want to wait for, consider using BetterLaunch.process instead.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str | list[str]</code> <p>The command to run. If a string is passed it will be split on spaces to separate the command from its arguments. Pass a list instead to have more control over which arguments to treat as a single argument.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The output of the command without the trailing newline.</p> <p>Raises:</p> Type Description <code>CalledProcessError</code> <p>If the command had a non-zero exit code. See the raised error's <code>returncode</code> and <code>output</code> attributes for details.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.find","title":"find","text":"<pre><code>find(package: str = None, filename: str = None, subdir: str = '**') -&gt; str\n</code></pre> <p>Resolve a path to a file or package.</p> <p>If the <code>filename</code> is absolute, all other arguments will be ignored and the filename will be returned.</p> <p>When <code>package</code> names a package discoverable by ament, the corresponding ROS2 package path will be used as the base path. Instead of a package name you may also provide an absolute path, in which case it will become the base path. Any path elements after the package name will be appended to the base path. For example, to find files inside the package's shared files, specify the package as <code>&lt;package&gt;/share</code>.</p> <p>Otherwise, if <code>package</code> was not specified we attempt to locate the current launch file's package by searching its directory and parent directories for a <code>package.xml</code>. If the package cannot be determined an exception is raised.</p> <p><code>subdir</code> accepts glob patterns and can be used to resolve ambiguities, e.g. <code>lib/**</code> (anywhere inside the package's lib folder) or <code>share/</code> (directly inside the share folder). If not specified, \"**\" will be used (any file or directory inside the base path).</p> <p>If only <code>subdir</code> is provided but not <code>filename</code>, the first matching candidate is returned. Otherwise the discovered candidates will be searched for the given filename.</p> <p>If neither <code>subdir</code> nor <code>filename</code> is provided the base path will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>Name of a ROS2 package to resolve.</p> <code>None</code> <code>filename</code> <code>str</code> <p>Name of a file to look for.</p> <code>None</code> <code>subdir</code> <code>str</code> <p>A glob pattern to locate subdirectories and files. See the <code>pathlib pattern language &lt;https://docs.python.org/3/library/pathlib.html#pathlib-pattern-language&gt;</code>_ for details.</p> <code>'**'</code> <p>Returns:</p> Type Description <code>str</code> <p>A resolved path.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the base path could not be determined, or if a <code>filename</code> is provided but could not be found within base path.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.find_group_for_namespace","title":"find_group_for_namespace","text":"<pre><code>find_group_for_namespace(namespace: str, create: bool = False) -&gt; Group\n</code></pre> <p>Find the group representing the passed namespace.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The namespace in question.</p> required <code>create</code> <code>bool</code> <p>If True, create missing groups along the way.</p> <code>False</code> <p>Returns:</p> Type Description <code>Group</code> <p>The group representing the final segment of the namespace, or None if no such group exists and create == False.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.get_foreign_nodes","title":"get_foreign_nodes","text":"<pre><code>get_foreign_nodes() -&gt; list[ForeignNode]\n</code></pre> <p>Lists all running nodes that have a process but have not been started by this better_launch process.</p> <p>Note however that if a process starts multiple nodes, only the first node can be discovered. This is because ROS2 does not provide an API for getting the process parameters from a node.</p> <p>Returns:</p> Type Description <code>list[ForeignNode]</code> <p>All nodes with a process that have not been started by this better_launch process.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.get_groups","title":"get_groups","text":"<pre><code>get_groups() -&gt; list[Group]\n</code></pre> <p>Returns a list of all groups in the order they were created.</p> <p>Returns:</p> Type Description <code>list[Group]</code> <p>All groups added so far.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.get_nodes","title":"get_nodes","text":"<pre><code>get_nodes(*, include_components: bool = False, include_launch_service: bool = True, include_foreign: bool = False) -&gt; list[AbstractNode]\n</code></pre> <p>Returns a list of all nodes in the order they were added. Components will be added right after their composers.</p> <p>Note that this will only return nodes that can be managed by better_launch. If a node process creates multiple nodes only the first node can be discovered, as ROS2 does not provide an API linking a node to its process (or even just its package).</p> <p>Parameters:</p> Name Type Description Default <code>include_components</code> <code>bool</code> <p>Whether to include Component instances. This will not include components that have been loaded from outside (e.g. <code>ros2 component load</code>).</p> <code>False</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service wrapper if it was created. Will be included after the regular nodes and before the foreign nodes.</p> <code>True</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes that have not been started by this launcher instance. Will be appended at the end of the returned nodes. However, take heed of the above warning regarding node discovery.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[AbstractNode]</code> <p>A list of all nodes, sorted by when they were added.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.get_ros_message_type","title":"get_ros_message_type","text":"<pre><code>get_ros_message_type(message_string: str) -&gt; type\n</code></pre> <p>Loads a ROS2 message type from a string representation.</p> <p>Message representations must follow the pattern <code>&lt;package&gt;/&lt;type&gt;/&lt;message&gt;</code>, where *  is the ROS2 package that defines the message. *  is the type of message, typically one of <code>msg</code>, <code>srv</code> or <code>action</code>. *  is the name of the message itself with proper capitalization. <p>Parameters:</p> Name Type Description Default <code>message_string</code> <code>str</code> <p>A message representation of the form <code>&lt;package&gt;/&lt;type&gt;/&lt;message&gt;</code>.</p> required <p>Returns:</p> Type Description <code>type</code> <p>The message class.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If the message type could not be imported.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name(name: str = '', check_running_nodes: bool = True) -&gt; str\n</code></pre> <p>Returns a unique name. If a name is provided it will be prepended with an underscore.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string to use as the base.</p> <code>''</code> <code>check_running_nodes</code> <code>bool</code> <p>If true, check the currently running ROS2 nodes for name collisions.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>A unique name.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.group","title":"group","text":"<pre><code>group(namespace: str, use_sim_time: bool = None) -&gt; Generator[Group, None, None]\n</code></pre> <p>Groups are used to bundle nodes into namespaces. While they influence the nodes' topics and service name, they have no runtime functionality.</p> <p>Groups are intended to be used as context objects and can be nested. Note that starting a new root branch (i.e. a group starting with \"/\") is valid and will change subsequent groups for the duration of the context window.</p> <p>.. code:: python</p> <pre><code>bl = BetterLaunch()\nwith bl.group(\"outer\"):\n    # Unless included in another group, \"outer\" will be attached to \"/\"\n    with bl.group(\"inner/sanctum\"):\n        # Regular nesting, nodes will live within \"/outer/inner/sanctum\"\n        bl.node(...)\n    with bl.group(\"/evil/tower\"):\n        # New root branch, nodes will live within \"/evil/tower\"\n        bl.node(...)\n    # Root branch exited, nodes will live within \"/outer\" once again\n    bl.node(...)\n</code></pre> <p>.. seealso::</p> <pre><code>* [group_root][]\n* [group_tip][]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The group's namespace.</p> required <code>use_sim_time</code> <code>bool</code> <p>Decide whether nodes within this group should use simulated time. Leave as None to use the parent group's use_sim_time setting. The root group defaults to False unless the corresponding environment variable or CLI switch have been modified.</p> <code>None</code> <p>Yields:</p> Type Description <code>Generator[Group, None, None]</code> <p>Places the group on the group stack and yields it. Exiting the context will pop the group from the group stack.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the group is created within a compose context.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.hello","title":"hello","text":"<pre><code>hello() -&gt; None\n</code></pre> <p>Prints our welcome message and some useful information. Note that this will not appear in the logs!</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.include","title":"include","text":"<pre><code>include(package: str, launchfile: str, subdir: str = None, *, pass_launch_func_args: bool = True, **kwargs) -&gt; None\n</code></pre> <p>Include another launch file, resolving its path using find.</p> <p>The file is first read into memory and checked. If it seems to be a better_launch launch file, it is executed immediately (using [exec]). The BetterLaunch instance and global context will be shared. Any arguments to launch_this in the included launch file will be ignored.</p> <p>If the file does not appear to be a better_launch launch file, it is assumed to be a regular ROS2 launch file. In this case a <code>launch.actions.IncludeLaunchDescription</code> instance is created and passed to ros2_actions.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package containing the specified launch file. May be <code>None</code> (see find).</p> required <code>launchfile</code> <code>str</code> <p>The name of a launch file to execute.</p> required <code>subdir</code> <code>str</code> <p>A path fragment the launch file must be located in.</p> <code>None</code> <code>pass_launch_func_args</code> <code>bool</code> <p>If True, all <code>launch_args</code> will be passed to the included launch file. Additional launch arguments can also be provided via <code>kwargs</code>.</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the passed in <code>search_args</code> cannot be handled.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.is_included","title":"is_included  <code>classmethod</code>","text":"<pre><code>is_included() -&gt; bool\n</code></pre> <p>Check if this is run from an included launchfile.</p> <p>NOTE: this will only work when (indirectly) invoked from launch_this.</p> <p>More specifically, this checks if a BetterLaunch instance has been stored in the calling frame's globals, which happens on the first instantiation. This mechanism is an implementation detail and should not be relied on.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if a launch_this function has already run.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When launch_this is not part of the current stack frame.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.load_params","title":"load_params","text":"<pre><code>load_params(package: str = None, configfile: str = None, subdir: str = None, *, qualifier: str | Node = None, strip_qualifiers: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Load parameters from a yaml file located through find.</p> <p>If the config only contains a <code>ros__parameters</code> section the entire config is returned regardless of whether a <code>qualifier</code> was passed. Otherwise, the loaded config dict is searched for a matching section. If no matching section can be found the returned dict will be empty.</p> <p>Globbing is used for matching qualifiers to paths, so the following wildcards are supported: * <code>**</code>: matches any number of tokens, may be followed by additional tokens and a node name * <code>*</code>: skips a single namespace token, or ignores the node's name if at the end</p> <p>Note that better_launch does not require you to place <code>ros__parameters</code> in your configs. If it exists it will later be used to match parameters to namespaces and nodes. For example, a config like</p> <pre><code>my_node:\n    ros__parameters:\n        int_of_fury: 5\n</code></pre> <p>will be passed to a ROS2 node process as <code>-p my_node:int_of_fury:=5</code> and thus become specific to any node named <code>my_node</code>.</p> <p>.. seealso::</p> <pre><code>`ROS2 design doc on wildcards &lt;https://github.com/ros2/design/blob/gh-pages/articles/160_ros_command_line_arguments.md#multiple-parameter-assignments&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>A package to search for the config file. May be <code>None</code> (see find).</p> <code>None</code> <code>configfile</code> <code>str</code> <p>The name of the config file to locate.</p> <code>None</code> <code>subdir</code> <code>str</code> <p>A path fragment that the config file must be located in.</p> <code>None</code> <code>qualifier</code> <code>str</code> <p>Used to specifiy which section of the config to return. E.g. if the yaml contains <code>{A: {B: C, D: E}}</code>, then the qualifier \"A/B\" will return <code>{A: {B: C}}</code>. The qualifier supports globbing patterns like <code>*</code> and <code>**</code> and will ignore <code>ros__parameters</code> keys.</p> <code>None</code> <code>strip_qualifiers</code> <code>bool</code> <p>If True and a qualifier was passed, the qualifier will not be part of the returned dict. Set to True if you want some inner part of the params, e.g. only the params for a specific node. Set to False if you want a slice of the full params, e.g. all params for nodes in a specific namespace.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>The key-value pairs from the config.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the path cannot be resolved, of if <code>qualifier</code> is supplied and no matching section could be found.</p> <code>IOError</code> <p>If the config file could not be read.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.log","title":"log","text":"<pre><code>log(severity: str | int, message: str, *args: list[Any], **kwargs: dict[str, Any]) -&gt; None\n</code></pre> <p>Pass the specified message to the logger.</p> <p>This mostly exists to provide this functionality for TOML launchfiles.</p> <p>Parameters:</p> Name Type Description Default <code>severity</code> <code>str | int</code> <p>A logging severity or level. Standard severities are debug, info, warning, error, critical, and fatal. Integers can be used for more fine grained control and custom log levels, as per the python logging module.</p> required <code>message</code> <code>str</code> <p>The message to log.</p> required <code>args</code> <code>list[Any]</code> <p>A sequence of additional arguments to format the message.</p> <code>()</code> <code>kwargs</code> <code>dict[str, Any]</code> <p>A dict of additional arguments to format the message.</p> <code>{}</code>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.node","title":"node","text":"<pre><code>node(package: str, executable: str, name: str = None, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: str | list[str] = None, use_sim_time: bool = None, drop_param_qualifiers: bool = False, cmd_args: list[str] = None, prefix_args: list[str] = None, env: dict[str, str] = None, isolate_env: bool = False, log_level: int = logging.INFO, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, anonymous: bool = False, hidden: bool = False, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, autostart_process: bool = True, ros_waittime: float = 3.0, lifecycle_waittime: float = 0.01, lifecycle_target: LifecycleStage | str = LifecycleStage.ACTIVE, raw: bool = False, remap_qualifier: str = None, qualify_all_remaps: bool = False) -&gt; Node\n</code></pre> <p>Create a new ROS2 node process. The bread and butter of every ROS setup!</p> <p>Please note that by default better_launch will generate an anonymous node name if no node name was specified. This helps to avoid multiple nodes with the same name, which in ROS2 is both possible and problematic. If you really don't want to specify a node name, pass an empty string instead.</p> <p>This method also handles lifecycle nodes (they REALLY should have a common interface). Note that especially for lifecycle nodes you probably want <code>autostart_process == True</code>, otherwise there lifecycle management will not exist. With <code>autostart_process == True</code>, a lifecycle node will automatically advance to <code>lifecycle_target</code> once it is up. Otherwise you can also call Node.start later.</p> <p>The <code>ROS2 documentation &lt;https://docs.ros.org/en/rolling/How-To-Guides/Node-arguments.html&gt;</code>_ can provide some additional information regarding <code>params</code>, <code>remaps</code>, and so on.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>executable</code> <code>str</code> <p>The executable that should be run.</p> required <code>name</code> <code>str</code> <p>The name you want the node to be known as. If <code>None</code>, a name will be derived from <code>package</code> and <code>executable</code> and <code>anonymous</code> will be set to True. Pass an empty string instead if you really want to use the node's default name - just know that you'll make a cute kitten really sad.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any ROS parameters you want to pass to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>str | list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>use_sim_time</code> <code>bool</code> <p>If set decides whether the node should use simulated time. Otherwise uses the setting from the current BetterLaunch.group_tip.</p> <code>None</code> <code>drop_param_qualifiers</code> <code>bool</code> <p>If True, any namespace/node qualifiers in the passed params are ignored.</p> <code>False</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional command line arguments to pass to the node.</p> <code>None</code> <code>prefix_args</code> <code>list[str]</code> <p>Arguments to prepend to the resolved run command, e.g. for executing the node through gdb.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the node's process. The node process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the node process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used. Be aware that this can result in many common things to not work anymore since e.g. keys like PATH will be missing.</p> <code>False</code> <code>log_level</code> <code>int</code> <p>The minimum severity a logged message from this node must have in order to be published. This will be added to the cmd_args unless it is None.</p> <code>INFO</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>anonymous</code> <code>bool</code> <p>If True, the node name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the node name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the node's process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the node process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the node process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the node executable via the system shell. While this gives access to the shell's builtins, this has the downside of running the node inside a \"mystery program\" which is platform and user dependent. Generally not advised.</p> <code>False</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the node process before returning from this function.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the node to register with ROS. This should cover the time between the process starting and the node initializing itself. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>lifecycle_waittime</code> <code>float</code> <p>How long to wait for the node's lifecycle management to come up. This should cover the time between the node initializing itself (see <code>ros_waittime</code>) and creating its additional topics and services. While neglible on modern computers, slower devices and embedded systems may experience a noticable delay here. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>0.01</code> <code>lifecycle_target</code> <code>LifecycleStage | str</code> <p>The lifecycle stage to bring the node into after starting. Has no effect if <code>autostart_process</code> is False or if the node does not appear to be a lifecycle node after waiting <code>ros_waittime + lifecycle_waittime</code>.</p> <code>ACTIVE</code> <code>raw</code> <code>bool</code> <p>If True, don't treat the executable as a ROS2 node and avoid passing it any command line arguments except those specified.</p> <code>False</code> <code>remap_qualifier</code> <code>str</code> <p>Additional qualifier that will precede the node's <code>__ns</code> and <code>__name</code> remap rules. Should be the original name of the node (i.e. whatever its default name is) and can be qualified with a namespace. Useful to prevent multiple nodes with the same name when a process can have more than one node (e.g. <code>controller_manager</code>). See this ROS2 design doc for more information.</p> <code>None</code> <code>qualify_all_remaps</code> <code>bool</code> <p>If True, apply the <code>remap_qualifier</code> to all remaps that are not already qualified.</p> <code>False</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node object wrapping the node process.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If you try to add a node withing a compose context.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.process","title":"process","text":"<pre><code>process(cmd: str | list[str], name: str = None, *, env: dict[str, str] = None, isolate_env: bool = False, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, anonymous: bool = False, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, autostart_process: bool = True) -&gt; Node\n</code></pre> <p>Starts an arbitrary process and wraps it in a Node object.</p> <p>This method for starting long-running non-ROS processes, similar to ROS2's <code>ExecuteProcess</code>. Bare command names are resolved using <code>shutil.which</code>.</p> <p>If you instead want to wait for the process to return (and retrieve its output), consider using BetterLaunch.exec instead.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str | list[str]</code> <p>The command to execute. If a string is provided, it will be split using <code>shlex.split</code>.</p> required <code>name</code> <code>str</code> <p>The name of the process. If not provided, it will be derived from the command.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the process. The process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used.</p> <code>False</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this process' output should be directed. Defaults to <code>LogSink.SCREEN</code>.</p> <code>SCREEN</code> <code>anonymous</code> <code>bool</code> <p>If True, the process name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the executable via the system shell.</p> <code>False</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the process before returning from this function.</p> <code>True</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node object wrapping the process.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the command is empty.</p> <code>FileNotFoundError</code> <p>If the executable cannot be found.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.publish_message","title":"publish_message","text":"<pre><code>publish_message(topic: str, message_type: str | type, message_args: dict[str, Any], qos_profile: QoSProfile | int = 10, *, time_to_publish: float = 1.0) -&gt; None\n</code></pre> <p>Convenience method to publish a single message. The publisher will be destroyed once the message has been published. If you plan to publish additional messages, use publisher instead and use the instance.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to publish messages on.</p> required <code>message_type</code> <code>str | type</code> <p>The message type that will be published. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>message_args</code> <code>dict[str, Any]</code> <p>The keyword arguments from which the message will be constructed.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <code>time_to_publish</code> <code>float</code> <p>How long to give the publisher to submit the message. Expect the message to get lost if you set this to 0.</p> <code>1.0</code>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.publisher","title":"publisher","text":"<pre><code>publisher(topic: str, message_type: str | type, qos_profile: QoSProfile | int = 10) -&gt; RosPublisher\n</code></pre> <p>Create a ROS2 publisher using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to publish messages on.</p> required <code>message_type</code> <code>str | type</code> <p>The message type that will be published. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <p>Returns:</p> Type Description <code>Publisher</code> <p>The publisher object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.qos_profile","title":"qos_profile","text":"<pre><code>qos_profile(history: Literal['keep_last', 'keep_all', 'default'] | HistoryPolicy = 'keep_all', queue_size: int = 10, reliability: Literal['reliable', 'best_effort', 'default'] | ReliabilityPolicy = 'reliable', durability: Literal['volatile', 'transient_local', 'default'] | DurabilityPolicy = 'volatile', deadline: float = 0, lifespan: float = 0, liveliness: Literal['auto', 'manual', 'default'] | LivelinessPolicy = 'default', alive_timeout: float = 0) -&gt; QoSProfile\n</code></pre> <p>Allows callers to quickly create a QoS profile without a million imports. Any value set to <code>default</code> will use the underlying RMW's default.</p> <p>See Quality of Service settings for details.</p> <p>Parameters:</p> Name Type Description Default <code>history</code> <code>Literal[&amp;quot;keep_last&amp;quot;, &amp;quot;keep_all&amp;quot;, &amp;quot;default&amp;quot;] | HistoryPolicy</code> <p><code>keep_all</code>: store up to N samples according to <code>queue_size</code>; <code>keep_all</code>: store as many samples as the RMW allows.</p> <code>'keep_all'</code> <code>queue_size</code> <code>int</code> <p>Number of samples to keep for <code>history = keep_all</code>.</p> <code>10</code> <code>reliability</code> <code>Literal[&amp;quot;reliable&amp;quot;, &amp;quot;best_effort&amp;quot;, &amp;quot;default&amp;quot;] | ReliabilityPolicy</code> <p><code>reliable</code>: retry sending on errors; <code>best_effort</code>: never retry.</p> <code>'reliable'</code> <code>durability</code> <code>Literal[&amp;quot;volatile&amp;quot;, &amp;quot;transient_local&amp;quot;, &amp;quot;default&amp;quot;] | DurabilityPolicy</code> <p><code>volatile</code>: fire and forget; <code>transient_local</code>: publisher keeps data available for late-joining subscribers. To create a latched topic configure both publisher and subscriber with <code>transient_local</code>.</p> <code>'volatile'</code> <code>deadline</code> <code>float</code> <p>Expected maximum time between published messages.</p> <code>0</code> <code>lifespan</code> <code>float</code> <p>How much time is allowed to pass between sending and receiving the message before it will be marked as stale.</p> <code>0</code> <code>liveliness</code> <code>Literal[&amp;quot;auto&amp;quot;, &amp;quot;manual&amp;quot;, &amp;quot;default&amp;quot;] | LivelinessPolicy</code> <p><code>auto</code>: all publishers of a node are considered alive for the <code>alive_timeout</code> when any one of them fires; <code>manual</code>: publishers have to regularly tell the system that they are alive.</p> <code>'default'</code> <code>alive_timeout</code> <code>float</code> <p>If a publisher doesn't tell the system it's alive for this amount of time it will be considered dead.</p> <code>0</code> <p>Returns:</p> Type Description <code>QoSProfile</code> <p>description</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.query_node","title":"query_node","text":"<pre><code>query_node(pattern: str, *, include_components: bool = True, include_launch_service: bool = False, include_foreign: bool = False) -&gt; AbstractNode\n</code></pre> <p>Retrieve the first node matching the provided pattern.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Either the name of a node, or a qualified node name (i.e. namespace + name). If a namespace is included it must be absolute, but may include <code>*</code> or <code>**</code> wildcards to skip one or more groups (via <code>fnmatch</code>).</p> required <code>include_components</code> <code>bool</code> <p>Whether to include components in the results, if any.</p> <code>True</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service in the result (if it exists).</p> <code>False</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes not created by this launcher.</p> <code>False</code> <p>Returns:</p> Type Description <code>AbstractNode</code> <p>The first node matching the provided pattern, or None if none matched.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.query_nodes","title":"query_nodes","text":"<pre><code>query_nodes(pattern: str, *, include_components: bool = True, include_launch_service: bool = False, include_foreign: bool = False) -&gt; Generator[AbstractNode, None, None]\n</code></pre> <p>Yield all nodes matching the provided pattern.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Either the name of a node, or a qualified node name (i.e. namespace + name). If a namespace is included it must be absolute, but may include <code>*</code> or <code>**</code> wildcards to skip one or more groups (via <code>fnmatch</code>).</p> required <code>include_components</code> <code>bool</code> <p>Whether to include components in the results, if any.</p> <code>True</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service in the result (if it exists).</p> <code>False</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes not created by this launcher.</p> <code>False</code> <p>Returns:</p> Type Description <code>Generator[AbstractNode, None, None]</code> <p>The nodes matching the pattern.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.ros2_actions","title":"ros2_actions","text":"<pre><code>ros2_actions(*ros2_actions) -&gt; Ros2LaunchWrapper\n</code></pre> <p>Submit additional ROS2 launch actions for execution.</p> <p>If no <code>launch.LaunchService</code> exists yet it will be created and started immediately.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.ros2_launch_service","title":"ros2_launch_service","text":"<pre><code>ros2_launch_service(name: str = 'LaunchService', launchservice_args: list[str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, start_immediately: bool = True) -&gt; Ros2LaunchWrapper\n</code></pre> <p>Create or retrieve a manager object that can be used for queueing ROS2 launch actions.</p> <p>Usually, calling ros2_actions is more convenient for queueing actions. However, calling this first allows to prevent starting the underlying <code>launch.LaunchService</code> immediately, giving more control over when the actions are executed.</p> <p>Since the <code>LaunchService</code> insists on running on the main thread it will be started as a sub process.</p> <p>Note that only one instance of the ROS2 wrapper should ever exist. Calling this method after it has been created will return the already existing instance instead. Any passed arguments will be silently discarded.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name used to identify the process and its logger.</p> <code>'LaunchService'</code> <code>launchservice_args</code> <code>list[str]</code> <p>Additional launch arguments to pass to the ROS2 launch service. These will end up in <code>launch.LaunchContext.argv</code>.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>How log output from the launch service should be handled. This will also include the output from all nodes launched by this launch service. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>start_immediately</code> <code>bool</code> <p>If True, the ROS2 launch service process is started immediately.</p> <code>True</code> <p>Returns:</p> Type Description <code>Ros2LaunchWrapper</code> <p>The wrapper hosting the ROS2 launch service process.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.ros_distro","title":"ros_distro  <code>staticmethod</code>","text":"<pre><code>ros_distro() -&gt; str\n</code></pre> <p>Returns the name of the currently sourced ros distro (i.e. $ROS_DISTRO).</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.run_later","title":"run_later","text":"<pre><code>run_later(delay: float, callback: Callable, *args, **kwargs) -&gt; Future\n</code></pre> <p>Convenience method for running a callback with a delay. The callback will be called on a separte thread.</p> <p>This mainly exists to cover the use case where you want to interact with ROS from an <code>rclpy.Timer</code>. A synchronous call from within a timer (e.g. a service call like Node.set_live_params) will block ROS' background event loop, preventing publishers, subscribers, services, etc. from doing their work. It will also prevent a clean shutdown as ROS usually waits for the event queue to become empty.</p> <p>When executing a long running task this way it is a good idea to check is_shutdown in between iterations.</p> <p>Parameters:</p> Name Type Description Default <code>delay</code> <code>float</code> <p>How long to wait in seconds before calling the callback.</p> required <code>callback</code> <code>Callable</code> <p>The function to call after the timeout. Will not be called if shutdown is called beforehand.</p> required <code>*args</code> <code>Any</code> <p>Positional arguments to the callback.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments to the callback.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Future</code> <p>A future which will be cancelled on <code>shutdown</code>, otherwise it will hold the callback's result or an exception the callback raised.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.service","title":"service","text":"<pre><code>service(topic: str, service_type: str | type, callback: Callable[[Any], Any], qos_profile: QoSProfile = None) -&gt; RosServiceProvider\n</code></pre> <p>Create a ROS2 service provider using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic the service will live on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>The function that will handle any requests to the service. The type of the request will be of type <code>service_type.Request</code>.</p> required <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <p>Returns:</p> Type Description <code>Service</code> <p>The service object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.service_client","title":"service_client","text":"<pre><code>service_client(topic: str, service_type: str | type, timeout: float = 5.0, qos_profile: QoSProfile = None) -&gt; RosServiceClient\n</code></pre> <p>Create a ROS2 service client that can be used to call a service.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The service topic to post requests on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>timeout</code> <code>float</code> <p>Time to wait for the service to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <p>Returns:</p> Type Description <code>Client</code> <p>The client object. Although not required for Jazzy and below, it is recommended to keep a reference.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str = None, signum: int = signal.SIGTERM) -&gt; None\n</code></pre> <p>Ask all nodes to shutdown and terminate the internal ROS2 thread. Any subsequent calls to BetterLaunch member functions, including this one, may fail. This will typically be called when you want to terminate your launch file.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string explaining the reason for the shutdown. If not given this will be requitted with a warning.</p> <code>None</code> <code>signum</code> <code>int</code> <p>The signal to send to child processes.</p> <code>SIGTERM</code>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.sleep","title":"sleep","text":"<pre><code>sleep(seconds: float) -&gt; None\n</code></pre> <p>Wait for the specified amount of time.</p> <p>This mostly exists to provide this functionality for TOML launchfiles.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>How many seconds to wait.</p> required"},{"location":"reference/better_launch/#better_launch.BetterLaunch.spin","title":"spin","text":"<pre><code>spin(exit_with_last_node: bool = True) -&gt; None\n</code></pre> <p>Join the BetterLaunch thread until it terminates. You do not need to call this if you're using the launch_this wrapper or the TUI.</p> <p>Parameters:</p> Name Type Description Default <code>exit_with_last_node</code> <code>bool</code> <p>If True this function will return when all nodes have been stopped.</p> <code>True</code>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.subscriber","title":"subscriber","text":"<pre><code>subscriber(topic: str, message_type: str | type, callback: Callable[[Any], None], qos_profile: QoSProfile | int = 10) -&gt; RosSubscriber\n</code></pre> <p>Create a ROS2 subscriber to receive messages.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to listen on for messages.</p> required <code>message_type</code> <code>str | type</code> <p>The type of the messages that will be received. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>A function that will be called whenever a message is received.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <p>Returns:</p> Type Description <code>Subscription</code> <p>The subscriber object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.wait_for_service","title":"wait_for_service","text":"<pre><code>wait_for_service(service: str, timeout: float = None) -&gt; bool\n</code></pre> <p>Wait for the specified service to appear.</p> <p>Parameters:</p> Name Type Description Default <code>service</code> <code>str</code> <p>The full path of the service to wait for.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the service. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the service appeared within the timeout, False otherwise.</p>"},{"location":"reference/better_launch/#better_launch.BetterLaunch.wait_for_topic","title":"wait_for_topic","text":"<pre><code>wait_for_topic(topic: str, timeout: float = None) -&gt; bool\n</code></pre> <p>Wait for the specified topic to appear.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The full path of the topic to wait for.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the topic. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the topic appeared within the timeout, False otherwise.</p>"},{"location":"reference/better_launch/#better_launch.LifecycleStage","title":"LifecycleStage","text":"<p>               Bases: <code>IntEnum</code></p> <p>Represents the main stages of nodes that support lifecycle management.</p>"},{"location":"reference/better_launch/#better_launch.launch_this","title":"launch_this","text":"<pre><code>launch_this(launch_func: Callable = None, *, ui: bool = False, colormode: Colormode = None, print_limit: int = None, screen_log_level: str | int = None, screen_log_format: str = None, file_log_level: str | int = None, file_log_format: str = None, use_sim_time: bool = None, manage_foreign_nodes: bool = False, join: bool = True, keep_alive: bool = False)\n</code></pre> <p>Use this to decorate your launch function. The function will be run automatically. The function is allowed to block even when using the UI.</p> <p>NOTE: this decorator cannot be used more than once per module.</p> <p>Parameters:</p> Name Type Description Default <code>launch_func</code> <code>Callable</code> <p>Your launch function, typically using BetterLaunch to start ROS2 nodes.</p> <code>None</code> <code>ui</code> <code>bool</code> <p>Whether to start the better_launch TUI. Superseded by the <code>BL_UI</code> environment variable and the <code>--bl_ui_override</code> argument.</p> <code>False</code> <code>colormode</code> <code>Colormode</code> <p>Decides what colors will be used for: * default: one color per log severity level and a single color for all message sources * severity: one color per log severity, don't colorize message sources * source: one color per message source, don't colorize log severity * none: don't colorize anything * rainbow: colorize log severity and give each message source its own color Superseded by the <code>BL_COLORMODE</code> environment variable and the <code>--bl_colormode_override</code> argument.</p> <code>None</code> <code>print_limit</code> <code>int</code> <p>Limit the length of messages printed to the screen.</p> <code>None</code> <code>screen_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be printed to the terminal/screen. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>screen_log_format</code> <code>str</code> <p>Customize how log output will be formatted when printing it to the screen. Will be overridden by the <code>BL_SCREEN_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>file_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be written to the lot file. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>file_log_format</code> <code>str</code> <p>Customize how log output will be formatted when writing it to a file. Will be overridden by the <code>BL_FILE_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>manage_foreign_nodes</code> <code>bool</code> <p>If True, the TUI will also include node processes not started by this process. Has no effect if the TUI is not started.</p> <code>False</code> <code>join</code> <code>bool</code> <p>If True, join the better_launch process. Has no effect when ui == True.</p> <code>True</code> <code>keep_alive</code> <code>bool</code> <p>If True, keep the process alive even when all nodes have stopped.</p> <code>False</code>"},{"location":"reference/better_launch/convenience/","title":"convenience","text":""},{"location":"reference/better_launch/convenience/#better_launch.convenience","title":"better_launch.convenience","text":"<p>Additional convenience methods that aren't general enough to be added to the main namespace.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.joint_state_publisher","title":"joint_state_publisher","text":"<pre><code>joint_state_publisher(use_gui: bool = False, node_name: str = None, **kwargs) -&gt; Node\n</code></pre> <p>Starts a <code>joint_state_publisher</code> or <code>joint_state_publisher_gui</code> node.</p> <p>Parameters:</p> Name Type Description Default <code>use_gui</code> <code>bool</code> <p>Whether to use the GUI version of the <code>joint_state_publisher</code>.</p> <code>False</code> <code>node_name</code> <code>str</code> <p>The name of the node. If not provided the name of the executable will be used. Will be anonymized unless <code>anonymous=False</code> is passed.</p> <code>None</code> <code>**kwargs</code> <code>dict</code> <p>Additional arguments to pass to the node (e.g. name, remaps, params, etc.). See BetterLaunch.node.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The spawned node instance.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.read_robot_description","title":"read_robot_description","text":"<pre><code>read_robot_description(package: str, description_file: str, subdir: str = None, *, xacro_args: list[str] = None) -&gt; str | None\n</code></pre> <p>Returns the contents of a robot description after a potential xacro parse.</p> <p>The file is resolved using BetterLaunch.find. If the description file ends with <code>.urdf</code> and <code>xacro_args</code> is not provided, it reads the URDF file directly. Otherwise it runs <code>xacro</code> to generate the URDF from a <code>.xacro</code> file.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package where the robot description file is located. May be <code>None</code> to use this launch file's package (see BetterLaunch.find).</p> required <code>description_file</code> <code>str</code> <p>The name of the robot description file (URDF or XACRO).</p> required <code>subdir</code> <code>str</code> <p>A path fragment the description file must be located in.</p> <code>None</code> <code>xacro_args</code> <code>list of str</code> <p>Additional arguments to pass to <code>xacro</code> when processing <code>.xacro</code> files.</p> <code>None</code> <p>Returns:</p> Type Description <code>str | None</code> <p>The parsed URDF XML as a string if successful, <code>None</code> otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the xacro command encountered an error while processing the file.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.record_topics","title":"record_topics","text":"<pre><code>record_topics(topics: list[str] = None, predicate: Callable[[str], bool] = None, *, bagdir: str = None, max_bag_duration: int = 0, max_bag_size: float = 0.0, recordings: int = 1, wait: bool = True, format: Literal['mcap', 'sqlite3'] = 'mcap', extra_args: list[str] = None) -&gt; None | Node\n</code></pre> <p>Record a rosbag.</p> <p>If topics is empty or not specified all currently published topics are used. The list of topics will be filtered by the predicate if provided.</p> <p>Parameters:</p> Name Type Description Default <code>topics</code> <code>list[str]</code> <p>The ROS topics to record. If not specified all currently known topics will be used instead.</p> <code>None</code> <code>predicate</code> <code>Callable[[str], bool]</code> <p>A function to decide which topics to record. I suggest to use fnmatch for simple wildcards.</p> <code>None</code> <code>bagdir</code> <code>str</code> <p>Where to record the bagfile. If not specified it will use the rosbag default (a timestamped folder in the currend working directory).</p> <code>None</code> <code>max_bag_duration</code> <code>int</code> <p>Start recording a new bagfile after recording for X seconds.</p> <code>0</code> <code>max_bag_size</code> <code>float</code> <p>Start recording a new bagfile after recording X MB.</p> <code>0.0</code> <code>recordings</code> <code>int</code> <p>Make this many recordings each with the set maximum bag duration/size. Continue until interrupted if &lt;= 0. Multiple recordings can only be done when <code>wait</code> is True.</p> <code>1</code> <code>wait</code> <code>bool</code> <p>If True, wait for the recording to finish before returning. Otherwise the process will be wrapped in a elements.Node object and returned. Must be True if <code>recordings</code> is != 1.</p> <code>True</code> <code>format</code> <code>Literal['mcap', 'sqlite3']</code> <p>Rosbag recording format, should be mcap or sqlite3.</p> <code>'mcap'</code> <code>extra_args</code> <code>Iterable[str]</code> <p>Additional arguments that will be passed to rosbag.</p> <code>None</code> <p>Returns:</p> Type Description <code>None | Node</code> <p>Nothing if wait is True, otherwise a elements.Node object wrapping the recording process.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.record_topics_from_file","title":"record_topics_from_file","text":"<pre><code>record_topics_from_file(topic_file: str, predicate: Callable[[str], bool] = None, *, bagdir: str = None, max_bag_duration: int = 0, max_bag_size: float = 0, recordings: int = 1, wait: bool = True, format: Literal['mcap', 'sqlite3'] = 'mcap', extra_args: list[str] = None) -&gt; None | Node\n</code></pre> <p>Record a rosbag.</p> <p>Reads topics to record from a text-file. Empty lines and lines starting with \"#\" will be ignored.</p> <p>Parameters:</p> Name Type Description Default <code>topic_file</code> <code>str</code> <p>The file to read the topic list from.</p> required <code>predicate</code> <code>Callable[[str], bool]</code> <p>A function to decide which topics to record. I suggest to use fnmatch for simple wildcards.</p> <code>None</code> <code>bagdir</code> <code>str</code> <p>Where to record the bagfile. If not specified it will use the rosbag default (a timestamped folder in the currend working directory).</p> <code>None</code> <code>topic_file</code> <code>str</code> <p>Text file with topics to record. Lines starting with \"#\" will be ignored.</p> required <code>max_bag_duration</code> <code>int</code> <p>Start recording a new bagfile after recording for X seconds.</p> <code>0</code> <code>max_bag_size</code> <code>float</code> <p>Start recording a new bagfile after recording X MB.</p> <code>0</code> <code>recordings</code> <code>int</code> <p>Make this many recordings each with the set maximum bag duration/size. Continue until interrupted if &lt;= 0. Multiple recordings can only be done when <code>wait</code> is True.</p> <code>1</code> <code>wait</code> <code>bool</code> <p>If True, wait for the recording to finish before returning. Otherwise the process will be wrapped in a elements.Node object and returned. Must be True if <code>recordings</code> is != 1.</p> <code>True</code> <code>format</code> <code>Literal['mcap', 'sqlite3']</code> <p>Rosbag recording format, should be mcap or sqlite3.</p> <code>'mcap'</code> <code>extra_args</code> <code>Iterable[str]</code> <p>Additional arguments that will be passed to rosbag.</p> <code>None</code> <p>Returns:</p> Type Description <code>None | Node</code> <p>Nothing if wait is True, otherwise a elements.Node object wrapping the recording process.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.robot_state_publisher","title":"robot_state_publisher","text":"<pre><code>robot_state_publisher(robot_description: str = None, *, node_name: str = None, pass_by_topic: bool = False, description_topic: str = '/robot_description', xacro_args: list[str] = None, **kwargs) -&gt; Node\n</code></pre> <p>Start a Robot State Publisher node from a robot description. </p> <p>Parameters:</p> Name Type Description Default <code>robot_description</code> <code>str</code> <p>Robot description the state publisher will use. This can be a urdf/xacro file path (e.g. from BetterLaunch.find), a topic, or an xml string as returned by read_robot_description. See also <code>pass_by_topic</code>. If not set it will be read from <code>/robot_description</code> (subject to remaps).</p> <code>None</code> <code>node_name</code> <code>str</code> <p>The name of the node. If not provided the name of the executable will be used. Will be anonymized unless <code>anonymous=False</code> is passed.</p> <code>None</code> <code>xacro_args</code> <code>list of str</code> <p>Additional arguments to pass to the Xacro processor when a <code>.xacro</code> file was passed.</p> <code>None</code> <code>pass_by_topic</code> <code>bool</code> <p>If True and the value provided for <code>robot_description</code> is either a file or xml string, the description will be passed to the controller manager via a topic rather than a ROS parameter. On ROS versions before lyrical this feature is disabled.</p> <code>False</code> <code>description_topic</code> <code>str</code> <p>The topic under which the robot description will be published if <code>pass_by_topic</code> is True.</p> <code>'/robot_description'</code> <code>**kwargs</code> <code>dict</code> <p>Additional arguments for the node, such as remappings or parameters.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The spawned node instance.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.rviz","title":"rviz","text":"<pre><code>rviz(package: str = None, configfile: str = None, subdir: str = None, *, extra_args: list[str] = None) -&gt; Node\n</code></pre> <p>Runs RViz2 with the given config file and optional warning level suppression.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>Path to locate the config file in (if one is specified).</p> <code>None</code> <code>configfile</code> <code>str</code> <p>Path to the RViz2 configuration file which will be resolved by BetterLaunch.find. Otherwise RViz2 will run with the default config.</p> <code>None</code> <code>subdir</code> <code>str</code> <p>A path fragment the config file must be located in.</p> <code>None</code> <code>extra_args</code> <code>list[str]</code> <p>Additional args to pass to the RViz2 executable.</p> <code>None</code> <p>Returns:</p> Type Description <code>Node</code> <p>The spawned node instance.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.spawn_controller","title":"spawn_controller","text":"<pre><code>spawn_controller(controller: str, params: str | list[str] | dict[str, Any] = None, *, remaps: dict[str, str] = None, cmd_args: list[str] = None, manager: str = 'controller_manager') -&gt; None\n</code></pre> <p>Spawn the specified controller.</p> <p>Note that right now there is no way to directly interact with the loaded controller's node due to the limited API ROS2 provides. I will find a way...</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>str</code> <p>The controller to spwawn.</p> required <code>params</code> <code>str | list[str] | dict[str, Any]</code> <p>Additional parameters for the controller node. Can be a path to a ROS2 config, a list of paths, or a dict with the actual key-value pairs. In versions of ROS before Jazzy the params will be serialized into a temporary yaml file.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Additional remaps specific to the controller. These will be qualified with the controller's name to avoid conflicts.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional CLI arguments to pass to the spawner command (e.g. <code>--load-only</code>).</p> <code>None</code> <code>manager</code> <code>str</code> <p>The name of the controller_manager node.</p> <code>'controller_manager'</code>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.spawn_controller_manager","title":"spawn_controller_manager","text":"<pre><code>spawn_controller_manager(param_files: str | list[str] = None, robot_description: str = None, *, remaps: dict[str, str] = None, params: dict[str, Any] = None, cmd_args: list[str] = None, name: str = 'controller_manager', pass_by_topic: bool = True, description_topic: str = '/robot_description', xacro_args: list[str] = None) -&gt; Node\n</code></pre> <p>Spawn a new controller manager.</p> <p>Parameters:</p> Name Type Description Default <code>param_files</code> <code>str | list[str]</code> <p>One or more config files to be read by the controller manager. These will also be passed on to any controllers loaded and are often the only way to provide them with namespaced parameters.</p> <code>None</code> <code>robot_description</code> <code>str</code> <p>Robot description to pass to the controller. This can be a urdf/xacro file path (e.g. from BetterLaunch.find), a topic, or an xml string as returned by read_robot_description. See also <code>pass_by_topic</code>. If not set the manager's default is used (<code>~/robot_description</code>, subject to remaps).</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Topic remaps for the controller manager, e.g. for the <code>~/robot_description</code> topic it usually subscribes to.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>The controller manager config to use (typically named <code>controller.yaml</code>). If a string is passed it is considered as a path and loaded via BetterLaunch.load_params.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional CLI arguments to pass to the spawner command (e.g. <code>--load-only</code>).</p> <code>None</code> <code>name</code> <code>str</code> <code>'controller_manager'</code> <code>pass_by_topic</code> <code>bool</code> <p>If True and the value provided for <code>robot_description</code> is either a file or xml string, the description will be passed to the controller manager via a topic rather than a ROS parameter. Automatically enabled on jazzy and newer, where passing by parameter is no longer supported.</p> <code>True</code> <code>description_topic</code> <code>str</code> <p>Where to publish the robot description if <code>pass_by_topic</code> is True.</p> <code>'/robot_description'</code> <code>xacro_args</code> <code>list[str]</code> <p>Additional arguments to pass to the Xacro processor when a <code>.xacro</code> robot description file was passed.</p> <p>The name the controller manager node should use. The rename is qualified and so won't affect controllers spawned later (see this document for details). Note however that many nodes and CLI programs (e.g. <code>ros2 control</code>) expect the manager to be named <code>controller_manager</code> and won't work properly otherwise.</p> <code>None</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node running the controller manager process.</p>"},{"location":"reference/better_launch/convenience/#better_launch.convenience.static_transform_publisher","title":"static_transform_publisher","text":"<pre><code>static_transform_publisher(parent_frame: str, child_frame: str, pos: Sequence[float] = None, rot: Sequence[float] = None) -&gt; Node\n</code></pre> <p>Publish a static transform between two frames.</p> <p>Parameters:</p> Name Type Description Default <code>parent_frame</code> <code>str</code> <p>The parent or source frame.</p> required <code>child_frame</code> <code>str</code> <p>The child or target frame.</p> required <code>pos</code> <code>Sequence[float]</code> <p>The xyz-translation from parent to child frame. You may also pass a sequence of 6 or 7 floats in order to specify a full pose. In this case, <code>rot</code> will be ignored.</p> <code>None</code> <code>rot</code> <code>Sequence[float]</code> <p>The rotation between the parent and child frame. If length is 3, the values are interpreted as roll-pitch-yaw euler angles. If length is 4, an xyzw-quaternion is assumed.</p> <code>None</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node running the publisher process.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If pos or rot have the wrong length.</p>"},{"location":"reference/better_launch/declarative/","title":"declarative","text":""},{"location":"reference/better_launch/declarative/#better_launch.declarative","title":"better_launch.declarative","text":""},{"location":"reference/better_launch/declarative/#better_launch.declarative.launch_toml","title":"launch_toml","text":"<pre><code>launch_toml(path: str, launch_args: dict[str, str] = None, *, ui: bool = None, colormode: Colormode = None, print_limit: int = None, screen_log_level: str | int = None, screen_log_format: str = None, file_log_level: str | int = None, file_log_format: str = None, use_sim_time: bool = None, eval_mode: str | EvalMode = None, join: bool = None, manage_foreign_nodes: bool = None, keep_alive: bool = None, allow_kwargs: bool = None) -&gt; None\n</code></pre> <p>Execute a TOML better_launch launchfile.</p> <p>In better_launch TOML launchfiles, most tables will be <code>call tables</code>. A call table is a dict that has a <code>func</code> key referring to one of the public BetterLaunch member functions. All other attributes will be treated as keyword arguments to that function. Call tables are executed in the order they appear in the launch file, and the result of calling their associated function will be stored under the call table's name.</p> <p>For example:</p> <p>.. code-block:: toml     name = \"the-node-of-destiny\"</p> <pre><code>[my_awesome_node]\nfunc = \"node\"\npackage = \"my-package\"\nexecutable = \"my-node\"\nname = \"${name}\"\n</code></pre> <p>Substitutions are also possible and use a similar syntax as in ROS1 (as shown for the <code>name</code> launch argument above). <code>if</code> and <code>unless</code> conditions can be added as well.</p> <p>All parameters below can be set through the launch file by declaring them on the global scope with a <code>bl_</code> prefix (i.e. <code>ui</code> becomes <code>bl_ui</code>).</p> <p>Please see the documentation for full details.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the TOML launchfile to execute.</p> required <code>launch_args</code> <code>dict[str, str]</code> <p>values for launch arguments declared by the launchfile.</p> <code>None</code> <code>eval_mode</code> <code>Literal[&amp;quot;full&amp;quot;, &amp;quot;literal&amp;quot;, &amp;quot;none&amp;quot;]</code> <p>How to treat <code>eval</code> substitutions.</p> <code>None</code> <code>ui</code> <code>bool</code> <p>Whether to start the better_launch TUI. Superseded by the <code>BL_UI</code> environment variable and the <code>--bl_ui_override</code> argument.</p> <code>None</code> <code>allow_kwargs</code> <code>bool</code> <p>Whether additional launch arguments are allowed.</p> <code>None</code> <code>colormode</code> <code>Colormode</code> <p>Decides what colors will be used for: * default: one color per log severity level and a single color for all message sources * severity: one color per log severity, don't colorize message sources * source: one color per message source, don't colorize log severity * none: don't colorize anything * rainbow: colorize log severity and give each message source its own color Superseded by the <code>BL_COLORMODE</code> environment variable and the <code>--bl_colormode_override</code> argument.</p> <code>None</code> <code>print_limit</code> <code>int</code> <p>Limit the length of messages printed to the screen.</p> <code>None</code> <code>screen_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be printed to the terminal/screen. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>screen_log_format</code> <code>str</code> <p>Customize how log output will be formatted when printing it to the screen. Will be overridden by the <code>BL_SCREEN_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>file_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be written to the lot file. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>file_log_format</code> <code>str</code> <p>Customize how log output will be formatted when writing it to a file. Will be overridden by the <code>BL_FILE_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>manage_foreign_nodes</code> <code>bool</code> <p>If True, the TUI will also include node processes not started by this process. Has no effect if the TUI is not started.</p> <code>None</code> <code>join</code> <code>bool</code> <p>If True, join the better_launch process. Has no effect when ui == True.</p> <code>None</code> <code>keep_alive</code> <code>bool</code> <p>If True, keep the process alive even when all nodes have stopped.</p> <code>None</code>"},{"location":"reference/better_launch/elements/","title":"elements","text":""},{"location":"reference/better_launch/elements/#better_launch.elements","title":"better_launch.elements","text":""},{"location":"reference/better_launch/elements/#better_launch.elements.Component","title":"Component","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.component_id","title":"component_id  <code>property</code>","text":"<pre><code>component_id: int\n</code></pre> <p>The ID this component got assigned when it was loaded into the composer. Will be None if the component is not loaded.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.composer","title":"composer  <code>property</code>","text":"<pre><code>composer: Composer\n</code></pre> <p>The composer this component is associated with.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.is_loaded","title":"is_loaded  <code>property</code>","text":"<pre><code>is_loaded: bool\n</code></pre> <p>True if the component is loaded, False otherwise.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.plugin","title":"plugin  <code>property</code>","text":"<pre><code>plugin: str\n</code></pre> <p>The special string that is used for loading the component. executable will return the same.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.__init__","title":"__init__","text":"<pre><code>__init__(composer: Composer, package: str, plugin: str, name: str, namespace: str, *, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, remaps: dict[str, str] = None, params: str | dict[str, Any] = None)\n</code></pre> <p>Representation of a component, a composable object that can be loaded into a running process. Components will always use their composer's namespace.</p> <p>Note that in ROS2 launch files you can reference existing composers by name when creating components. This is a clutch because ROS2 does not have a way to retrieve a reference to an already existing node. Since in better_launch we have actual node instances, referring to composers by name is not supported. Use [BetterLaunch.component` or construct your own component and pass a Composer instance.</p> <p>Also note that since components are loaded via a service call there are some additional restrictions on the types of <code>params</code>. In particular, they must be compatible with the <code>ROS2 Parameter message type &lt;https://github.com/ros2/rcl_interfaces/blob/rolling/rcl_interfaces/msg/ParameterValue.msg&gt;</code>_. This is not verified on construction.</p> <p>In addition, note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>.. seealso::</p> <pre><code>`ROS2 About Composition &lt;https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Composition.html&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>composer</code> <code>Composer</code> <p>The composer this component will be associated with.</p> required <code>package</code> <code>str</code> <p>The package providing this component.</p> required <code>plugin</code> <code>str</code> <p>The special string that can be used for loading the component.</p> required <code>name</code> <code>str</code> <p>The name of the component in ROS.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the component does not reside in the same namespace as its composer.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str, signum: int = signal.SIGTERM, timeout: float = 0.0) -&gt; None\n</code></pre> <p>Unload this component if it was loaded.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string describing why the component is being unloaded.</p> required <code>signum</code> <code>int</code> <p>Ignored for components.</p> <code>SIGTERM</code>"},{"location":"reference/better_launch/elements/#better_launch.elements.Component.start","title":"start","text":"<pre><code>start(use_intra_process_comms: bool = True, **composer_extra_params) -&gt; None\n</code></pre> <p>Load this component into its composer.</p> <p>Additional keyword arguments will be passed as ROS parameters to the component.</p> <p>Parameters:</p> Name Type Description Default <code>use_intra_process_comms</code> <code>bool</code> <p>If True, the component will use intra process communication for exchanging messages with other components within the same composer.</p> <code>True</code>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer","title":"Composer","text":"<p>               Bases: <code>AbstractNode</code></p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.is_lifecycle","title":"is_lifecycle  <code>property</code>","text":"<pre><code>is_lifecycle: bool\n</code></pre> <p>Composers are not lifecycle nodes.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.language","title":"language  <code>property</code>","text":"<pre><code>language: str\n</code></pre> <p>The implementation language of this composer, usually <code>cpp</code> or <code>py</code>. Corresponds to this composer's package (e.g. rclcpp_composition), but will be <code>None</code> if it's a custom implementation.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.managed_components","title":"managed_components  <code>property</code>","text":"<pre><code>managed_components: list[Component]\n</code></pre> <p>The components that were explicitly loaded through this composer instance. This will not contain components that have been loaded via external service calls.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.__init__","title":"__init__","text":"<pre><code>__init__(wrapped_node: AbstractNode, *, component_remaps: dict[str, str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>A composer is a special ROS2 node that can host other nodes (Component) within the same process, reducing overhead and enabling efficient intra process communication for message exchange.</p> <p>As it is possible to reuse already running composers (even without a reference to the actual process), this is merely a wrapper around another [AbstractNode] providing additional functionality. The wrapped node instance is typically a Node or ForeignNode. See BetterLaunch.compose for the most common use cases. </p> <p>Note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>.. seealso::</p> <pre><code>`ROS2 About Composition &lt;https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Composition.html&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>wrapped_node</code> <code>AbstractNode</code> <p>A representation of the actual ROS2 node that will be managed by this composer. This is usually a [Node` or ForeignNode instance.</p> required <code>component_remaps</code> <code>dict[str, str]</code> <p>Any remaps you want to apply to all components loaded into this composer.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the composer mode is not recognized.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.get_live_components","title":"get_live_components","text":"<pre><code>get_live_components() -&gt; dict[int, str]\n</code></pre> <p>Use a service call to retrieve the components currently loaded into this composer and their IDs.</p> <p>Returns:</p> Type Description <code>dict[int, str]</code> <p>A dict mapping component IDs to full node names.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.is_composer","title":"is_composer  <code>classmethod</code>","text":"<pre><code>is_composer(node: AbstractNode, timeout: float = 0.0) -&gt; bool\n</code></pre> <p>Checks whether a node provides services for loading components.</p> <p>For a node to be a composer, it must be running, be registered with ROS and offer the ROS composition services. This method only checks whether one of the key services is present.</p> <p>If a timeout is specified, the check will be repeated until it succeeds or the specified amount of time has passed. This is to ensure that a freshly started node had enough time to create its topics, especially on slower devices. </p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node object to check.</p> required <code>timeout</code> <code>float</code> <p>How long to wait at most for the composition services to appear. Wait forever if None.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node supports loading components, False otherwise.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.load_component","title":"load_component","text":"<pre><code>load_component(component: Component, use_intra_process_comms: bool = True, **composer_extra_params: dict) -&gt; int\n</code></pre> <p>Load this component into its composer.</p> <p>Additional keyword arguments will be passed as ROS parameters to the component. If the component is not associated with this composer yet, a warning will be logged and its association will be updated.</p> <p>Note that since components are loaded via a service call that there are some additional restrictions on the types of <code>Component.params</code> and <code>composer_extra_params</code>. In particular, they must be compatible with the `ROS2 Parameter message type https://github.com/ros2/rcl_interfaces/blob/rolling/rcl_interfaces/msg/ParameterValue.msg][]_.</p> <p>Also note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>Parameters:</p> Name Type Description Default <code>component</code> <code>Component</code> <p>The component to load.</p> required <code>use_intra_process_comms</code> <code>bool</code> <p>If True, the component will use intra process communication for exchanging messages with other components within the same composer.</p> <code>True</code> <p>Returns:</p> Type Description <code>int</code> <p>The ID the component got assigned by ROS.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If this composer is not running, if the component is already loaded, or if the parameters could not be serialized.</p> <code>RuntimeError</code> <p>If loading the component failed.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str, signum: int = signal.SIGTERM, timeout: float = None) -&gt; None\n</code></pre> <p>This will shutdown the composer node. Unloading any loaded components is left to the actual composer implementation.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string describing why the composer and its components are being shutdown.</p> required <code>signum</code> <code>int</code> <p>The signal that should be send to the composer.</p> <code>SIGTERM</code> <code>timeout</code> <code>float</code> <p>How long to wait for each component and the composer to shutdown before returning. Don't wait if timeout is 0.0. Wait forever if timeout is None.</p> <code>None</code> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout &gt; 0 was set and any of the components or the composer did not shutdown before then.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.start","title":"start","text":"<pre><code>start(service_timeout: float = 5.0) -&gt; None\n</code></pre> <p>Start this node. Once this succeeds, is_running will return True.</p> <p>Parameters:</p> Name Type Description Default <code>service_timeout</code> <code>float</code> <p>How long to wait for each composition service to appear (3 total). Wait forever if set negative. Don't check at all if None or 0.</p> <code>5.0</code>"},{"location":"reference/better_launch/elements/#better_launch.elements.Composer.unload_component","title":"unload_component","text":"<pre><code>unload_component(component: Component | int, timeout: float = None) -&gt; None\n</code></pre> <p>Unload the specified component, essentially stopping its node.</p> <p>Note that an unload request will be issued even if the component reports it is not loaded.</p> <p>Parameters:</p> Name Type Description Default <code>component</code> <code>Component | int</code> <p>The component or a component's ID to stop.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the component to be unloaded before returning. Don't wait if timeout is 0.0. Wait forever if timeout is None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if unloading the component succeeded, False otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If this composer is not running.</p> <code>KeyError</code> <p>If the provided component has not been loaded into this node</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode","title":"ForeignNode","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.cmd_args","title":"cmd_args  <code>property</code>","text":"<pre><code>cmd_args: list[str]\n</code></pre> <p>Additional arguments passed to the node process.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.pid","title":"pid  <code>property</code>","text":"<pre><code>pid: int\n</code></pre> <p>The process ID of the node process. Will be -1 if the process is not running.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.__init__","title":"__init__","text":"<pre><code>__init__(process: Process, package: str, name: str, namespace: str, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: list[str] = None, cmd_args: list[str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>This class is used for representing nodes not managed by this better_launch process.</p> <p>Although ROS2 has no mechanism to retrieve e.g. the package name from a running node, most information can be retrieved from the process object itself.</p> <p>In general, you should probably use ForeignNode.wrap_process instead of instantiating this class directly.</p> <p>Parameters:</p> Name Type Description Default <code>process</code> <code>Process</code> <p>A reference to the running node's process.</p> required <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>name</code> <code>str</code> <p>This node's name. If it is a ROS node it should be how the node registers with ROS.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional command line arguments to pass to the node.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Note however that this only applies to output generated by THIS process, not the node's output. To properly capture the node output you must call ForeignNode.takeover first.</p> <code>SCREEN</code>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; int\n</code></pre> <p>Wait for the underlying process to terminate and return its exit code. Returns immediately if the process is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the process to finish. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit code of the process, or None if it is already terminated.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was specified and the process is still running by the time the timeout expires.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Usually a foreign node will already be running upon construction. However, this method can be used when restarting the process after it has terminated.</p> <p>See also takeover.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.takeover","title":"takeover","text":"<pre><code>takeover(kill_after: float = 0, **node_args) -&gt; Node\n</code></pre> <p>Replaces a foreign node with a node belonging to this better_launch process. This allows to e.g. capture the node's output and control a few additional runtime parameters. Any interactions with this foreign node instance after this function returns are undefined behavior.</p> <p>NOTE: Currently the only way to takeover a node is to stop the original process, then recreate and restart the node with the same arguments as the original node.</p> <p>Parameters:</p> Name Type Description Default <code>kill_after</code> <code>float</code> <p>Kill the node process if it takes longer than this many seconds to shutdown. Disabled when &lt;= 0.</p> <code>0</code> <code>node_args</code> <code>dict[str, Any]</code> <p>Additional arguments to pass to the node. See Node for additional details.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The new node instance that should replace this foreign node.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.ForeignNode.wrap_process","title":"wrap_process  <code>classmethod</code>","text":"<pre><code>wrap_process(process: Process) -&gt; ForeignNode\n</code></pre> <p>Collect information like namespace, package, executable and so on from a process and wrap it in a ForeignNode instance.</p> <p>Note that there is no way to verify whether the process actually belongs to a ROS node. Consider using e.g. find_ros2_node_processes to that effect.</p> <p>Parameters:</p> Name Type Description Default <code>process</code> <code>Process</code> <p>The process to wrap.</p> required <p>Returns:</p> Type Description <code>ForeignNode</code> <p>A node object carrying the extracted node information.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Group","title":"Group","text":""},{"location":"reference/better_launch/elements/#better_launch.elements.Group.__init__","title":"__init__","text":"<pre><code>__init__(parent: Group, namespace: str, use_sim_time: bool)\n</code></pre> <p>Groups are used in better_launch to manage node namespaces and common remaps. Beyond that they don't have any meaning for ROS, and there is usually no reason to interact with them directly.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Group</code> <p>This group's parent group.</p> required <code>namespace</code> <code>str</code> <p>The namespace fragment this group represents.</p> required <code>use_sim_time</code> <code>bool</code> <p>Whether nodes inside this group should use simulated time.</p> required"},{"location":"reference/better_launch/elements/#better_launch.elements.Group.add_child","title":"add_child","text":"<pre><code>add_child(child: Group) -&gt; None\n</code></pre> <p>Add a child group to this group.</p> <p>Parameters:</p> Name Type Description Default <code>child</code> <code>Group</code> <p>The group to add.</p> required"},{"location":"reference/better_launch/elements/#better_launch.elements.Group.add_node","title":"add_node","text":"<pre><code>add_node(node: Node) -&gt; None\n</code></pre> <p>Add a node to this group. </p> <p>This group will not do any magic to enforce its namespace or remaps onto the node. Calling this with a node that has been added before will do nothing.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Node</code> <p>The node to add.</p> required"},{"location":"reference/better_launch/elements/#better_launch.elements.Group.assemble_namespace","title":"assemble_namespace","text":"<pre><code>assemble_namespace() -&gt; str\n</code></pre> <p>Return the full namespace string this group represents.</p> <p>Returns:</p> Type Description <code>str</code> <p>This group's namespace path from the root group.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager","title":"LifecycleManager","text":""},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.current_stage","title":"current_stage  <code>property</code>","text":"<pre><code>current_stage: LifecycleStage\n</code></pre> <p>The node's current (that is, last known) lifecycle stage.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.ros_state","title":"ros_state  <code>property</code>","text":"<pre><code>ros_state: int\n</code></pre> <p>The node's current (that is, last known) ROS lifecycle state ID.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.__init__","title":"__init__","text":"<pre><code>__init__(node: AbstractNode)\n</code></pre> <p>Offers additional functionality to manage a node's lifecycle. Verification whether a node supports lifecycle management should happen before this is instantiated.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node who's lifecycle should be managed.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the transition service failed to connect.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.find_transition_path","title":"find_transition_path  <code>classmethod</code>","text":"<pre><code>find_transition_path(start_ros_state: int, goal_ros_state: int) -&gt; list[int]\n</code></pre> <p>Finds a sequence of transitions that will bring a node from an initial lifecycle state (the ROS state, not our LifecycleStage enum) to a target lifecycle state. </p> <p>This is fairly low level and probably never needed.</p> <p>Parameters:</p> Name Type Description Default <code>start_ros_state</code> <code>int</code> <p>The initial ROS lifecycle state.</p> required <code>goal_ros_state</code> <code>int</code> <p>The final ROS lifecycle state.</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>A sequence of ROS lifecycle transitions that form a path from the start state to the goal state.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.is_lifecycle","title":"is_lifecycle  <code>classmethod</code>","text":"<pre><code>is_lifecycle(node: AbstractNode, timeout: float = None) -&gt; bool\n</code></pre> <p>Checks whether a node supports lifecycle management. </p> <p>For a node to support lifecycle management, it must be running, be registered with ROS and offer the ROS lifecycle management services. This method only checks whether one of the key services is present.</p> <p>If a timeout is specified, the check will be repeated until it succeeds or the specified amount of time has passed. This is to ensure that a freshly started node had enough time to create its topics, especially on slower devices. See AbstractNode.is_lifecycle_node for additional information.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node object to check for lifecycle support.</p> required <code>timeout</code> <code>float</code> <p>How long to wait at most for the lifecycle services to appear. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node supports lifecycle management, False otherwise.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleManager.transition","title":"transition","text":"<pre><code>transition(target_stage: LifecycleStage | str) -&gt; bool\n</code></pre> <p>Transition the managed node into the target lifecycle stage. Does nothing if the node is already in the desired stage. </p> <p>Note that you don't have to do step-by-step transitions - simply specify the stage you want the node to end up in and it will go through all the intermediate steps (assuming a path exists).</p> <p>Parameters:</p> Name Type Description Default <code>target_stage</code> <code>LifecycleStage | str</code> <p>The lifecycle stage you want the node to end up in.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the transition sequence succeeded, False if one of the steps failed.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no path to the target stage could be found.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.LifecycleStage","title":"LifecycleStage","text":"<p>               Bases: <code>IntEnum</code></p> <p>Represents the main stages of nodes that support lifecycle management.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Node","title":"Node","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Node.pid","title":"pid  <code>property</code>","text":"<pre><code>pid: int\n</code></pre> <p>The process ID of the node process. Will be -1 if the process is not running.</p>"},{"location":"reference/better_launch/elements/#better_launch.elements.Node.__init__","title":"__init__","text":"<pre><code>__init__(package: str, executable: str, name: str, namespace: str, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: str | list[str] = None, use_sim_time: bool = False, drop_param_qualifiers: bool = False, cmd_args: str | list[str] = None, prefix_args: str | list[str] = None, env: dict[str, str] = None, isolate_env: bool = False, log_level: int = logging.INFO, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, raw: bool = False, remap_qualifier: str = None, qualify_all_remaps: bool = False)\n</code></pre> <p>An object used for starting a ROS node and capturing its output.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>executable</code> <code>str</code> <p>The executable that should be run.</p> required <code>name</code> <code>str</code> <p>The name you want the node to be known as.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>str | list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>drop_param_qualifiers</code> <code>bool</code> <p>If True, any namespace/node qualifiers in the passed params are ignored.</p> <code>False</code> <code>cmd_args</code> <code>str | list[str]</code> <p>Additional command line arguments to pass to the node. If a string is passed it will be split using shlex.</p> <code>None</code> <code>prefix_args</code> <code>str | list[str]</code> <p>Arguments to prepend to the resolved run command, e.g. for executing the node through gdb. If a string is passed it will be split using shlex.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the node's process. The node process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the node process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used. Be aware that this can result in many common things to not work anymore since e.g. keys like PATH will be missing.</p> <code>False</code> <code>log_level</code> <code>int</code> <p>The minimum severity a logged message from this node must have in order to be published. This will be added to the cmd_args unless it is None.</p> <code>INFO</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the node's process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the node process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the node process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the node executable via the system shell. While this gives access to the shell's builtins, this has the downside of running the node inside a \"mystery program\" which is platform and user dependent. Generally not advised.</p> <code>False</code> <code>raw</code> <code>bool</code> <p>If True, don't treat the executable as a ROS2 node and avoid passing it any command line arguments except those specified.</p> <code>False</code> <code>remap_qualifier</code> <code>str</code> <p>Additional qualifier that will precede the node's <code>__ns</code> and <code>__name</code> remap rules. Should be the original name of the node (i.e. whatever its default name is) and can be qualified with a namespace. Useful to prevent multiple nodes with the same name when a process can have more than one node (e.g. <code>controller_manager</code>). See <code>this ROS2 design doc &lt;https://design.ros2.org/articles/static_remapping.html#how-the-syntax-works</code>_ for more information.</p> <code>None</code> <code>qualify_all_remaps</code> <code>bool</code> <p>If True, apply the <code>remap_qualifier</code> to all remaps that are not already qualified.</p> <code>False</code>"},{"location":"reference/better_launch/elements/#better_launch.elements.Node.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; int\n</code></pre> <p>Wait for the underlying process to terminate and return its exit code. Returns immediately if the process is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the process to finish. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit code of the process, or None if it is already terminated.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was specified and the process is still running by the time the timeout expires.</p>"},{"location":"reference/better_launch/elements/abstract_node/","title":"abstract_node","text":""},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node","title":"better_launch.elements.abstract_node","text":""},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode","title":"AbstractNode","text":""},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.executable","title":"executable  <code>property</code>","text":"<pre><code>executable: str\n</code></pre> <p>How this node can be executed. This is not required to be an executable file. It's meaning depends on the node implementation.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.fullname","title":"fullname  <code>property</code>","text":"<pre><code>fullname: str\n</code></pre> <p>The concatenation of this node's namespace and name. Will always start with '/'.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.is_running","title":"is_running  <code>property</code>","text":"<pre><code>is_running: bool\n</code></pre> <p>True if the node is currently running.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.lifecycle","title":"lifecycle  <code>property</code>","text":"<pre><code>lifecycle: LifecycleManager\n</code></pre> <p>Returns this node's LifecyceManager.</p> <p>Note: make sure to call is_lifecycle_node before retrieving this object!</p> <p>Returns:</p> Type Description <code>LifecycleManager</code> <p>The object used for managing this node's lifecycle. Will be None if lifecycle management is not supported or <code>is_lifecycle_node</code> has not been called before.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.name","title":"name  <code>property</code>","text":"<pre><code>name: str\n</code></pre> <p>The name of this node. If this represents a ROS node this will also be the name by which it is known in ROS.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.namespace","title":"namespace  <code>property</code>","text":"<pre><code>namespace: str\n</code></pre> <p>This node's namespace.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.package","title":"package  <code>property</code>","text":"<pre><code>package: str\n</code></pre> <p>The package this node can be found in.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.param_files","title":"param_files  <code>property</code>","text":"<pre><code>param_files: list[str]\n</code></pre> <p>Any param files that should be passed to the node as paths.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.params","title":"params  <code>property</code>","text":"<pre><code>params: dict[str, Any]\n</code></pre> <p>The ROS params that were passed to this node. If a string was passed it is assumed to be a filepath and will be loaded with BetterLaunch.load_params.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.remaps","title":"remaps  <code>property</code>","text":"<pre><code>remaps: dict[str, str]\n</code></pre> <p>Any topic remaps that were passed to this node.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.__init__","title":"__init__","text":"<pre><code>__init__(package: str, executable: str, name: str, namespace: str, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: str | list[str] = None, *, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>Base class for all node-like objects.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package this node can be found in.</p> required <code>executable</code> <code>str</code> <p>How the node can be executed. Not necessarily an executable file object.</p> required <code>name</code> <code>str</code> <p>This node's name. If it is a ROS node it should be how the node registers with ROS.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Topic remaps for this node.</p> <code>None</code> <code>params</code> <code>dict[str, Any]</code> <p>Node parameters. If a string is passed it will be lazy loaded with BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>str | list[str]</code> <p>Paths to parameter files that will be passed to the node as is.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the name is empty or the namespace is not absolute.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.get_info_sheet","title":"get_info_sheet","text":"<pre><code>get_info_sheet() -&gt; str\n</code></pre> <p>Returns a summary of this node's information for display in a terminal.</p> <p>Returns:</p> Type Description <code>str</code> <p>A detailed description of this node.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.get_published_services","title":"get_published_services","text":"<pre><code>get_published_services() -&gt; dict[str, list[str]]\n</code></pre> <p>Get the ROS2 services provided by this node.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>The service topics and message types. Will be empty if is_ros2_connected is False.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.get_published_topics","title":"get_published_topics","text":"<pre><code>get_published_topics() -&gt; dict[str, list[str]]\n</code></pre> <p>Get the ROS2 topics published by this node.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>The topics and their message types. Will be empty if is_ros2_connected is False.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.get_subscribed_topics","title":"get_subscribed_topics","text":"<pre><code>get_subscribed_topics() -&gt; dict[str, list[str]]\n</code></pre> <p>Get the ROS2 topics this node is subscribed to.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>The topics and their message types. Will be empty if is_ros2_connected is False.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.is_lifecycle_node","title":"is_lifecycle_node","text":"<pre><code>is_lifecycle_node(timeout: float = 0.0) -&gt; bool\n</code></pre> <p>Checks if this is a lifecycle node and initializes a : pyLifecycleManager if supported and not done so before.</p> <p>Note that if you simply want to check whether this node supports lifecycle management right now, check whether lifecycle is None will be considerably cheaper.</p> <p>Whether a node supports lifecycle management can only be known from outside once its process is started and it has registered with ROS. When this is called while the node is alive and it supports lifecycle management, a LifecycleManager object will be initialized for it. This will persist even if the node is shutdown, but will obviously no longer provide useful functionality.</p> <p>Note that at the time of writing (Jazzy), the ROS node registers with ROS before the lifecycle topics are created. This makes sense of course, but also means that there is a short window where the node is registered with ROS but not a lifecycle node yet. This can be a problem, especially on slower devices like a Raspberry Pi 3. In these cases I advise you follow this pattern:</p> <p>.. code:: python</p> <pre><code>node = Node(...)\n# Wait until the node is registered in ROS\nif node.is_ros2_connected(timeout=5.0):\n    # Give the node some additional time to create its lifecycle topics\n    if node.is_lifecycle_node(timeout=0.1):\n        # Now the node can be managed\n        node.lifecycle.transition(...)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the node to reveal its lifecycle capabilities. Wait forever if None.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node supports lifecycle management, False otherwise.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.is_ros2_connected","title":"is_ros2_connected","text":"<pre><code>is_ros2_connected(timeout: float = 0.0) -&gt; bool\n</code></pre> <p>Check whether this node is registered within ROS.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the node to sign up with ROS. Wait forever if None.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node can be discovered by ROS, False otherwise.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; None\n</code></pre> <p>Join this node and return once it is shut down. Return immediately if it is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait in seconds. Wait forever if None.</p> <code>None</code> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was set and the node is still running by the time it expires.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str, signum: int = signal.SIGTERM, timeout: float = 0.0) -&gt; None\n</code></pre> <p>Shutdown this node. Once this succeeds, is_running will return False.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string describing why this node is being shutdown.</p> required <code>signum</code> <code>int</code> <p>The signal that should be send to the node (if supported).</p> <code>SIGTERM</code> <code>timeout</code> <code>float</code> <p>How long to wait for the node to shutdown before returning. Don't wait if timeout is 0.0. Wait forever if timeout is None.</p> <code>0.0</code> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout &gt; 0 was set and the node did not shutdown before then.</p>"},{"location":"reference/better_launch/elements/abstract_node/#better_launch.elements.abstract_node.AbstractNode.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start this node. Once this succeeds, is_running will return True.</p>"},{"location":"reference/better_launch/elements/composer/","title":"composer","text":""},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer","title":"better_launch.elements.composer","text":""},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component","title":"Component","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.component_id","title":"component_id  <code>property</code>","text":"<pre><code>component_id: int\n</code></pre> <p>The ID this component got assigned when it was loaded into the composer. Will be None if the component is not loaded.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.composer","title":"composer  <code>property</code>","text":"<pre><code>composer: Composer\n</code></pre> <p>The composer this component is associated with.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.is_loaded","title":"is_loaded  <code>property</code>","text":"<pre><code>is_loaded: bool\n</code></pre> <p>True if the component is loaded, False otherwise.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.plugin","title":"plugin  <code>property</code>","text":"<pre><code>plugin: str\n</code></pre> <p>The special string that is used for loading the component. executable will return the same.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.__init__","title":"__init__","text":"<pre><code>__init__(composer: Composer, package: str, plugin: str, name: str, namespace: str, *, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, remaps: dict[str, str] = None, params: str | dict[str, Any] = None)\n</code></pre> <p>Representation of a component, a composable object that can be loaded into a running process. Components will always use their composer's namespace.</p> <p>Note that in ROS2 launch files you can reference existing composers by name when creating components. This is a clutch because ROS2 does not have a way to retrieve a reference to an already existing node. Since in better_launch we have actual node instances, referring to composers by name is not supported. Use [BetterLaunch.component` or construct your own component and pass a Composer instance.</p> <p>Also note that since components are loaded via a service call there are some additional restrictions on the types of <code>params</code>. In particular, they must be compatible with the <code>ROS2 Parameter message type &lt;https://github.com/ros2/rcl_interfaces/blob/rolling/rcl_interfaces/msg/ParameterValue.msg&gt;</code>_. This is not verified on construction.</p> <p>In addition, note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>.. seealso::</p> <pre><code>`ROS2 About Composition &lt;https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Composition.html&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>composer</code> <code>Composer</code> <p>The composer this component will be associated with.</p> required <code>package</code> <code>str</code> <p>The package providing this component.</p> required <code>plugin</code> <code>str</code> <p>The special string that can be used for loading the component.</p> required <code>name</code> <code>str</code> <p>The name of the component in ROS.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the component does not reside in the same namespace as its composer.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str, signum: int = signal.SIGTERM, timeout: float = 0.0) -&gt; None\n</code></pre> <p>Unload this component if it was loaded.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string describing why the component is being unloaded.</p> required <code>signum</code> <code>int</code> <p>Ignored for components.</p> <code>SIGTERM</code>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Component.start","title":"start","text":"<pre><code>start(use_intra_process_comms: bool = True, **composer_extra_params) -&gt; None\n</code></pre> <p>Load this component into its composer.</p> <p>Additional keyword arguments will be passed as ROS parameters to the component.</p> <p>Parameters:</p> Name Type Description Default <code>use_intra_process_comms</code> <code>bool</code> <p>If True, the component will use intra process communication for exchanging messages with other components within the same composer.</p> <code>True</code>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer","title":"Composer","text":"<p>               Bases: <code>AbstractNode</code></p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.is_lifecycle","title":"is_lifecycle  <code>property</code>","text":"<pre><code>is_lifecycle: bool\n</code></pre> <p>Composers are not lifecycle nodes.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.language","title":"language  <code>property</code>","text":"<pre><code>language: str\n</code></pre> <p>The implementation language of this composer, usually <code>cpp</code> or <code>py</code>. Corresponds to this composer's package (e.g. rclcpp_composition), but will be <code>None</code> if it's a custom implementation.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.managed_components","title":"managed_components  <code>property</code>","text":"<pre><code>managed_components: list[Component]\n</code></pre> <p>The components that were explicitly loaded through this composer instance. This will not contain components that have been loaded via external service calls.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.__init__","title":"__init__","text":"<pre><code>__init__(wrapped_node: AbstractNode, *, component_remaps: dict[str, str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>A composer is a special ROS2 node that can host other nodes (Component) within the same process, reducing overhead and enabling efficient intra process communication for message exchange.</p> <p>As it is possible to reuse already running composers (even without a reference to the actual process), this is merely a wrapper around another [AbstractNode] providing additional functionality. The wrapped node instance is typically a Node or ForeignNode. See BetterLaunch.compose for the most common use cases. </p> <p>Note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>.. seealso::</p> <pre><code>`ROS2 About Composition &lt;https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Composition.html&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>wrapped_node</code> <code>AbstractNode</code> <p>A representation of the actual ROS2 node that will be managed by this composer. This is usually a [Node` or ForeignNode instance.</p> required <code>component_remaps</code> <code>dict[str, str]</code> <p>Any remaps you want to apply to all components loaded into this composer.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the composer mode is not recognized.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.get_live_components","title":"get_live_components","text":"<pre><code>get_live_components() -&gt; dict[int, str]\n</code></pre> <p>Use a service call to retrieve the components currently loaded into this composer and their IDs.</p> <p>Returns:</p> Type Description <code>dict[int, str]</code> <p>A dict mapping component IDs to full node names.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.is_composer","title":"is_composer  <code>classmethod</code>","text":"<pre><code>is_composer(node: AbstractNode, timeout: float = 0.0) -&gt; bool\n</code></pre> <p>Checks whether a node provides services for loading components.</p> <p>For a node to be a composer, it must be running, be registered with ROS and offer the ROS composition services. This method only checks whether one of the key services is present.</p> <p>If a timeout is specified, the check will be repeated until it succeeds or the specified amount of time has passed. This is to ensure that a freshly started node had enough time to create its topics, especially on slower devices. </p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node object to check.</p> required <code>timeout</code> <code>float</code> <p>How long to wait at most for the composition services to appear. Wait forever if None.</p> <code>0.0</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node supports loading components, False otherwise.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.load_component","title":"load_component","text":"<pre><code>load_component(component: Component, use_intra_process_comms: bool = True, **composer_extra_params: dict) -&gt; int\n</code></pre> <p>Load this component into its composer.</p> <p>Additional keyword arguments will be passed as ROS parameters to the component. If the component is not associated with this composer yet, a warning will be logged and its association will be updated.</p> <p>Note that since components are loaded via a service call that there are some additional restrictions on the types of <code>Component.params</code> and <code>composer_extra_params</code>. In particular, they must be compatible with the `ROS2 Parameter message type https://github.com/ros2/rcl_interfaces/blob/rolling/rcl_interfaces/msg/ParameterValue.msg][]_.</p> <p>Also note that new nodes created by a component are using the composer's remaps. See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>Parameters:</p> Name Type Description Default <code>component</code> <code>Component</code> <p>The component to load.</p> required <code>use_intra_process_comms</code> <code>bool</code> <p>If True, the component will use intra process communication for exchanging messages with other components within the same composer.</p> <code>True</code> <p>Returns:</p> Type Description <code>int</code> <p>The ID the component got assigned by ROS.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If this composer is not running, if the component is already loaded, or if the parameters could not be serialized.</p> <code>RuntimeError</code> <p>If loading the component failed.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str, signum: int = signal.SIGTERM, timeout: float = None) -&gt; None\n</code></pre> <p>This will shutdown the composer node. Unloading any loaded components is left to the actual composer implementation.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string describing why the composer and its components are being shutdown.</p> required <code>signum</code> <code>int</code> <p>The signal that should be send to the composer.</p> <code>SIGTERM</code> <code>timeout</code> <code>float</code> <p>How long to wait for each component and the composer to shutdown before returning. Don't wait if timeout is 0.0. Wait forever if timeout is None.</p> <code>None</code> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout &gt; 0 was set and any of the components or the composer did not shutdown before then.</p>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.start","title":"start","text":"<pre><code>start(service_timeout: float = 5.0) -&gt; None\n</code></pre> <p>Start this node. Once this succeeds, is_running will return True.</p> <p>Parameters:</p> Name Type Description Default <code>service_timeout</code> <code>float</code> <p>How long to wait for each composition service to appear (3 total). Wait forever if set negative. Don't check at all if None or 0.</p> <code>5.0</code>"},{"location":"reference/better_launch/elements/composer/#better_launch.elements.composer.Composer.unload_component","title":"unload_component","text":"<pre><code>unload_component(component: Component | int, timeout: float = None) -&gt; None\n</code></pre> <p>Unload the specified component, essentially stopping its node.</p> <p>Note that an unload request will be issued even if the component reports it is not loaded.</p> <p>Parameters:</p> Name Type Description Default <code>component</code> <code>Component | int</code> <p>The component or a component's ID to stop.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the component to be unloaded before returning. Don't wait if timeout is 0.0. Wait forever if timeout is None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if unloading the component succeeded, False otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If this composer is not running.</p> <code>KeyError</code> <p>If the provided component has not been loaded into this node</p>"},{"location":"reference/better_launch/elements/foreign_node/","title":"foreign_node","text":""},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node","title":"better_launch.elements.foreign_node","text":""},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode","title":"ForeignNode","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.cmd_args","title":"cmd_args  <code>property</code>","text":"<pre><code>cmd_args: list[str]\n</code></pre> <p>Additional arguments passed to the node process.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.pid","title":"pid  <code>property</code>","text":"<pre><code>pid: int\n</code></pre> <p>The process ID of the node process. Will be -1 if the process is not running.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.__init__","title":"__init__","text":"<pre><code>__init__(process: Process, package: str, name: str, namespace: str, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: list[str] = None, cmd_args: list[str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>This class is used for representing nodes not managed by this better_launch process.</p> <p>Although ROS2 has no mechanism to retrieve e.g. the package name from a running node, most information can be retrieved from the process object itself.</p> <p>In general, you should probably use ForeignNode.wrap_process instead of instantiating this class directly.</p> <p>Parameters:</p> Name Type Description Default <code>process</code> <code>Process</code> <p>A reference to the running node's process.</p> required <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>name</code> <code>str</code> <p>This node's name. If it is a ROS node it should be how the node registers with ROS.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional command line arguments to pass to the node.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Note however that this only applies to output generated by THIS process, not the node's output. To properly capture the node output you must call ForeignNode.takeover first.</p> <code>SCREEN</code>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; int\n</code></pre> <p>Wait for the underlying process to terminate and return its exit code. Returns immediately if the process is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the process to finish. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit code of the process, or None if it is already terminated.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was specified and the process is still running by the time the timeout expires.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Usually a foreign node will already be running upon construction. However, this method can be used when restarting the process after it has terminated.</p> <p>See also takeover.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.takeover","title":"takeover","text":"<pre><code>takeover(kill_after: float = 0, **node_args) -&gt; Node\n</code></pre> <p>Replaces a foreign node with a node belonging to this better_launch process. This allows to e.g. capture the node's output and control a few additional runtime parameters. Any interactions with this foreign node instance after this function returns are undefined behavior.</p> <p>NOTE: Currently the only way to takeover a node is to stop the original process, then recreate and restart the node with the same arguments as the original node.</p> <p>Parameters:</p> Name Type Description Default <code>kill_after</code> <code>float</code> <p>Kill the node process if it takes longer than this many seconds to shutdown. Disabled when &lt;= 0.</p> <code>0</code> <code>node_args</code> <code>dict[str, Any]</code> <p>Additional arguments to pass to the node. See Node for additional details.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The new node instance that should replace this foreign node.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.ForeignNode.wrap_process","title":"wrap_process  <code>classmethod</code>","text":"<pre><code>wrap_process(process: Process) -&gt; ForeignNode\n</code></pre> <p>Collect information like namespace, package, executable and so on from a process and wrap it in a ForeignNode instance.</p> <p>Note that there is no way to verify whether the process actually belongs to a ROS node. Consider using e.g. find_ros2_node_processes to that effect.</p> <p>Parameters:</p> Name Type Description Default <code>process</code> <code>Process</code> <p>The process to wrap.</p> required <p>Returns:</p> Type Description <code>ForeignNode</code> <p>A node object carrying the extracted node information.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.find_foreign_nodes","title":"find_foreign_nodes","text":"<pre><code>find_foreign_nodes() -&gt; list[AbstractNode]\n</code></pre> <p>Searches the running processes for ROS2 nodes that have not been started by this better_launch process and wraps them in ForeignNode instances.</p> <p>Returns:</p> Type Description <code>list[AbstractNode]</code> <p>A list of discovered foreign ROS2 nodes.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.find_process_for_node","title":"find_process_for_node","text":"<pre><code>find_process_for_node(namespace: str, name: str) -&gt; list[psutil.Process]\n</code></pre> <p>Find processes that look like ROS2 nodes which have been passed the specified namespace and name.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The namespace to look for.</p> required <code>name</code> <code>str</code> <p>The node name to look for.</p> required <p>Returns:</p> Type Description <code>list[Process]</code> <p>A list processes that match the above criteria.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.find_ros2_node_processes","title":"find_ros2_node_processes","text":"<pre><code>find_ros2_node_processes() -&gt; list[psutil.Process]\n</code></pre> <p>Finds processes that seem to be ROS2 nodes.</p> <p>Unfortunately, ROS2 doesn't provide any means of discovering node internals other than by looking at the process command line. Lucky for us, there are a couple of distinct command line arguments that are somewhat unique to ROS. These are: - --ros-args for passing arguments - __ns:= - __node:= - __name:= <p>If any of these are present, the process will be added to the returned list.</p> <p>Returns:</p> Type Description <code>list[Process]</code> <p>The processes that appear to be ROS2 nodes.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.get_package_for_path","title":"get_package_for_path","text":"<pre><code>get_package_for_path(path: str) -&gt; tuple[str, str]\n</code></pre> <p>Find the ROS2 package associated with the specified path.</p> <p>This will first check the currently registered packages (which are stored in <code>$AMENT_PREFIX_PATH</code>). If none of these match it will search through the path starting from the end for a valid <code>package.xml</code> file to get the package name.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>An absolute path to find the package for.</p> required <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>The package name and path to the package, or (None, None) if the package could not be determined.</p>"},{"location":"reference/better_launch/elements/foreign_node/#better_launch.elements.foreign_node.parse_process_args","title":"parse_process_args","text":"<pre><code>parse_process_args(process: Process, node: AbstractNode = None) -&gt; tuple[str, str, dict[str, str], dict[str, str], list[str], list[str]]\n</code></pre> <p>Parse ROS2 command line arguments and return the user-specified parts.</p> <p>In particular, this will return the passed ROS2 params, remaps and additional command line arguments. Special ROS2 arguments like <code>--ros-args</code>, <code>--remap</code>, etc. will not be included. A node may be passed in order to resolve <code>nodename:key:=value</code> style args and load parameter files from <code>--params-file</code>.</p> <p>Parameters:</p> Name Type Description Default <code>process</code> <code>Process</code> <p>The process to get the cmd args from.</p> required <code>node</code> <code>AbstractNode</code> <p>A node to use when resolving additional details.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[dict[str, str], dict[str, str], list[str]]</code> <p>The node's (namespace, name, remaps, params, param_files, cmd_args).</p>"},{"location":"reference/better_launch/elements/group/","title":"group","text":""},{"location":"reference/better_launch/elements/group/#better_launch.elements.group","title":"better_launch.elements.group","text":""},{"location":"reference/better_launch/elements/group/#better_launch.elements.group.Group","title":"Group","text":""},{"location":"reference/better_launch/elements/group/#better_launch.elements.group.Group.__init__","title":"__init__","text":"<pre><code>__init__(parent: Group, namespace: str, use_sim_time: bool)\n</code></pre> <p>Groups are used in better_launch to manage node namespaces and common remaps. Beyond that they don't have any meaning for ROS, and there is usually no reason to interact with them directly.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Group</code> <p>This group's parent group.</p> required <code>namespace</code> <code>str</code> <p>The namespace fragment this group represents.</p> required <code>use_sim_time</code> <code>bool</code> <p>Whether nodes inside this group should use simulated time.</p> required"},{"location":"reference/better_launch/elements/group/#better_launch.elements.group.Group.add_child","title":"add_child","text":"<pre><code>add_child(child: Group) -&gt; None\n</code></pre> <p>Add a child group to this group.</p> <p>Parameters:</p> Name Type Description Default <code>child</code> <code>Group</code> <p>The group to add.</p> required"},{"location":"reference/better_launch/elements/group/#better_launch.elements.group.Group.add_node","title":"add_node","text":"<pre><code>add_node(node: Node) -&gt; None\n</code></pre> <p>Add a node to this group. </p> <p>This group will not do any magic to enforce its namespace or remaps onto the node. Calling this with a node that has been added before will do nothing.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Node</code> <p>The node to add.</p> required"},{"location":"reference/better_launch/elements/group/#better_launch.elements.group.Group.assemble_namespace","title":"assemble_namespace","text":"<pre><code>assemble_namespace() -&gt; str\n</code></pre> <p>Return the full namespace string this group represents.</p> <p>Returns:</p> Type Description <code>str</code> <p>This group's namespace path from the root group.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/","title":"lifecycle_manager","text":""},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager","title":"better_launch.elements.lifecycle_manager","text":""},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager","title":"LifecycleManager","text":""},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.current_stage","title":"current_stage  <code>property</code>","text":"<pre><code>current_stage: LifecycleStage\n</code></pre> <p>The node's current (that is, last known) lifecycle stage.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.ros_state","title":"ros_state  <code>property</code>","text":"<pre><code>ros_state: int\n</code></pre> <p>The node's current (that is, last known) ROS lifecycle state ID.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.__init__","title":"__init__","text":"<pre><code>__init__(node: AbstractNode)\n</code></pre> <p>Offers additional functionality to manage a node's lifecycle. Verification whether a node supports lifecycle management should happen before this is instantiated.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node who's lifecycle should be managed.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the transition service failed to connect.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.find_transition_path","title":"find_transition_path  <code>classmethod</code>","text":"<pre><code>find_transition_path(start_ros_state: int, goal_ros_state: int) -&gt; list[int]\n</code></pre> <p>Finds a sequence of transitions that will bring a node from an initial lifecycle state (the ROS state, not our LifecycleStage enum) to a target lifecycle state. </p> <p>This is fairly low level and probably never needed.</p> <p>Parameters:</p> Name Type Description Default <code>start_ros_state</code> <code>int</code> <p>The initial ROS lifecycle state.</p> required <code>goal_ros_state</code> <code>int</code> <p>The final ROS lifecycle state.</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>A sequence of ROS lifecycle transitions that form a path from the start state to the goal state.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.is_lifecycle","title":"is_lifecycle  <code>classmethod</code>","text":"<pre><code>is_lifecycle(node: AbstractNode, timeout: float = None) -&gt; bool\n</code></pre> <p>Checks whether a node supports lifecycle management. </p> <p>For a node to support lifecycle management, it must be running, be registered with ROS and offer the ROS lifecycle management services. This method only checks whether one of the key services is present.</p> <p>If a timeout is specified, the check will be repeated until it succeeds or the specified amount of time has passed. This is to ensure that a freshly started node had enough time to create its topics, especially on slower devices. See AbstractNode.is_lifecycle_node for additional information.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>AbstractNode</code> <p>The node object to check for lifecycle support.</p> required <code>timeout</code> <code>float</code> <p>How long to wait at most for the lifecycle services to appear. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node supports lifecycle management, False otherwise.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleManager.transition","title":"transition","text":"<pre><code>transition(target_stage: LifecycleStage | str) -&gt; bool\n</code></pre> <p>Transition the managed node into the target lifecycle stage. Does nothing if the node is already in the desired stage. </p> <p>Note that you don't have to do step-by-step transitions - simply specify the stage you want the node to end up in and it will go through all the intermediate steps (assuming a path exists).</p> <p>Parameters:</p> Name Type Description Default <code>target_stage</code> <code>LifecycleStage | str</code> <p>The lifecycle stage you want the node to end up in.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the transition sequence succeeded, False if one of the steps failed.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no path to the target stage could be found.</p>"},{"location":"reference/better_launch/elements/lifecycle_manager/#better_launch.elements.lifecycle_manager.LifecycleStage","title":"LifecycleStage","text":"<p>               Bases: <code>IntEnum</code></p> <p>Represents the main stages of nodes that support lifecycle management.</p>"},{"location":"reference/better_launch/elements/live_params_mixin/","title":"live_params_mixin","text":""},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin","title":"better_launch.elements.live_params_mixin","text":""},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin.LiveParamsMixin","title":"LiveParamsMixin","text":"<p>Mixin class to add interactions with ROS parameters for running nodes. This class must be mixed in with an object providing a <code>fullname</code> member.</p>"},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin.LiveParamsMixin.get_live_params","title":"get_live_params","text":"<pre><code>get_live_params(*params: str, timeout: float = 5.0) -&gt; dict[str, Any]\n</code></pre> <p>Retrieves the values for the specified ROS parameters of this node. If no parameters are provided, all parameters will be listed.</p> <p>Note that this is different from the <code>params</code> used to start the node. This method will directly query the ROS node for its parameters, while <code>params</code> is used to pass arguments on startup.</p> <p>Parameters:</p> Name Type Description Default <code>*params</code> <code>str</code> <p>ROS parameter names to retrieve.</p> <code>()</code> <code>timeout</code> <code>float</code> <p>How long to wait for the service to connect.</p> <code>5.0</code> <p>Returns:</p> Type Description <code>Any</code> <p>The names and values of the ROS parameters of this node as specified.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service fails to connect.</p>"},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin.LiveParamsMixin.list_live_params","title":"list_live_params","text":"<pre><code>list_live_params(*, timeout: float = 5.0) -&gt; list[str]\n</code></pre> <p>List the names of the ROS parameters this node has registered.</p> <p>Note that this is different from the <code>params</code> used to start the node. This method will directly query the ROS node for its parameters, while <code>params</code> is used to pass arguments on startup.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the service to connect.</p> <code>5.0</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of this node's ROS parameters.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service fails to connect.</p>"},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin.LiveParamsMixin.set_live_params","title":"set_live_params","text":"<pre><code>set_live_params(params: dict[str, Any], *, timeout: float = 5.0) -&gt; dict[str, bool]\n</code></pre> <p>Sets the specified ROS parameters on this node.</p> <p>Note that this is different from the <code>params</code> used to start the node. This method will directly query the ROS node for its parameters, while <code>params</code> is used to pass arguments on startup.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict[str, Any]</code> <p>The parameters to update.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the service to connect.</p> <code>5.0</code> <p>Returns:</p> Type Description <code>dict[str, bool]:</code> <p>A dict showing which parameter updates succeeded.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service fails to connect.</p>"},{"location":"reference/better_launch/elements/live_params_mixin/#better_launch.elements.live_params_mixin.LiveParamsMixin.set_live_params_atomic","title":"set_live_params_atomic","text":"<pre><code>set_live_params_atomic(params: dict[str, Any], *, timeout: float = 5.0) -&gt; bool\n</code></pre> <p>Sets the specified ROS parameters on this node. No updates will be performed if any of the operations fail.</p> <p>Note that this is different from the <code>params</code> used to start the node. This method will directly query the ROS node for its parameters, while <code>params</code> is used to pass arguments on startup.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict[str, Any]</code> <p>The parameters to update.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the service to connect.</p> <code>5.0</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all updates succeeded, False otherwise.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service fails to connect.</p>"},{"location":"reference/better_launch/elements/node/","title":"node","text":""},{"location":"reference/better_launch/elements/node/#better_launch.elements.node","title":"better_launch.elements.node","text":""},{"location":"reference/better_launch/elements/node/#better_launch.elements.node.Node","title":"Node","text":"<p>               Bases: <code>AbstractNode</code>, <code>LiveParamsMixin</code></p>"},{"location":"reference/better_launch/elements/node/#better_launch.elements.node.Node.pid","title":"pid  <code>property</code>","text":"<pre><code>pid: int\n</code></pre> <p>The process ID of the node process. Will be -1 if the process is not running.</p>"},{"location":"reference/better_launch/elements/node/#better_launch.elements.node.Node.__init__","title":"__init__","text":"<pre><code>__init__(package: str, executable: str, name: str, namespace: str, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: str | list[str] = None, use_sim_time: bool = False, drop_param_qualifiers: bool = False, cmd_args: str | list[str] = None, prefix_args: str | list[str] = None, env: dict[str, str] = None, isolate_env: bool = False, log_level: int = logging.INFO, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, raw: bool = False, remap_qualifier: str = None, qualify_all_remaps: bool = False)\n</code></pre> <p>An object used for starting a ROS node and capturing its output.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>executable</code> <code>str</code> <p>The executable that should be run.</p> required <code>name</code> <code>str</code> <p>The name you want the node to be known as.</p> required <code>namespace</code> <code>str</code> <p>The node's namespace. Must be absolute, i.e. start with a '/'.</p> required <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any arguments you want to provide to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>str | list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>drop_param_qualifiers</code> <code>bool</code> <p>If True, any namespace/node qualifiers in the passed params are ignored.</p> <code>False</code> <code>cmd_args</code> <code>str | list[str]</code> <p>Additional command line arguments to pass to the node. If a string is passed it will be split using shlex.</p> <code>None</code> <code>prefix_args</code> <code>str | list[str]</code> <p>Arguments to prepend to the resolved run command, e.g. for executing the node through gdb. If a string is passed it will be split using shlex.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the node's process. The node process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the node process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used. Be aware that this can result in many common things to not work anymore since e.g. keys like PATH will be missing.</p> <code>False</code> <code>log_level</code> <code>int</code> <p>The minimum severity a logged message from this node must have in order to be published. This will be added to the cmd_args unless it is None.</p> <code>INFO</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the node's process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the node process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the node process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the node executable via the system shell. While this gives access to the shell's builtins, this has the downside of running the node inside a \"mystery program\" which is platform and user dependent. Generally not advised.</p> <code>False</code> <code>raw</code> <code>bool</code> <p>If True, don't treat the executable as a ROS2 node and avoid passing it any command line arguments except those specified.</p> <code>False</code> <code>remap_qualifier</code> <code>str</code> <p>Additional qualifier that will precede the node's <code>__ns</code> and <code>__name</code> remap rules. Should be the original name of the node (i.e. whatever its default name is) and can be qualified with a namespace. Useful to prevent multiple nodes with the same name when a process can have more than one node (e.g. <code>controller_manager</code>). See <code>this ROS2 design doc &lt;https://design.ros2.org/articles/static_remapping.html#how-the-syntax-works</code>_ for more information.</p> <code>None</code> <code>qualify_all_remaps</code> <code>bool</code> <p>If True, apply the <code>remap_qualifier</code> to all remaps that are not already qualified.</p> <code>False</code>"},{"location":"reference/better_launch/elements/node/#better_launch.elements.node.Node.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; int\n</code></pre> <p>Wait for the underlying process to terminate and return its exit code. Returns immediately if the process is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the process to finish. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit code of the process, or None if it is already terminated.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was specified and the process is still running by the time the timeout expires.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/","title":"ros2_launch_wrapper","text":""},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper","title":"better_launch.elements.ros2_launch_wrapper","text":""},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper","title":"Ros2LaunchWrapper","text":"<p>               Bases: <code>AbstractNode</code></p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.launchservice_args","title":"launchservice_args  <code>property</code>","text":"<pre><code>launchservice_args: list[str]\n</code></pre> <p>Additional arguments that are passed to the launch service.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.pid","title":"pid  <code>property</code>","text":"<pre><code>pid: int\n</code></pre> <p>The process ID of the node process. Will be -1 if the process is not running.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.__init__","title":"__init__","text":"<pre><code>__init__(name: str = 'LaunchService', launchservice_args: list[str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN)\n</code></pre> <p>Hosts a separate process running a ROS2 <code>LaunchService</code> instance (the main entrypoint of the ROS2 launch system).</p> <p>Note that although the ROS2 launch service may start an arbitrary number of nodes they will not be accessible for interaction beyond showing log output. Output on stdout and stderr from the process (and its nodes) will be captured, reformatted and separated by source.</p> <p>While this is a node-like object, it does not represent a node in ROS. This was done so that e.g. the TUI can be used to interact with the ROS2 launch system to e.g. include regular ROS2 launch files. Running a full process and a separate launch system is fairly resource heavy, however, <code>LaunchService</code> insists on running on the main thread.</p> <p>.. seealso::</p> <pre><code>`ROS2 LaunchService &lt;https://github.com/ros2/launch/blob/rolling/launch/launch/launch_service.py&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name that will be used for the logger and the child process.</p> <code>'LaunchService'</code> <code>launchservice_args</code> <code>list[str]</code> <p>Additional arguments to pass to the ROS2 launch service. These will show up in the ROS2 <code>LaunchContext</code>.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>How log output from the launch service should be handled. This will also include the output from all nodes launched by this launch service. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.describe_launch_actions","title":"describe_launch_actions","text":"<pre><code>describe_launch_actions() -&gt; str\n</code></pre> <p>Returns a best-effort summary of all <code>LaunchDescriptions</code> that have been loaded so far.</p> <p>Since ROS2 does not provide a way to serialize its slew of launch actions (beyond maybe pickle), it is not possible to create a full representation without special handlers. However, most launch actions expose their relevant data as properties. This function will look for these and recurse into them if they return launch actions.</p> <p>Returns:</p> Type Description <code>str</code> <p>A formatted and indented representation of the launch descriptions scheduled for loading thus far.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.is_lifecycle_node","title":"is_lifecycle_node","text":"<pre><code>is_lifecycle_node(timeout: float = None) -&gt; bool\n</code></pre> <p>This is never a lifecycle node.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.is_ros2_connected","title":"is_ros2_connected","text":"<pre><code>is_ros2_connected(timeout: float = None) -&gt; bool\n</code></pre> <p>Equal to is_running for this class.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.join","title":"join","text":"<pre><code>join(timeout: float = None) -&gt; None\n</code></pre> <p>Wait for the underlying process to terminate and return its exit code. Returns immediately if the process is not running.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the process to finish. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The exit code of the process, or None if it is already terminated.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If a timeout was specified and the process is still running by the time the timeout expires.</p>"},{"location":"reference/better_launch/elements/ros2_launch_wrapper/#better_launch.elements.ros2_launch_wrapper.Ros2LaunchWrapper.queue_ros2_actions","title":"queue_ros2_actions","text":"<pre><code>queue_ros2_actions(*actions) -&gt; None\n</code></pre> <p>Add ROS2 actions that will be loaded asynchronously by the launch service once it is running. Actions are bundled as a <code>LaunchDescription</code> before sending them off.</p>"},{"location":"reference/better_launch/gazebo/","title":"gazebo","text":""},{"location":"reference/better_launch/gazebo/#better_launch.gazebo","title":"better_launch.gazebo","text":"<p>Additional functions for working with Gazebo, taking inspiration from simple_launch.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge","title":"GazeboBridge","text":""},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge.gazebo_message_types","title":"gazebo_message_types  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gazebo_message_types = {'actuator_msgs/msg/Actuators': 'gz.msgs.Actuators', 'builtin_interfaces/msg/Time': 'gz.msgs.Time', 'geometry_msgs/msg/Point': 'gz.msgs.Vector3d', 'geometry_msgs/msg/Pose': 'gz.msgs.Pose', 'geometry_msgs/msg/PoseArray': 'gz.msgs.Pose_V', 'geometry_msgs/msg/PoseStamped': 'gz.msgs.Pose', 'geometry_msgs/msg/PoseWithCovariance': 'gz.msgs.PoseWithCovariance', 'geometry_msgs/msg/PoseWithCovarianceStamped': 'gz.msgs.PoseWithCovariance', 'geometry_msgs/msg/Quaternion': 'gz.msgs.Quaternion', 'geometry_msgs/msg/Transform': 'gz.msgs.Pose', 'geometry_msgs/msg/TransformStamped': 'gz.msgs.Pose', 'geometry_msgs/msg/Twist': 'gz.msgs.Twist', 'geometry_msgs/msg/TwistStamped': 'gz.msgs.Twist', 'geometry_msgs/msg/TwistWithCovariance': 'gz.msgs.TwistWithCovariance', 'geometry_msgs/msg/TwistWithCovarianceStamped': 'gz.msgs.TwistWithCovariance', 'geometry_msgs/msg/Vector3': 'gz.msgs.Vector3d', 'geometry_msgs/msg/Wrench': 'gz.msgs.Wrench', 'geometry_msgs/msg/WrenchStamped': 'gz.msgs.Wrench', 'gps_msgs/msg/GPSFix': 'gz.msgs.NavSat', 'nav_msgs/msg/Odometry': 'gz.msgs.OdometryWithCovariance', 'rcl_interfaces/msg/ParameterValue': 'gz.msgs.Any', 'ros_gz_interfaces/msg/Altimeter': 'gz.msgs.Altimeter', 'ros_gz_interfaces/msg/Contact': 'gz.msgs.Contact', 'ros_gz_interfaces/msg/Contacts': 'gz.msgs.Contacts', 'ros_gz_interfaces/msg/Dataframe': 'gz.msgs.Dataframe', 'ros_gz_interfaces/msg/Entity': 'gz.msgs.Entity', 'ros_gz_interfaces/msg/Float32Array': 'gz.msgs.Float_V', 'ros_gz_interfaces/msg/GuiCamera': 'gz.msgs.GUICamera', 'ros_gz_interfaces/msg/JointWrench': 'gz.msgs.JointWrench', 'ros_gz_interfaces/msg/Light': 'gz.msgs.Light', 'ros_gz_interfaces/msg/ParamVec': 'gz.msgs.Param', 'ros_gz_interfaces/msg/SensorNoise': 'gz.msgs.SensorNoise', 'ros_gz_interfaces/msg/StringVec': 'gz.msgs.StringMsg_V', 'ros_gz_interfaces/msg/TrackVisual': 'gz.msgs.TrackVisual', 'ros_gz_interfaces/msg/VideoRecord': 'gz.msgs.VideoRecord', 'rosgraph_msgs/msg/Clock': 'gz.msgs.Clock', 'sensor_msgs/msg/BatteryState': 'gz.msgs.BatteryState', 'sensor_msgs/msg/CameraInfo': 'gz.msgs.CameraInfo', 'sensor_msgs/msg/FluidPressure': 'gz.msgs.FluidPressure', 'sensor_msgs/msg/Image': 'gz.msgs.Image', 'sensor_msgs/msg/Imu': 'gz.msgs.IMU', 'sensor_msgs/msg/JointState': 'gz.msgs.Model', 'sensor_msgs/msg/Joy': 'gz.msgs.Joy', 'sensor_msgs/msg/LaserScan': 'gz.msgs.LaserScan', 'sensor_msgs/msg/MagneticField': 'gz.msgs.Magnetometer', 'sensor_msgs/msg/NavSatFix': 'gz.msgs.NavSat', 'sensor_msgs/msg/PointCloud2': 'gz.msgs.PointCloudPacked', 'std_msgs/msg/Bool': 'gz.msgs.Boolean', 'std_msgs/msg/ColorRGBA': 'gz.msgs.Color', 'std_msgs/msg/Empty': 'gz.msgs.Empty', 'std_msgs/msg/Float32': 'gz.msgs.Float', 'std_msgs/msg/Float64': 'gz.msgs.Double', 'std_msgs/msg/Header': 'gz.msgs.Header', 'std_msgs/msg/Int32': 'gz.msgs.Int32', 'std_msgs/msg/String': 'gz.msgs.StringMsg', 'std_msgs/msg/UInt32': 'gz.msgs.UInt32', 'tf2_msgs/msg/TFMessage': 'gz.msgs.Pose_V', 'trajectory_msgs/msg/JointTrajectory': 'gz.msgs.JointTrajectory', 'vision_msgs/msg/Detection2D': 'gz.msgs.AnnotatedAxisAligned2DBox', 'vision_msgs/msg/Detection2DArray': 'gz.msgs.AnnotatedAxisAligned2DBox_V'}\n</code></pre> <p>Map from ROS2 message types to Gazebo message types.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge.__init__","title":"__init__","text":"<pre><code>__init__(topic: str, ros2_type: str = None, direction: Literal['ros2gz', 'gz2ros', 'bidirectional', '[', ']', '@'] = 'bidirectional', gazebo_type: str = None, *, remaps: dict[str, str] = None)\n</code></pre> <p>Create a definition for a gazebo bridge. Convert to a string in order to get the canonical Gazebo bridge representation. To start a topic bridge see spawn_topic_bridge.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The ROS2 topic to bridge into Gazebo. Any remaps have to happen on the ROS2 side.</p> required <code>ros2_type</code> <code>str</code> <p>The message type of the ROS2 topic. If not provided the type will be looked up in the currently published topics.</p> <code>None</code> <code>direction</code> <code>str</code> <p>The direction in which messages will be passed,</p> <code>'bidirectional'</code> <code>gazebo_type</code> <code>str</code> <p>The message type of the Gazebo topic. If not provided it will be looked up from the common GazeboBridge.gazebo_message_types.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Additional topic remaps for the bridge node.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If an invalid direction is provided, or if the ROS2 message type was not specified and could not be looked up.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge.clock_bridge","title":"clock_bridge  <code>classmethod</code>","text":"<pre><code>clock_bridge() -&gt; GazeboBridge\n</code></pre> <p>Creates a GazeboBridge instance for bridging the /clock topic from Gazebo to ROS.</p> <p>Returns:</p> Type Description <code>GazeboBridge</code> <p>An instance of the GazeboBridge for the clock topic.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge.from_string","title":"from_string  <code>classmethod</code>","text":"<pre><code>from_string(bridge: str, remaps: dict[str, str] = None) -&gt; GazeboBridge\n</code></pre> <p>Extract the topic, ROS2 message type, direction, and gazebo message type from a bridge definition string.</p> <p>Parameters:</p> Name Type Description Default <code>bridge</code> <code>str</code> <p>A bridge definition following the pattern <code>&lt;topic&gt;@&lt;ros2_type&gt;&lt;direction&gt;&lt;gazebo_type&gt;</code>.</p> required <code>remaps</code> <code>str</code> <p>Topic remaps the bridge should use once it is started.</p> <code>None</code> <p>Returns:</p> Type Description <code>GazeboBridge</code> <p>An instance with the aforementionend parameters.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the bridge string doesn't match the expected pattern.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.GazeboBridge.joint_state_bridge","title":"joint_state_bridge  <code>classmethod</code>","text":"<pre><code>joint_state_bridge(model: str) -&gt; GazeboBridge\n</code></pre> <p>Creates a GazeboBridge instance for the bridging the joint states of a given model to ROS.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>str</code> <p>The model name to associate with the joint state topic.</p> required <p>Returns:</p> Type Description <code>GazeboBridge</code> <p>An instance of the GazeboBridge for the joint state topic.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.gazebo_launch","title":"gazebo_launch","text":"<pre><code>gazebo_launch(package: str, world_file: str, gz_args: list[str] = None, world_save_file: str = None, save_after: float = 10.0) -&gt; None\n</code></pre> <p>Starts a Gazebo simulation with the specified world. If <code>world_save_file</code> is specified, the world will be saved as an SDF to that file after <code>save_after</code> seconds, including base world and spawned entities.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>(str,)</code> <p>A package to locate the world_file in. May be <code>None</code> (see BetterLaunch.find).</p> required <code>world_file</code> <code>str</code> <p>Path to the primary world file for the simulation.</p> required <code>gz_args</code> <code>_type_</code> <p>Optional arguments to pass to the Gazebo simulator.</p> <code>None</code> <code>world_save_file</code> <code>_type_</code> <p>Path where the resulting SDF should be saved.</p> <code>None</code> <code>save_after</code> <code>float</code> <p>Time in seconds after which the world SDF should be saved.</p> <code>10.0</code>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_active_world_name","title":"get_active_world_name","text":"<pre><code>get_active_world_name(force_query: bool = False) -&gt; str\n</code></pre> <p>Return the name of the currently loaded world. If it was not loaded from better_launch, Gazebo will be asked directly.</p> <p>Parameters:</p> Name Type Description Default <code>force_query</code> <code>bool</code> <p>If True, don't use the cached value.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>The name of a Gazebo world.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If querying Gazebo for the world name failed.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_gazebo_axes_args","title":"get_gazebo_axes_args","text":"<pre><code>get_gazebo_axes_args(x: float = 0.0, y: float = 0.0, z: float = 0.0, yaw: float = 0.0, pitch: float = 0.0, roll: float = 0.0) -&gt; dict[str, float]\n</code></pre> <p>Constructs a list of command-line arguments that can be used e.g. when spawning a new model.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>translation x component</p> <code>0.0</code> <code>y</code> <code>float</code> <p>translation y component</p> <code>0.0</code> <code>z</code> <code>float</code> <p>translation z component</p> <code>0.0</code> <code>yaw</code> <code>float</code> <p>rotation yaw component</p> <code>0.0</code> <code>pitch</code> <code>float</code> <p>rotation pitch component</p> <code>0.0</code> <code>roll</code> <code>float</code> <p>rotation roll component</p> <code>0.0</code> <p>Returns:</p> Type Description <code>dict[str, float]</code> <p>A dictionary containing the axes names and values. The axes names correspond to what Gazebo expects on the command line and so can be passed to e.g. spawn_model.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_gazebo_exec","title":"get_gazebo_exec","text":"<pre><code>get_gazebo_exec() -&gt; str\n</code></pre> <p>Locates the gazebo executable.</p> <p>Returns:</p> Type Description <code>str</code> <p>Full path to the gazebo executable.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the executable cannot be located.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_gazebo_version","title":"get_gazebo_version","text":"<pre><code>get_gazebo_version() -&gt; str\n</code></pre> <p>For a short time, Gazebo rebranded to \"Ignition\" before returning to Gazebo again. This function returns the associated prefix.</p> <p>Returns:</p> Type Description <code>str</code> <p>The prefix indicating the type of Gazebo: \"ign\" for Ignition Gazebo or \"gz\" for Gazebo Classic.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_model_prefix","title":"get_model_prefix","text":"<pre><code>get_model_prefix(model: str) -&gt; str\n</code></pre> <p>Construct the Gazebo prefix string for the given model name.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>str</code> <p>The name of the model.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A string representing the model's prefix in the Gazebo world.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.get_model_topic","title":"get_model_topic","text":"<pre><code>get_model_topic(model: str, topic: str) -&gt; str\n</code></pre> <p>Constructs a string representing the model topic in Gazebo.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>str</code> <p>The model name.</p> required <code>topic</code> <code>str</code> <p>The topic name related to the model.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The path of the given model's specified topic.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.save_world","title":"save_world","text":"<pre><code>save_world(filepath: str, after: float = 5.0) -&gt; None\n</code></pre> <p>Saves the current gazebo world to a file. Resolves any spawned URDF through their description parameter and converts to SDF.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to the file to save the gazebo world to.</p> required <code>after</code> <code>float</code> <p>How long to wait before saving.</p> <code>5.0</code>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.spawn_image_bridge","title":"spawn_image_bridge","text":"<pre><code>spawn_image_bridge(*bridges: Union[str, GazeboBridge], node_name: str = None, remaps: dict[str, str] = None, cmd_args: list[str] = None, qos: str = None, **kwargs) -&gt; Node\n</code></pre> <p>Spawn a bridge to efficiently relay images from Gazebo to ROS2 (one direction only).</p> <p>Parameters:</p> Name Type Description Default <code>bridges</code> <code>list[str | GazeboBridge]</code> <p>The image topics to bridge. These can be specified as either Gazebo bridge definitions, GazeboBridge instances. Also accepts regular topics, in which case the type is assumed to be <code>sensor_msgs/Image</code>.</p> <code>()</code> <code>node_name</code> <code>str</code> <p>The name of the node running the bridge.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>How topics should be remapped in ROS2.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional commandline arguments to the bridge executable.</p> <code>None</code> <code>qos</code> <code>str</code> <p>The <code>ROS2 quality of service &lt;https://docs.ros.org/en/rolling/Concepts/Intermediate/About-Quality-of-Service-Settings.html&gt;</code>_ definition to use. Note that this is not supported in all versions of ros-gz-image and may cause the bridge to terminate.</p> <code>None</code> <code>kwargs</code> <code>dict[str, Any]</code> <p>Additional node arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node running the bridge process.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a topic is passed which is not an image topic.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.spawn_model","title":"spawn_model","text":"<pre><code>spawn_model(model_name: str, model: str, model_source: Literal['topic', 'file', 'string', 'param', 'auto'] = 'auto', spawn_args: dict[str, Any] = None, *, pass_by_topic: bool = False, topic_base_name: str = '/gazebo/models/', xacro_args: list[str] = None) -&gt; Node\n</code></pre> <p>Spawns a model into Gazebo under the given name from the specified topic or file.</p> <p>The <code>spawn_args</code> dictionary can include additional options, such as the initial pose of the model.</p> <p>Note that when spawning robot models this way, they typically also need a convenience.joint_state_publisher.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>The name of the model to spawn in the Gazebo environment.</p> required <code>model</code> <code>str</code> <p>The model to spawn. The contents of this string depend on the model_source, but should ultimately lead to a full XML description.</p> required <code>model_source</code> <code>str</code> <p>Where to read the model from. Auto will try to guess the source based on the content of <code>model</code>: does it look like XML, is it a file, is it an existing topic? Otherwise assume it's a ROS2 parameter.</p> <code>'auto'</code> <code>spawn_args</code> <code>dict[str, Any]</code> <p>Additional arguments for spawning the model, such as pose and other options. See get_gazebo_axes_args for defining the model's orientation.</p> <code>None</code> <code>pass_by_topic</code> <code>bool</code> <p>If True and the model is a file or string, pass it by topic instead. On ROS versions before lyrical this feature is disabled.</p> <code>False</code> <code>topic_base_name</code> <code>str</code> <p>Use this as the base topic string when <code>pass_by_topic</code> is True. The <code>model_name</code> will be appended to form the full topic.</p> <code>'/gazebo/models/'</code> <code>xacro_args</code> <code>list[str]</code> <p>Arguments to pass to the xacro parser if a xacro file is passed as the model.</p> <code>None</code> <p>Returns:</p> Type Description <code>Node</code> <p>The spawned node instance.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.spawn_topic_bridge","title":"spawn_topic_bridge","text":"<pre><code>spawn_topic_bridge(*bridges: Union[str, GazeboBridge], node_name: str = 'gz_bridge', remaps: dict[str, str] = None, cmd_args: list[str] = None, **kwargs) -&gt; Node\n</code></pre> <p>Start a Gazebo topic bridge to relay messages between ROS2 and Gazebo.</p> <p>Note that there is a separate function for bridging image topics more efficiently. See spawn_image_bridge.</p> <p>Parameters:</p> Name Type Description Default <code>bridges</code> <code>list[str | GazeboBridge]</code> <p>Definitions of topic bridges. This can be either a typical string (<code>&lt;topic&gt;@&lt;ros2_type&gt;&lt;direction&gt;&lt;gazebo_type&gt;</code>) or a GazeboBridge instance. Note that in order to bridge services you will have to specify them as strings for now.</p> <code>()</code> <code>node_name</code> <code>str</code> <p>The name of the bridge node.</p> <code>'gz_bridge'</code> <code>remaps</code> <code>dict[str, str]</code> <p>Any topic remaps you wish to apply to the bridge.</p> <code>None</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional command line arguments to the gazebo bridge executable.</p> <code>None</code> <code>kwargs</code> <code>dict[str, Any]</code> <p>Additional node arguments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node running the bridge process.</p>"},{"location":"reference/better_launch/gazebo/#better_launch.gazebo.spawn_world_transform","title":"spawn_world_transform","text":"<pre><code>spawn_world_transform(gazebo_world_frame: str = None) -&gt; Node\n</code></pre> <p>Runs a static_transform_publisher to connect the ROS <code>world</code> frame and a Gazebo world frame, if the Gazebo world frame is specified and different from 'world'.</p> <p>Parameters:</p> Name Type Description Default <code>gazebo_world_frame</code> <code>str</code> <p>The name of the Gazebo world frame. Uses get_active_world_name if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Node</code> <p>The spawned node instance.</p>"},{"location":"reference/better_launch/launcher/","title":"launcher","text":""},{"location":"reference/better_launch/launcher/#better_launch.launcher","title":"better_launch.launcher","text":""},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch","title":"BetterLaunch","text":"<p>This should be all you need to create beautiful, simple and convenient launch files!</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.group_root","title":"group_root  <code>property</code>","text":"<pre><code>group_root: Group\n</code></pre> <p>The root group (\"/\").</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.group_tip","title":"group_tip  <code>property</code>","text":"<pre><code>group_tip: Group\n</code></pre> <p>The most recent group.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.is_shutdown","title":"is_shutdown  <code>property</code>","text":"<pre><code>is_shutdown: bool\n</code></pre> <p>Whether better_launch has shutdown.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.launch_args","title":"launch_args  <code>property</code>","text":"<pre><code>launch_args: dict[str, Any]\n</code></pre> <p>All key-value pairs that have been passed to the launch function.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.launchfile","title":"launchfile  <code>property</code>","text":"<pre><code>launchfile: str\n</code></pre> <p>The path of the (main) better_launch launchfile being executed.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.ros_adapter","title":"ros_adapter  <code>property</code>","text":"<pre><code>ros_adapter: ROSAdapter\n</code></pre> <p>Contains and runs a shared ROS2 node to interact with topics, services, etc. The adapter is instantiated lazily as it brings a major performance hit (about 6 MiB memory and a high CPU spike).</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.shared_node","title":"shared_node  <code>property</code>","text":"<pre><code>shared_node: Node\n</code></pre> <p>A ROS2 node instance that can be used for creating publishers, services, etc.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.__init__","title":"__init__","text":"<pre><code>__init__(name: str = None, launch_args: dict[str, Any] = None, root_namespace: str = '/', *, short_unique_names: bool = False)\n</code></pre> <p>Note that BetterLaunch is a singleton: only the first invocation to <code>__init__</code> will succeed. All subsequent calls will return the previous instance. If you need access to the BetterLaunch instance outside your launch function, consider using one of the following classmethods instead:</p> <ul> <li>BetterLaunch.instance</li> <li>BetterLaunch.wait_for_instance</li> </ul> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of this instance, will default to the launchfile's filename.</p> <code>None</code> <code>launch_args</code> <code>dict</code> <p>Override the launch arguments BetterLaunch has access to. By default this will be the launch function's arguments. These will mainly be used for passing to included launch files.</p> <code>None</code> <code>root_namespace</code> <code>str</code> <p>The namespace of the root group.</p> <code>'/'</code> <code>short_unique_names</code> <code>bool</code> <p>If True, use short random hex strings for unique names instead of random words.</p> <code>False</code>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.action_client","title":"action_client","text":"<pre><code>action_client(topic: str, action_type: str | type, timeout: float = 5.0, qos_profile: QoSProfile = None) -&gt; RosActionClient\n</code></pre> <p>Create a ROS2 action client to execute long-running actions.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic namespace on which the action interface is provided.</p> required <code>action_type</code> <code>str | type</code> <p>The type of actions the action server handles. Strings must follow the pattern <code>&lt;package&gt;/action/&lt;message&gt;</code>.</p> required <code>timeout</code> <code>float</code> <p>Time to wait for the action server to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the action server handles connections and retains data.</p> <code>None</code> <p>Returns:</p> Type Description <code>ActionClient</code> <p>The action client object. Although not required for Jazzy and below, it is recommended to keep a reference.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the action server did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.action_server","title":"action_server","text":"<pre><code>action_server(topic: str, action_type: str | type, callback: Callable[[Any], Any], qos_profile: QoSProfile = None) -&gt; RosActionServer\n</code></pre> <p>Create a ROS2 action server using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic namespace to provide the action interface on.</p> required <code>action_type</code> <code>str | type</code> <p>The type of the actions to be handled. Strings must follow the pattern <code>&lt;package&gt;/action/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>A function that will handle incoming action requests. The type of the requests will be of type <code>rclpy.action.server.ServerGoalHandle</code> and contain an <code>action_type.Goal</code>.</p> required <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the action server handles connections and retains data.</p> <code>None</code> <p>Returns:</p> Type Description <code>ActionServer</code> <p>The action server object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.add_shutdown_callback","title":"add_shutdown_callback","text":"<pre><code>add_shutdown_callback(callback: Callable[[], Any]) -&gt; None\n</code></pre> <p>Adds a callback which will be called when better_launch shuts down.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>The callback to call on shutdown.</p> required"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.all_ros2_node_names","title":"all_ros2_node_names","text":"<pre><code>all_ros2_node_names() -&gt; list[str]\n</code></pre> <p>Returns a list of all currently registered node's full names (namespace + name).</p> <p>This list is guaranteed to be complete as far as ROS2 is concerned. If you require a node object you can actually interact with consider using query_node or get_nodes instead.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of all running nodes' full names.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.call_service","title":"call_service","text":"<pre><code>call_service(topic: str, service_type: str | type, request_args: dict[str, Any] | Any = None, *, timeout: float = 5.0, qos_profile: QoSProfile = None, call_async: bool = False) -&gt; Any\n</code></pre> <p>Makes a single service request and returns the result. The client is destroyed once the request has been handled. If you plan to make additional requests, use service_client instead.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The service topic to post requests on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>request_args</code> <code>dict[str, Any] | Any</code> <p>The keyword arguments from which the request message will be constructed. May also be an instance of <code>service_type.Request</code>.</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Time to wait for the service to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <code>call_async</code> <code>bool</code> <p>If True, make the service call async and return a <code>rclpy.task.Future</code> instead.</p> <code>False</code> <p>Returns:</p> Type Description <code>Any</code> <p>A <code>rclpy.task.Future</code> if <code>call_async</code> is True, otherwise the result of the service call of type <code>service_type.Request</code>.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.component","title":"component","text":"<pre><code>component(package: str, plugin: str, name: str = None, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, anonymous: bool = False, hidden: bool = False, use_intra_process_comms: bool = True, ros_waittime: float = 3.0, lifecycle_waittime: float = 0.01, lifecycle_target: LifecycleStage | str = LifecycleStage.ACTIVE, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, **extra_composer_args: dict[str, Any]) -&gt; Component\n</code></pre> <p>Create a component and load it into an existing compose context.</p> <p>If you instead want to load components without a <code>compose</code> context, you should instantiate Component objects directly, then load them via Component.start or Composer.load_component. See the examples for more details.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the component implementation.</p> required <code>plugin</code> <code>str</code> <p>The name the component is registered as, typically of the form <code>&lt;package&gt;::&lt;Name&gt;</code>.</p> required <code>name</code> <code>str</code> <p>The name the instantiated component should be known as. If <code>None</code>, a name will be derived from <code>package</code> and <code>plugin</code>, and <code>anonymous</code> will be set to True. Pass an empty string instead if you really want to use the node's default name - just know that you'll make a cute kitten really sad.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>anonymous</code> <code>bool</code> <p>If True, the composer name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the composer name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any ROS parameters you want to pass to the component. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>use_intra_process_comms</code> <code>bool</code> <p>If True, ask the composer node to enable intra-process communication, i.e. share memory between components when passing messages instead of serializing and deserializing.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the component to register with ROS. This should cover the time between the process starting and the component initializing itself. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>lifecycle_waittime</code> <code>float</code> <p>How long to wait for the component's lifecycle management to come up. This should cover the time between the component initializing itself (see <code>ros_waittime</code>) and creating its additional topics and services. While neglible on modern computers, slower devices and embedded systems may experience a noticable delay here. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>0.01</code> <code>lifecycle_target</code> <code>LifecycleStage | str</code> <p>The lifecycle stage to bring the component into after starting. Has no effect if <code>autostart_process</code> is False or if the component does not appear to be a lifecycle component after waiting <code>ros_waittime + lifecycle_waittime</code>.</p> <code>ACTIVE</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Returns:</p> Type Description <code>Component</code> <p>The component that has been loaded into the current compose context.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If this is called outside a compose context.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.compose","title":"compose","text":"<pre><code>compose(name: str = None, language: str = 'cpp', variant: Literal['normal', 'multithreading', 'isolated'] = 'normal', *, reuse_existing: bool = True, component_remaps: dict[str, str] = None, anonymous: bool = False, hidden: bool = False, use_sim_time: bool = None, autostart_process: bool = True, ros_waittime: float = 3.0, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN) -&gt; Generator[Composer, None, None]\n</code></pre> <p>Creates a composer node which can be used to load Components. Components can be instantiated directly, or preferably via component. Only components can reside within a composer.</p> <p>Existing composers can be reused even if they have been created outside of better_launch. See Composer for further details.</p> <p>This method should be used as a context, e.g.</p> <p>.. code:: python</p> <pre><code>bl = BetterLaunch()\nwith bl.compose(\"my-composer\"):\n    bl.component(\"my_package\", \"mystuff:TheComponentOfDreams\", \"normal-component\")\n</code></pre> <p>Note that new nodes created by a component are using the composer's remaps. This for example applies to transform listeners (but not transform publishers). See the <code>related issue &lt;https://github.com/ros2/rclcpp/issues/2404&gt;</code>_ in rclcpp.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name you want the composer to be known as. <code>anonymous</code> will be set to True if no name is provided. Pass an empty string if you really want to use the composer node's default name.</p> <code>None</code> <code>language</code> <code>str</code> <p>The implementation of the standard composer you want to use. Ignored if <code>reuse_existing</code> is True and a matching node is found.</p> <code>'cpp'</code> <code>variant</code> <code>Literal['normal', 'multithreading', 'isolated']</code> <p>ROS2 provides special composers for components that need multithreading or should be isolated from the rest. Ignored if <code>reuse_existing</code> is True and a matching node is found.</p> <code>'normal'</code> <code>reuse_existing</code> <code>bool</code> <p>If True and a node matching the current namespace and provided name is found, it will be used instead of creating a new node. This will even work for composers not created through better_launch, although in that case it won't be possible to stop them.</p> <code>True</code> <code>component_remaps</code> <code>dict[str, str]</code> <p>Any remaps you want to apply to all components loaded into this composer.</p> <code>None</code> <code>anonymous</code> <code>bool</code> <p>If True, the composer name will be appended with a unique suffix to avoid name conflicts. <code>reuse_existing</code> will be set to False in this case.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the composer name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>use_sim_time</code> <code>bool</code> <p>If set decides whether the node should use simulated time. Otherwise uses the setting from the current BetterLaunch.group_tip. Only effective when a new composer is created. By ROS2 design, all componentens added to this composer will inherit this setting.</p> <code>None</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the composer process before returning from this function. Note that setting this to False for a composer will make it unusable as a context object, since you won't be able to load any components.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the composer to register with ROS. This should cover the time between the process starting and the composer initializing itself. Set negative to wait indefinitely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <p>Yields:</p> Type Description <code>Generator[Composer, None, None]</code> <p>Sets the composition flag and yields the composer.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If you try to create a composer within a compose context.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.does_node_exist","title":"does_node_exist","text":"<pre><code>does_node_exist(name: str, namespace: str = None) -&gt; bool\n</code></pre> <p>Checks whether the specified node is known to ROS2.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the node.</p> required <code>namespace</code> <code>str</code> <p>If given, a pattern to match against the node's namespace. Supports path-like patterns, e.g. <code>*</code> denotes a wildcard (see <code>fnmatch</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if ROS2 knows the node, False otherwise. Note that there are rare circumstances where a crashed (terminated) node is still reported by ROS. To get an actual handle use query_node instead.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.exec","title":"exec  <code>classmethod</code>","text":"<pre><code>exec(cmd: str | list[str]) -&gt; str\n</code></pre> <p>Run the specified command, await its termination and return its output. Bare commands are resolved using <code>shutil.which</code>.</p> <p>For long-lived commands that you don't want to wait for, consider using BetterLaunch.process instead.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str | list[str]</code> <p>The command to run. If a string is passed it will be split on spaces to separate the command from its arguments. Pass a list instead to have more control over which arguments to treat as a single argument.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The output of the command without the trailing newline.</p> <p>Raises:</p> Type Description <code>CalledProcessError</code> <p>If the command had a non-zero exit code. See the raised error's <code>returncode</code> and <code>output</code> attributes for details.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.find","title":"find","text":"<pre><code>find(package: str = None, filename: str = None, subdir: str = '**') -&gt; str\n</code></pre> <p>Resolve a path to a file or package.</p> <p>If the <code>filename</code> is absolute, all other arguments will be ignored and the filename will be returned.</p> <p>When <code>package</code> names a package discoverable by ament, the corresponding ROS2 package path will be used as the base path. Instead of a package name you may also provide an absolute path, in which case it will become the base path. Any path elements after the package name will be appended to the base path. For example, to find files inside the package's shared files, specify the package as <code>&lt;package&gt;/share</code>.</p> <p>Otherwise, if <code>package</code> was not specified we attempt to locate the current launch file's package by searching its directory and parent directories for a <code>package.xml</code>. If the package cannot be determined an exception is raised.</p> <p><code>subdir</code> accepts glob patterns and can be used to resolve ambiguities, e.g. <code>lib/**</code> (anywhere inside the package's lib folder) or <code>share/</code> (directly inside the share folder). If not specified, \"**\" will be used (any file or directory inside the base path).</p> <p>If only <code>subdir</code> is provided but not <code>filename</code>, the first matching candidate is returned. Otherwise the discovered candidates will be searched for the given filename.</p> <p>If neither <code>subdir</code> nor <code>filename</code> is provided the base path will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>Name of a ROS2 package to resolve.</p> <code>None</code> <code>filename</code> <code>str</code> <p>Name of a file to look for.</p> <code>None</code> <code>subdir</code> <code>str</code> <p>A glob pattern to locate subdirectories and files. See the <code>pathlib pattern language &lt;https://docs.python.org/3/library/pathlib.html#pathlib-pattern-language&gt;</code>_ for details.</p> <code>'**'</code> <p>Returns:</p> Type Description <code>str</code> <p>A resolved path.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the base path could not be determined, or if a <code>filename</code> is provided but could not be found within base path.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.find_group_for_namespace","title":"find_group_for_namespace","text":"<pre><code>find_group_for_namespace(namespace: str, create: bool = False) -&gt; Group\n</code></pre> <p>Find the group representing the passed namespace.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The namespace in question.</p> required <code>create</code> <code>bool</code> <p>If True, create missing groups along the way.</p> <code>False</code> <p>Returns:</p> Type Description <code>Group</code> <p>The group representing the final segment of the namespace, or None if no such group exists and create == False.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.get_foreign_nodes","title":"get_foreign_nodes","text":"<pre><code>get_foreign_nodes() -&gt; list[ForeignNode]\n</code></pre> <p>Lists all running nodes that have a process but have not been started by this better_launch process.</p> <p>Note however that if a process starts multiple nodes, only the first node can be discovered. This is because ROS2 does not provide an API for getting the process parameters from a node.</p> <p>Returns:</p> Type Description <code>list[ForeignNode]</code> <p>All nodes with a process that have not been started by this better_launch process.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.get_groups","title":"get_groups","text":"<pre><code>get_groups() -&gt; list[Group]\n</code></pre> <p>Returns a list of all groups in the order they were created.</p> <p>Returns:</p> Type Description <code>list[Group]</code> <p>All groups added so far.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.get_nodes","title":"get_nodes","text":"<pre><code>get_nodes(*, include_components: bool = False, include_launch_service: bool = True, include_foreign: bool = False) -&gt; list[AbstractNode]\n</code></pre> <p>Returns a list of all nodes in the order they were added. Components will be added right after their composers.</p> <p>Note that this will only return nodes that can be managed by better_launch. If a node process creates multiple nodes only the first node can be discovered, as ROS2 does not provide an API linking a node to its process (or even just its package).</p> <p>Parameters:</p> Name Type Description Default <code>include_components</code> <code>bool</code> <p>Whether to include Component instances. This will not include components that have been loaded from outside (e.g. <code>ros2 component load</code>).</p> <code>False</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service wrapper if it was created. Will be included after the regular nodes and before the foreign nodes.</p> <code>True</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes that have not been started by this launcher instance. Will be appended at the end of the returned nodes. However, take heed of the above warning regarding node discovery.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[AbstractNode]</code> <p>A list of all nodes, sorted by when they were added.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.get_ros_message_type","title":"get_ros_message_type","text":"<pre><code>get_ros_message_type(message_string: str) -&gt; type\n</code></pre> <p>Loads a ROS2 message type from a string representation.</p> <p>Message representations must follow the pattern <code>&lt;package&gt;/&lt;type&gt;/&lt;message&gt;</code>, where *  is the ROS2 package that defines the message. *  is the type of message, typically one of <code>msg</code>, <code>srv</code> or <code>action</code>. *  is the name of the message itself with proper capitalization. <p>Parameters:</p> Name Type Description Default <code>message_string</code> <code>str</code> <p>A message representation of the form <code>&lt;package&gt;/&lt;type&gt;/&lt;message&gt;</code>.</p> required <p>Returns:</p> Type Description <code>type</code> <p>The message class.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If the message type could not be imported.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.get_unique_name","title":"get_unique_name","text":"<pre><code>get_unique_name(name: str = '', check_running_nodes: bool = True) -&gt; str\n</code></pre> <p>Returns a unique name. If a name is provided it will be prepended with an underscore.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string to use as the base.</p> <code>''</code> <code>check_running_nodes</code> <code>bool</code> <p>If true, check the currently running ROS2 nodes for name collisions.</p> <code>True</code> <p>Returns:</p> Type Description <code>str</code> <p>A unique name.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.group","title":"group","text":"<pre><code>group(namespace: str, use_sim_time: bool = None) -&gt; Generator[Group, None, None]\n</code></pre> <p>Groups are used to bundle nodes into namespaces. While they influence the nodes' topics and service name, they have no runtime functionality.</p> <p>Groups are intended to be used as context objects and can be nested. Note that starting a new root branch (i.e. a group starting with \"/\") is valid and will change subsequent groups for the duration of the context window.</p> <p>.. code:: python</p> <pre><code>bl = BetterLaunch()\nwith bl.group(\"outer\"):\n    # Unless included in another group, \"outer\" will be attached to \"/\"\n    with bl.group(\"inner/sanctum\"):\n        # Regular nesting, nodes will live within \"/outer/inner/sanctum\"\n        bl.node(...)\n    with bl.group(\"/evil/tower\"):\n        # New root branch, nodes will live within \"/evil/tower\"\n        bl.node(...)\n    # Root branch exited, nodes will live within \"/outer\" once again\n    bl.node(...)\n</code></pre> <p>.. seealso::</p> <pre><code>* [group_root][]\n* [group_tip][]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The group's namespace.</p> required <code>use_sim_time</code> <code>bool</code> <p>Decide whether nodes within this group should use simulated time. Leave as None to use the parent group's use_sim_time setting. The root group defaults to False unless the corresponding environment variable or CLI switch have been modified.</p> <code>None</code> <p>Yields:</p> Type Description <code>Generator[Group, None, None]</code> <p>Places the group on the group stack and yields it. Exiting the context will pop the group from the group stack.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the group is created within a compose context.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.hello","title":"hello","text":"<pre><code>hello() -&gt; None\n</code></pre> <p>Prints our welcome message and some useful information. Note that this will not appear in the logs!</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.include","title":"include","text":"<pre><code>include(package: str, launchfile: str, subdir: str = None, *, pass_launch_func_args: bool = True, **kwargs) -&gt; None\n</code></pre> <p>Include another launch file, resolving its path using find.</p> <p>The file is first read into memory and checked. If it seems to be a better_launch launch file, it is executed immediately (using [exec]). The BetterLaunch instance and global context will be shared. Any arguments to launch_this in the included launch file will be ignored.</p> <p>If the file does not appear to be a better_launch launch file, it is assumed to be a regular ROS2 launch file. In this case a <code>launch.actions.IncludeLaunchDescription</code> instance is created and passed to ros2_actions.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package containing the specified launch file. May be <code>None</code> (see find).</p> required <code>launchfile</code> <code>str</code> <p>The name of a launch file to execute.</p> required <code>subdir</code> <code>str</code> <p>A path fragment the launch file must be located in.</p> <code>None</code> <code>pass_launch_func_args</code> <code>bool</code> <p>If True, all <code>launch_args</code> will be passed to the included launch file. Additional launch arguments can also be provided via <code>kwargs</code>.</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the passed in <code>search_args</code> cannot be handled.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.is_included","title":"is_included  <code>classmethod</code>","text":"<pre><code>is_included() -&gt; bool\n</code></pre> <p>Check if this is run from an included launchfile.</p> <p>NOTE: this will only work when (indirectly) invoked from launch_this.</p> <p>More specifically, this checks if a BetterLaunch instance has been stored in the calling frame's globals, which happens on the first instantiation. This mechanism is an implementation detail and should not be relied on.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if a launch_this function has already run.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When launch_this is not part of the current stack frame.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.load_params","title":"load_params","text":"<pre><code>load_params(package: str = None, configfile: str = None, subdir: str = None, *, qualifier: str | Node = None, strip_qualifiers: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Load parameters from a yaml file located through find.</p> <p>If the config only contains a <code>ros__parameters</code> section the entire config is returned regardless of whether a <code>qualifier</code> was passed. Otherwise, the loaded config dict is searched for a matching section. If no matching section can be found the returned dict will be empty.</p> <p>Globbing is used for matching qualifiers to paths, so the following wildcards are supported: * <code>**</code>: matches any number of tokens, may be followed by additional tokens and a node name * <code>*</code>: skips a single namespace token, or ignores the node's name if at the end</p> <p>Note that better_launch does not require you to place <code>ros__parameters</code> in your configs. If it exists it will later be used to match parameters to namespaces and nodes. For example, a config like</p> <pre><code>my_node:\n    ros__parameters:\n        int_of_fury: 5\n</code></pre> <p>will be passed to a ROS2 node process as <code>-p my_node:int_of_fury:=5</code> and thus become specific to any node named <code>my_node</code>.</p> <p>.. seealso::</p> <pre><code>`ROS2 design doc on wildcards &lt;https://github.com/ros2/design/blob/gh-pages/articles/160_ros_command_line_arguments.md#multiple-parameter-assignments&gt;`_\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>A package to search for the config file. May be <code>None</code> (see find).</p> <code>None</code> <code>configfile</code> <code>str</code> <p>The name of the config file to locate.</p> <code>None</code> <code>subdir</code> <code>str</code> <p>A path fragment that the config file must be located in.</p> <code>None</code> <code>qualifier</code> <code>str</code> <p>Used to specifiy which section of the config to return. E.g. if the yaml contains <code>{A: {B: C, D: E}}</code>, then the qualifier \"A/B\" will return <code>{A: {B: C}}</code>. The qualifier supports globbing patterns like <code>*</code> and <code>**</code> and will ignore <code>ros__parameters</code> keys.</p> <code>None</code> <code>strip_qualifiers</code> <code>bool</code> <p>If True and a qualifier was passed, the qualifier will not be part of the returned dict. Set to True if you want some inner part of the params, e.g. only the params for a specific node. Set to False if you want a slice of the full params, e.g. all params for nodes in a specific namespace.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>The key-value pairs from the config.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the path cannot be resolved, of if <code>qualifier</code> is supplied and no matching section could be found.</p> <code>IOError</code> <p>If the config file could not be read.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.log","title":"log","text":"<pre><code>log(severity: str | int, message: str, *args: list[Any], **kwargs: dict[str, Any]) -&gt; None\n</code></pre> <p>Pass the specified message to the logger.</p> <p>This mostly exists to provide this functionality for TOML launchfiles.</p> <p>Parameters:</p> Name Type Description Default <code>severity</code> <code>str | int</code> <p>A logging severity or level. Standard severities are debug, info, warning, error, critical, and fatal. Integers can be used for more fine grained control and custom log levels, as per the python logging module.</p> required <code>message</code> <code>str</code> <p>The message to log.</p> required <code>args</code> <code>list[Any]</code> <p>A sequence of additional arguments to format the message.</p> <code>()</code> <code>kwargs</code> <code>dict[str, Any]</code> <p>A dict of additional arguments to format the message.</p> <code>{}</code>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.node","title":"node","text":"<pre><code>node(package: str, executable: str, name: str = None, *, remaps: dict[str, str] = None, params: str | dict[str, Any] = None, param_files: str | list[str] = None, use_sim_time: bool = None, drop_param_qualifiers: bool = False, cmd_args: list[str] = None, prefix_args: list[str] = None, env: dict[str, str] = None, isolate_env: bool = False, log_level: int = logging.INFO, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, anonymous: bool = False, hidden: bool = False, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, autostart_process: bool = True, ros_waittime: float = 3.0, lifecycle_waittime: float = 0.01, lifecycle_target: LifecycleStage | str = LifecycleStage.ACTIVE, raw: bool = False, remap_qualifier: str = None, qualify_all_remaps: bool = False) -&gt; Node\n</code></pre> <p>Create a new ROS2 node process. The bread and butter of every ROS setup!</p> <p>Please note that by default better_launch will generate an anonymous node name if no node name was specified. This helps to avoid multiple nodes with the same name, which in ROS2 is both possible and problematic. If you really don't want to specify a node name, pass an empty string instead.</p> <p>This method also handles lifecycle nodes (they REALLY should have a common interface). Note that especially for lifecycle nodes you probably want <code>autostart_process == True</code>, otherwise there lifecycle management will not exist. With <code>autostart_process == True</code>, a lifecycle node will automatically advance to <code>lifecycle_target</code> once it is up. Otherwise you can also call Node.start later.</p> <p>The <code>ROS2 documentation &lt;https://docs.ros.org/en/rolling/How-To-Guides/Node-arguments.html&gt;</code>_ can provide some additional information regarding <code>params</code>, <code>remaps</code>, and so on.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>The package providing the node.</p> required <code>executable</code> <code>str</code> <p>The executable that should be run.</p> required <code>name</code> <code>str</code> <p>The name you want the node to be known as. If <code>None</code>, a name will be derived from <code>package</code> and <code>executable</code> and <code>anonymous</code> will be set to True. Pass an empty string instead if you really want to use the node's default name - just know that you'll make a cute kitten really sad.</p> <code>None</code> <code>remaps</code> <code>dict[str, str]</code> <p>Tells the node to replace any topics it wants to interact with according to the provided dict.</p> <code>None</code> <code>params</code> <code>str | dict[str, Any]</code> <p>Any ROS parameters you want to pass to the node. These are the args you would typically have to declare in your launch file. A string will be interpreted as a path to a yaml file which will be lazy loaded using BetterLaunch.load_params.</p> <code>None</code> <code>param_files</code> <code>str | list[str]</code> <p>Paths to parameter files that will be passed to the node as is. If both param_files and params are present, param_files will be passed first (same order), followed by the params.</p> <code>None</code> <code>use_sim_time</code> <code>bool</code> <p>If set decides whether the node should use simulated time. Otherwise uses the setting from the current BetterLaunch.group_tip.</p> <code>None</code> <code>drop_param_qualifiers</code> <code>bool</code> <p>If True, any namespace/node qualifiers in the passed params are ignored.</p> <code>False</code> <code>cmd_args</code> <code>list[str]</code> <p>Additional command line arguments to pass to the node.</p> <code>None</code> <code>prefix_args</code> <code>list[str]</code> <p>Arguments to prepend to the resolved run command, e.g. for executing the node through gdb.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the node's process. The node process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the node process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used. Be aware that this can result in many common things to not work anymore since e.g. keys like PATH will be missing.</p> <code>False</code> <code>log_level</code> <code>int</code> <p>The minimum severity a logged message from this node must have in order to be published. This will be added to the cmd_args unless it is None.</p> <code>INFO</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this node's output should be directed. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>anonymous</code> <code>bool</code> <p>If True, the node name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>hidden</code> <code>bool</code> <p>If True, the node name will be prepended with a \"_\", hiding it from common listings.</p> <code>False</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the node's process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the node process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the node process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the node executable via the system shell. While this gives access to the shell's builtins, this has the downside of running the node inside a \"mystery program\" which is platform and user dependent. Generally not advised.</p> <code>False</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the node process before returning from this function.</p> <code>True</code> <code>ros_waittime</code> <code>float</code> <p>How long to wait for the node to register with ROS. This should cover the time between the process starting and the node initializing itself. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>3.0</code> <code>lifecycle_waittime</code> <code>float</code> <p>How long to wait for the node's lifecycle management to come up. This should cover the time between the node initializing itself (see <code>ros_waittime</code>) and creating its additional topics and services. While neglible on modern computers, slower devices and embedded systems may experience a noticable delay here. Set negative to wait indefinitely. Set to None to avoid the check entirely. Will do nothing if <code>autostart_process</code> is False.</p> <code>0.01</code> <code>lifecycle_target</code> <code>LifecycleStage | str</code> <p>The lifecycle stage to bring the node into after starting. Has no effect if <code>autostart_process</code> is False or if the node does not appear to be a lifecycle node after waiting <code>ros_waittime + lifecycle_waittime</code>.</p> <code>ACTIVE</code> <code>raw</code> <code>bool</code> <p>If True, don't treat the executable as a ROS2 node and avoid passing it any command line arguments except those specified.</p> <code>False</code> <code>remap_qualifier</code> <code>str</code> <p>Additional qualifier that will precede the node's <code>__ns</code> and <code>__name</code> remap rules. Should be the original name of the node (i.e. whatever its default name is) and can be qualified with a namespace. Useful to prevent multiple nodes with the same name when a process can have more than one node (e.g. <code>controller_manager</code>). See this ROS2 design doc for more information.</p> <code>None</code> <code>qualify_all_remaps</code> <code>bool</code> <p>If True, apply the <code>remap_qualifier</code> to all remaps that are not already qualified.</p> <code>False</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node object wrapping the node process.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If you try to add a node withing a compose context.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.process","title":"process","text":"<pre><code>process(cmd: str | list[str], name: str = None, *, env: dict[str, str] = None, isolate_env: bool = False, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, anonymous: bool = False, on_exit: Callable = None, max_respawns: int = 0, respawn_delay: float = 0.0, use_shell: bool = False, autostart_process: bool = True) -&gt; Node\n</code></pre> <p>Starts an arbitrary process and wraps it in a Node object.</p> <p>This method for starting long-running non-ROS processes, similar to ROS2's <code>ExecuteProcess</code>. Bare command names are resolved using <code>shutil.which</code>.</p> <p>If you instead want to wait for the process to return (and retrieve its output), consider using BetterLaunch.exec instead.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str | list[str]</code> <p>The command to execute. If a string is provided, it will be split using <code>shlex.split</code>.</p> required <code>name</code> <code>str</code> <p>The name of the process. If not provided, it will be derived from the command.</p> <code>None</code> <code>env</code> <code>dict[str, str]</code> <p>Additional environment variables to set for the process. The process will merge these with the environment variables of the better_launch host process unless <code>isolate_env</code> is True.</p> <code>None</code> <code>isolate_env</code> <code>bool</code> <p>If True, the process' env will not be inherited from the parent process and only those passed via <code>env</code> will be used.</p> <code>False</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>Determines if and where this process' output should be directed. Defaults to <code>LogSink.SCREEN</code>.</p> <code>SCREEN</code> <code>anonymous</code> <code>bool</code> <p>If True, the process name will be appended with a unique suffix to avoid name conflicts.</p> <code>False</code> <code>on_exit</code> <code>Callable</code> <p>A function to call when the process terminates (after any possible respawns).</p> <code>None</code> <code>max_respawns</code> <code>int</code> <p>How often to restart the process if it terminates.</p> <code>0</code> <code>respawn_delay</code> <code>float</code> <p>How long to wait before restarting the process after it terminates.</p> <code>0.0</code> <code>use_shell</code> <code>bool</code> <p>If True, invoke the executable via the system shell.</p> <code>False</code> <code>autostart_process</code> <code>bool</code> <p>If True, start the process before returning from this function.</p> <code>True</code> <p>Returns:</p> Type Description <code>Node</code> <p>The node object wrapping the process.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the command is empty.</p> <code>FileNotFoundError</code> <p>If the executable cannot be found.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.publish_message","title":"publish_message","text":"<pre><code>publish_message(topic: str, message_type: str | type, message_args: dict[str, Any], qos_profile: QoSProfile | int = 10, *, time_to_publish: float = 1.0) -&gt; None\n</code></pre> <p>Convenience method to publish a single message. The publisher will be destroyed once the message has been published. If you plan to publish additional messages, use publisher instead and use the instance.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to publish messages on.</p> required <code>message_type</code> <code>str | type</code> <p>The message type that will be published. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>message_args</code> <code>dict[str, Any]</code> <p>The keyword arguments from which the message will be constructed.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <code>time_to_publish</code> <code>float</code> <p>How long to give the publisher to submit the message. Expect the message to get lost if you set this to 0.</p> <code>1.0</code>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.publisher","title":"publisher","text":"<pre><code>publisher(topic: str, message_type: str | type, qos_profile: QoSProfile | int = 10) -&gt; RosPublisher\n</code></pre> <p>Create a ROS2 publisher using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to publish messages on.</p> required <code>message_type</code> <code>str | type</code> <p>The message type that will be published. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <p>Returns:</p> Type Description <code>Publisher</code> <p>The publisher object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.qos_profile","title":"qos_profile","text":"<pre><code>qos_profile(history: Literal['keep_last', 'keep_all', 'default'] | HistoryPolicy = 'keep_all', queue_size: int = 10, reliability: Literal['reliable', 'best_effort', 'default'] | ReliabilityPolicy = 'reliable', durability: Literal['volatile', 'transient_local', 'default'] | DurabilityPolicy = 'volatile', deadline: float = 0, lifespan: float = 0, liveliness: Literal['auto', 'manual', 'default'] | LivelinessPolicy = 'default', alive_timeout: float = 0) -&gt; QoSProfile\n</code></pre> <p>Allows callers to quickly create a QoS profile without a million imports. Any value set to <code>default</code> will use the underlying RMW's default.</p> <p>See Quality of Service settings for details.</p> <p>Parameters:</p> Name Type Description Default <code>history</code> <code>Literal[&amp;quot;keep_last&amp;quot;, &amp;quot;keep_all&amp;quot;, &amp;quot;default&amp;quot;] | HistoryPolicy</code> <p><code>keep_all</code>: store up to N samples according to <code>queue_size</code>; <code>keep_all</code>: store as many samples as the RMW allows.</p> <code>'keep_all'</code> <code>queue_size</code> <code>int</code> <p>Number of samples to keep for <code>history = keep_all</code>.</p> <code>10</code> <code>reliability</code> <code>Literal[&amp;quot;reliable&amp;quot;, &amp;quot;best_effort&amp;quot;, &amp;quot;default&amp;quot;] | ReliabilityPolicy</code> <p><code>reliable</code>: retry sending on errors; <code>best_effort</code>: never retry.</p> <code>'reliable'</code> <code>durability</code> <code>Literal[&amp;quot;volatile&amp;quot;, &amp;quot;transient_local&amp;quot;, &amp;quot;default&amp;quot;] | DurabilityPolicy</code> <p><code>volatile</code>: fire and forget; <code>transient_local</code>: publisher keeps data available for late-joining subscribers. To create a latched topic configure both publisher and subscriber with <code>transient_local</code>.</p> <code>'volatile'</code> <code>deadline</code> <code>float</code> <p>Expected maximum time between published messages.</p> <code>0</code> <code>lifespan</code> <code>float</code> <p>How much time is allowed to pass between sending and receiving the message before it will be marked as stale.</p> <code>0</code> <code>liveliness</code> <code>Literal[&amp;quot;auto&amp;quot;, &amp;quot;manual&amp;quot;, &amp;quot;default&amp;quot;] | LivelinessPolicy</code> <p><code>auto</code>: all publishers of a node are considered alive for the <code>alive_timeout</code> when any one of them fires; <code>manual</code>: publishers have to regularly tell the system that they are alive.</p> <code>'default'</code> <code>alive_timeout</code> <code>float</code> <p>If a publisher doesn't tell the system it's alive for this amount of time it will be considered dead.</p> <code>0</code> <p>Returns:</p> Type Description <code>QoSProfile</code> <p>description</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.query_node","title":"query_node","text":"<pre><code>query_node(pattern: str, *, include_components: bool = True, include_launch_service: bool = False, include_foreign: bool = False) -&gt; AbstractNode\n</code></pre> <p>Retrieve the first node matching the provided pattern.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Either the name of a node, or a qualified node name (i.e. namespace + name). If a namespace is included it must be absolute, but may include <code>*</code> or <code>**</code> wildcards to skip one or more groups (via <code>fnmatch</code>).</p> required <code>include_components</code> <code>bool</code> <p>Whether to include components in the results, if any.</p> <code>True</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service in the result (if it exists).</p> <code>False</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes not created by this launcher.</p> <code>False</code> <p>Returns:</p> Type Description <code>AbstractNode</code> <p>The first node matching the provided pattern, or None if none matched.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.query_nodes","title":"query_nodes","text":"<pre><code>query_nodes(pattern: str, *, include_components: bool = True, include_launch_service: bool = False, include_foreign: bool = False) -&gt; Generator[AbstractNode, None, None]\n</code></pre> <p>Yield all nodes matching the provided pattern.</p> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>Either the name of a node, or a qualified node name (i.e. namespace + name). If a namespace is included it must be absolute, but may include <code>*</code> or <code>**</code> wildcards to skip one or more groups (via <code>fnmatch</code>).</p> required <code>include_components</code> <code>bool</code> <p>Whether to include components in the results, if any.</p> <code>True</code> <code>include_launch_service</code> <code>bool</code> <p>Whether to include the ROS2 launch service in the result (if it exists).</p> <code>False</code> <code>include_foreign</code> <code>bool</code> <p>Whether to include foreign nodes not created by this launcher.</p> <code>False</code> <p>Returns:</p> Type Description <code>Generator[AbstractNode, None, None]</code> <p>The nodes matching the pattern.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.ros2_actions","title":"ros2_actions","text":"<pre><code>ros2_actions(*ros2_actions) -&gt; Ros2LaunchWrapper\n</code></pre> <p>Submit additional ROS2 launch actions for execution.</p> <p>If no <code>launch.LaunchService</code> exists yet it will be created and started immediately.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.ros2_launch_service","title":"ros2_launch_service","text":"<pre><code>ros2_launch_service(name: str = 'LaunchService', launchservice_args: list[str] = None, output: LogSink | Iterable[LogSink] | Iterable[str] | str = LogSink.SCREEN, start_immediately: bool = True) -&gt; Ros2LaunchWrapper\n</code></pre> <p>Create or retrieve a manager object that can be used for queueing ROS2 launch actions.</p> <p>Usually, calling ros2_actions is more convenient for queueing actions. However, calling this first allows to prevent starting the underlying <code>launch.LaunchService</code> immediately, giving more control over when the actions are executed.</p> <p>Since the <code>LaunchService</code> insists on running on the main thread it will be started as a sub process.</p> <p>Note that only one instance of the ROS2 wrapper should ever exist. Calling this method after it has been created will return the already existing instance instead. Any passed arguments will be silently discarded.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name used to identify the process and its logger.</p> <code>'LaunchService'</code> <code>launchservice_args</code> <code>list[str]</code> <p>Additional launch arguments to pass to the ROS2 launch service. These will end up in <code>launch.LaunchContext.argv</code>.</p> <code>None</code> <code>output</code> <code>LogSink | Iterable[LogSink] | Iterable[str] | str</code> <p>How log output from the launch service should be handled. This will also include the output from all nodes launched by this launch service. Common choices are <code>screen</code> to print to terminal, <code>log</code> to write to a common log file, <code>own_log</code> to write to a node-specific log file, and <code>none</code> to not write any output anywhere. See configure_logger for details.</p> <code>SCREEN</code> <code>start_immediately</code> <code>bool</code> <p>If True, the ROS2 launch service process is started immediately.</p> <code>True</code> <p>Returns:</p> Type Description <code>Ros2LaunchWrapper</code> <p>The wrapper hosting the ROS2 launch service process.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.ros_distro","title":"ros_distro  <code>staticmethod</code>","text":"<pre><code>ros_distro() -&gt; str\n</code></pre> <p>Returns the name of the currently sourced ros distro (i.e. $ROS_DISTRO).</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.run_later","title":"run_later","text":"<pre><code>run_later(delay: float, callback: Callable, *args, **kwargs) -&gt; Future\n</code></pre> <p>Convenience method for running a callback with a delay. The callback will be called on a separte thread.</p> <p>This mainly exists to cover the use case where you want to interact with ROS from an <code>rclpy.Timer</code>. A synchronous call from within a timer (e.g. a service call like Node.set_live_params) will block ROS' background event loop, preventing publishers, subscribers, services, etc. from doing their work. It will also prevent a clean shutdown as ROS usually waits for the event queue to become empty.</p> <p>When executing a long running task this way it is a good idea to check is_shutdown in between iterations.</p> <p>Parameters:</p> Name Type Description Default <code>delay</code> <code>float</code> <p>How long to wait in seconds before calling the callback.</p> required <code>callback</code> <code>Callable</code> <p>The function to call after the timeout. Will not be called if shutdown is called beforehand.</p> required <code>*args</code> <code>Any</code> <p>Positional arguments to the callback.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments to the callback.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Future</code> <p>A future which will be cancelled on <code>shutdown</code>, otherwise it will hold the callback's result or an exception the callback raised.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.service","title":"service","text":"<pre><code>service(topic: str, service_type: str | type, callback: Callable[[Any], Any], qos_profile: QoSProfile = None) -&gt; RosServiceProvider\n</code></pre> <p>Create a ROS2 service provider using the shared_node.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic the service will live on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>The function that will handle any requests to the service. The type of the request will be of type <code>service_type.Request</code>.</p> required <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <p>Returns:</p> Type Description <code>Service</code> <p>The service object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.service_client","title":"service_client","text":"<pre><code>service_client(topic: str, service_type: str | type, timeout: float = 5.0, qos_profile: QoSProfile = None) -&gt; RosServiceClient\n</code></pre> <p>Create a ROS2 service client that can be used to call a service.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The service topic to post requests on.</p> required <code>service_type</code> <code>str | type</code> <p>The service's message type. Strings must follow the pattern <code>&lt;package&gt;/srv/&lt;message&gt;</code>.</p> required <code>timeout</code> <code>float</code> <p>Time to wait for the service to become available. Ignored if &lt;= 0.</p> <code>5.0</code> <code>qos_profile</code> <code>QoSProfile</code> <p>A quality of service profile that changes how the service handles connections.</p> <code>None</code> <p>Returns:</p> Type Description <code>Client</code> <p>The client object. Although not required for Jazzy and below, it is recommended to keep a reference.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the service did not become available within the specified timeout.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.shutdown","title":"shutdown","text":"<pre><code>shutdown(reason: str = None, signum: int = signal.SIGTERM) -&gt; None\n</code></pre> <p>Ask all nodes to shutdown and terminate the internal ROS2 thread. Any subsequent calls to BetterLaunch member functions, including this one, may fail. This will typically be called when you want to terminate your launch file.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A human-readable string explaining the reason for the shutdown. If not given this will be requitted with a warning.</p> <code>None</code> <code>signum</code> <code>int</code> <p>The signal to send to child processes.</p> <code>SIGTERM</code>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.sleep","title":"sleep","text":"<pre><code>sleep(seconds: float) -&gt; None\n</code></pre> <p>Wait for the specified amount of time.</p> <p>This mostly exists to provide this functionality for TOML launchfiles.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>float</code> <p>How many seconds to wait.</p> required"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.spin","title":"spin","text":"<pre><code>spin(exit_with_last_node: bool = True) -&gt; None\n</code></pre> <p>Join the BetterLaunch thread until it terminates. You do not need to call this if you're using the launch_this wrapper or the TUI.</p> <p>Parameters:</p> Name Type Description Default <code>exit_with_last_node</code> <code>bool</code> <p>If True this function will return when all nodes have been stopped.</p> <code>True</code>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.subscriber","title":"subscriber","text":"<pre><code>subscriber(topic: str, message_type: str | type, callback: Callable[[Any], None], qos_profile: QoSProfile | int = 10) -&gt; RosSubscriber\n</code></pre> <p>Create a ROS2 subscriber to receive messages.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The topic to listen on for messages.</p> required <code>message_type</code> <code>str | type</code> <p>The type of the messages that will be received. Strings must follow the pattern <code>&lt;package&gt;/msg/&lt;message&gt;</code>.</p> required <code>callback</code> <code>Callable[[Any], Any]</code> <p>A function that will be called whenever a message is received.</p> required <code>qos_profile</code> <code>QoSProfile | int</code> <p>A quality of service profile that changes how the publisher handles connections and retains data.</p> <code>10</code> <p>Returns:</p> Type Description <code>Subscription</code> <p>The subscriber object. Although not required for Jazzy and below, it is recommended to keep a reference.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.wait_for_service","title":"wait_for_service","text":"<pre><code>wait_for_service(service: str, timeout: float = None) -&gt; bool\n</code></pre> <p>Wait for the specified service to appear.</p> <p>Parameters:</p> Name Type Description Default <code>service</code> <code>str</code> <p>The full path of the service to wait for.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the service. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the service appeared within the timeout, False otherwise.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunch.wait_for_topic","title":"wait_for_topic","text":"<pre><code>wait_for_topic(topic: str, timeout: float = None) -&gt; bool\n</code></pre> <p>Wait for the specified topic to appear.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>The full path of the topic to wait for.</p> required <code>timeout</code> <code>float</code> <p>How long to wait for the topic. Wait forever if None.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the topic appeared within the timeout, False otherwise.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunchMeta","title":"BetterLaunchMeta","text":"<p>               Bases: <code>type</code></p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunchMeta.instance","title":"instance","text":"<pre><code>instance() -&gt; BetterLaunch\n</code></pre> <p>Immediately retrieve the BetterLaunch singleton instance.</p> <p>Returns:</p> Type Description <code>BetterLaunch</code> <p>The BetterLaunch singleton instance, or None if it doesn't exist yet.</p>"},{"location":"reference/better_launch/launcher/#better_launch.launcher.BetterLaunchMeta.wait_for_instance","title":"wait_for_instance","text":"<pre><code>wait_for_instance(timeout: float = None) -&gt; BetterLaunch\n</code></pre> <p>Retrieve the BetterLaunch singleton instance as soon as possible.</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float</code> <p>How long to wait for the singleton instance to appear. Wait forever if timeout is None. Don't wait at all if timeout is 0.0.</p> <code>None</code> <p>Returns:</p> Type Description <code>BetterLaunch</code> <p>The BetterLaunch singleton instance.</p> <p>Raises:</p> Type Description <code>TimeoutError</code> <p>If the timeout has passed and no instance has appeared yet.</p>"},{"location":"reference/better_launch/ros/","title":"ros","text":""},{"location":"reference/better_launch/ros/#better_launch.ros","title":"better_launch.ros","text":""},{"location":"reference/better_launch/ros/handlers/","title":"handlers","text":""},{"location":"reference/better_launch/ros/handlers/#better_launch.ros.handlers","title":"better_launch.ros.handlers","text":"<p>Module with handlers for launch specific logging taken from ros2 launch.</p> <p>.. seealso::</p> <pre><code>`ROS2 launch/handlers.py &lt;https://github.com/ros2/launch/blob/rolling/launch/launch/logging/handlers.py&gt;`_\n</code></pre>"},{"location":"reference/better_launch/ros/handlers/#better_launch.ros.handlers.with_per_logger_formatting","title":"with_per_logger_formatting","text":"<pre><code>with_per_logger_formatting(cls)\n</code></pre> <p>Add per logger formatting capabilities to the given logging.Handler.</p>"},{"location":"reference/better_launch/ros/logging/","title":"logging","text":""},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging","title":"better_launch.ros.logging","text":"<p>Module for the launch specific logging taken from ROS2.</p> <p>.. seealso::</p> <pre><code>`ROS2 rclpy/logging.py &lt;https://github.com/ros2/launch/blob/rolling/launch/launch/logging/logging.py&gt;`_\n</code></pre>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig","title":"LaunchConfig","text":"<p>Launch Logging Configuration class.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.log_dir","title":"log_dir  <code>property</code> <code>writable</code>","text":"<pre><code>log_dir\n</code></pre> <p>Get the current log directory, generating it if necessary.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.log_handler_factory","title":"log_handler_factory  <code>property</code> <code>writable</code>","text":"<pre><code>log_handler_factory\n</code></pre> <p>Get the log_handler_factory, generating it if necessary.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.get_log_file_handler","title":"get_log_file_handler","text":"<pre><code>get_log_file_handler(file_name='launch.log')\n</code></pre> <p>Get the logging handler to a log file.</p> <p>See launch_config() documentation for application wide log file logging configuration.</p> <p>:param: file_name of the log file whose handler is to be retrieved. :return: the logging handler associated to the file (always the same once constructed).</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.get_log_file_path","title":"get_log_file_path","text":"<pre><code>get_log_file_path(file_name='launch.log')\n</code></pre> <p>Get the absolute path to the given log file.</p> <p>:param: file_name of the log file from which to get the absolute path. :return: the absolute path to the log file.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.get_screen_handler","title":"get_screen_handler","text":"<pre><code>get_screen_handler()\n</code></pre> <p>Get the one and only screen logging handler.</p> <p>See launch_config() documentation for screen logging configuration.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.set_log_format","title":"set_log_format","text":"<pre><code>set_log_format(log_format, *, log_style=None)\n</code></pre> <p>Set up launch log file format.</p> <p>:param log_format: the format used when logging to the main launch log file,     as expected by the <code>logging.Formatter</code> constructor.     Alternatively, the 'default' alias can be given to log verbosity level,     logger name and logged message.     This format can also be overridden by the environment variable     'OVERRIDE_LAUNCH_LOG_FORMAT'. :param log_style: the log style used if no alias is given for log_format.     No style can be provided if a format alias is given.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.LaunchConfig.set_screen_format","title":"set_screen_format","text":"<pre><code>set_screen_format(screen_format, *, screen_style=None)\n</code></pre> <p>Set up screen formats.</p> <p>For the <code>screen_format</code> argument there are a few aliases:</p> <ul> <li>'default' to log verbosity level, logger name and logged message</li> <li>'default_with_timestamp' to add timestamps to the 'default' format</li> </ul> <p>:param screen_format: format specification used when logging to the screen,     as expected by the <code>logging.Formatter</code> constructor.     Alternatively, aliases for common formats are available, see above.     This format can also be overridden by the environment variable     'OVERRIDE_LAUNCH_SCREEN_FORMAT'. :param screen_style: the screen style used if no alias is used for     screen_format.     No style can be provided if a format alias is given.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.get_logger","title":"get_logger","text":"<pre><code>get_logger(name=None) -&gt; logging.Logger\n</code></pre> <p>Get named logger, configured to output to screen and launch main log file.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.get_output_loggers","title":"get_output_loggers","text":"<pre><code>get_output_loggers(process_name, output_config)\n</code></pre> <p>Get the stdout and stderr output loggers for the given process name.</p> <p>The output_config may be a dictionary with one or more of the optional keys 'stdout', 'stderr', or 'both' (stdout and stderr combined) which represent the various process output sources, and values for those keys to assign one or more logging destinations to the source. The logging destination values may be:</p> <ul> <li>'screen': log it to the screen,</li> <li>'log': log it to launch log file, or</li> <li>'own_log': log it to a separate log file.</li> </ul> <p>When logging the stdout and stderr separately, the log file names follow the <code>&lt;process_name&gt;-&lt;source&gt;.log</code> pattern where <code>&lt;source&gt;</code> is either 'stdout' or 'stderr' When the 'both' logging destination is used the log file name follows the <code>&lt;process_name&gt;.log</code> pattern.</p> <p>The \"launch log file\" is a log file which is create for each run of the launch.LaunchService, and at least captures the log output from launch itself, but may also include output from subprocess's if configured so.</p> <p>Alternatively, the output_config parameter may be a string which represents one of a couple available aliases for common logging configurations. The available aliases are:</p> <ul> <li>'screen': stdout and stderr are logged to the screen,</li> <li>'log': stdout and stderr are logged to launch log file and stderr to         the screen,</li> <li>'both': both stdout and stderr are logged to the screen and to launch         main log file,</li> <li>'own_log' for stdout, stderr and their combination to be logged to         their own log files, and</li> <li>'full' to have stdout and stderr sent to the screen, to the main launch         log file, and their own separate and combined log files.</li> </ul> <p>:param process_name: the process-like action whose outputs want to be logged. :param output_config: configuration for the output loggers,     see above for details. :returns: a tuple with the stdout and stderr output loggers.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.log_launch_config","title":"log_launch_config","text":"<pre><code>log_launch_config(*, logger=logging.root)\n</code></pre> <p>Log logging configuration details relevant for a user with the given logger.</p>"},{"location":"reference/better_launch/ros/logging/#better_launch.ros.logging.reset","title":"reset","text":"<pre><code>reset()\n</code></pre> <p>Reset logging.</p>"},{"location":"reference/better_launch/ros/ros_adapter/","title":"ros_adapter","text":""},{"location":"reference/better_launch/ros/ros_adapter/#better_launch.ros.ros_adapter","title":"better_launch.ros.ros_adapter","text":""},{"location":"reference/better_launch/ros/ros_adapter/#better_launch.ros.ros_adapter.ROSAdapter","title":"ROSAdapter","text":"<p>Wraps rclpy API to ease ROS node usage in <code>launch_ros</code> actions.</p>"},{"location":"reference/better_launch/ros/ros_adapter/#better_launch.ros.ros_adapter.ROSAdapter.__init__","title":"__init__","text":"<pre><code>__init__(*, argv: list[str] = None, autostart: bool = True)\n</code></pre> <p>Construct adapter.</p> <p>:param: argv List of global arguments for rclpy context initialization. :param: autostart Whether to start adapter on construction or not.</p>"},{"location":"reference/better_launch/ros/ros_adapter/#better_launch.ros.ros_adapter.ROSAdapter.shutdown","title":"shutdown","text":"<pre><code>shutdown()\n</code></pre> <p>Shutdown ROS adapter.</p>"},{"location":"reference/better_launch/ros/ros_adapter/#better_launch.ros.ros_adapter.ROSAdapter.start","title":"start","text":"<pre><code>start()\n</code></pre> <p>Start ROS adapter.</p>"},{"location":"reference/better_launch/toml/","title":"toml","text":""},{"location":"reference/better_launch/toml/#better_launch.toml","title":"better_launch.toml","text":""},{"location":"reference/better_launch/toml/substitutions/","title":"substitutions","text":""},{"location":"reference/better_launch/toml/substitutions/#better_launch.toml.substitutions","title":"better_launch.toml.substitutions","text":""},{"location":"reference/better_launch/toml/substitutions/#better_launch.toml.substitutions.SubstitutionError","title":"SubstitutionError","text":"<p>               Bases: <code>ValueError</code></p> <p>Exception type that will be thrown by substitution handlers.</p>"},{"location":"reference/better_launch/toml/substitutions/#better_launch.toml.substitutions.apply_substitutions","title":"apply_substitutions","text":"<pre><code>apply_substitutions(value: str, substitutions: dict[str, Callable] = None, context: dict[str, Any] = None, *, eval_mode: EvalMode = EvalMode.NONE) -&gt; Any\n</code></pre> <p>Applies substitutions to a string.</p> <p>Substitution strings are expected to follow the pattern: <code>${key: *args}</code>, where <code>key</code> is a substitution type and <code>*args</code> are additional arguments to the substitution handler.</p> <p>If no other substitutions are specified, this function will handle the following ones: - ${env:  [default]} - ${param:  } - ${eval: [python-strings]} <p>Substitutions other than those above will be looked up in the provided <code>context</code> dict.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>A string that may contain substitution tokens.</p> required <code>substitutions</code> <code>dict[str, Callable]</code> <p>Custom substitution handlers. If None, defaults to param, env, and eval.</p> <code>None</code> <code>context</code> <code>dict[str, Any]</code> <p>A dict containing additional values the substitution may use.</p> <code>None</code> <code>eval_type</code> <code>Literal[full, literal, none]</code> <p>If and to what extent <code>eval</code> should be allowed. Default is \"full\".</p> required <p>Returns:</p> Type Description <code>str</code> <p>The input string with all substitution tokens handled.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the input string contains unbalanced braces or quotes.</p> <code>SubstitutionError</code> <p>If a substitution handler fails.</p>"},{"location":"reference/better_launch/toml/toml_parser/","title":"toml_parser","text":""},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser","title":"better_launch.toml.toml_parser","text":""},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser.TomlReader","title":"TomlReader","text":""},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser.TomlReader.__init__","title":"__init__","text":"<pre><code>__init__(text: str | list[str])\n</code></pre> <p>TOML parser with better comment handling compared to existing ones.</p> <p>Noticably, this parser will preserve and associate comments that appear before the key they are describing. Comments appear in the same dict as their associated key under the key <code>__comment_&lt;key&gt;__</code>. In addition, a comment at the start of the file will be retained as well. This root comment has the key <code>__comment__</code>.</p> <p>Some standard features are not supported right now: - octal integers (0oXXXX) - dates and times remain strings - arrays of tables ([[my-aot]])</p> <p>The following non-standard features are supported: - write <code>key = &lt;base-type&gt;</code> to set its value to the corresponding class. Base types are int, float, str, bool, list, dict.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str | list[str]</code> <p>The toml content to be parsed.</p> required"},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser.TomlReader.parse","title":"parse","text":"<pre><code>parse() -&gt; dict[str, Any]\n</code></pre> <p>Parse TOML text and return a dictionary.</p>"},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser.load","title":"load","text":"<pre><code>load(path: str) -&gt; dict[str, Any]\n</code></pre> <p>Read the contents of a TOML file and parse them.</p>"},{"location":"reference/better_launch/toml/toml_parser/#better_launch.toml.toml_parser.loads","title":"loads","text":"<pre><code>loads(text: str) -&gt; dict[str, Any]\n</code></pre> <p>Parse TOML text and return a dictionary.</p>"},{"location":"reference/better_launch/tui/","title":"tui","text":""},{"location":"reference/better_launch/tui/#better_launch.tui","title":"better_launch.tui","text":""},{"location":"reference/better_launch/tui/better_tui/","title":"better_tui","text":""},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui","title":"better_launch.tui.better_tui","text":""},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui.AppMode","title":"AppMode","text":"<p>               Bases: <code>IntEnum</code></p> <p>States of our TUI. The TUI decides what to display based on the active state.</p>"},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui.BetterTui","title":"BetterTui","text":""},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui.BetterTui.__init__","title":"__init__","text":"<pre><code>__init__(launch_func: Callable[[], None], *, manage_foreign_nodes: bool = False, keep_alive: bool = False, color_depth: Literal[1, 4, 8, 24] = 8)\n</code></pre> <p>Our TUI class. Use run to start the TUI.</p> <p>In order to override keybindings you may set the BL_TUI_KEYBINDS environment variable. For example, this will bind the nodes menu to ctrl-n and setting the log level to ctrl-l:</p> <p>.. code:: bash</p> <pre><code>BL_TUI_KEYBINDS=\"nodes: c-n; loglevel: c-l\" bl better_launch 02_ui.launch.py\n</code></pre> <p>The syntax is always <code>&lt;action&gt;:&lt;keys&gt;</code> separated by <code>;</code>. The valid actions are <code>exit</code>, <code>mute</code>, <code>nodes</code>, <code>loglevel</code>, <code>cancel</code>, <code>enter</code>, <code>next</code>, <code>previous</code>. For valid key specifiers see <code>prompt_toolkit &lt;https://python-prompt-toolkit.readthedocs.io/en/stable/pages/advanced_topics/key_bindings.html&gt;</code>_. Take special note of how the Alt/Meta/Option key is treated. </p> <p>Parameters:</p> Name Type Description Default <code>launch_func</code> <code>Callable[[], None]</code> <p>The launch function to run once this TUI is started.</p> required <code>manage_foreign_nodes</code> <code>bool</code> <p>Whether to list foreign nodes in the nodes menu.</p> <code>False</code> <code>keep_alive</code> <code>bool</code> <p>If True, the TUI will keep running even when the last node stops running.</p> <code>False</code> <code>color_depth</code> <code>Literal[1, 4, 8, 24]</code> <p>How many colors to use for display. Corresponds to monochrome, ANSI colors, 256 colors, true colors.</p> <code>8</code>"},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui.BetterTui.quit","title":"quit","text":"<pre><code>quit(reason: str) -&gt; None\n</code></pre> <p>Shutdown better_launch if it is still running, then exit the TUI.</p> <p>Parameters:</p> Name Type Description Default <code>reason</code> <code>str</code> <p>A reason for the shutdown that will be shown to the user.</p> required"},{"location":"reference/better_launch/tui/better_tui/#better_launch.tui.better_tui.BetterTui.run","title":"run","text":"<pre><code>run()\n</code></pre> <p>Initialize the TUI app, then run the launch function before starting the TUI.</p>"},{"location":"reference/better_launch/tui/footer_menu/","title":"footer_menu","text":""},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu","title":"better_launch.tui.footer_menu","text":""},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu","title":"FooterMenu","text":"<p>               Bases: <code>FormattedTextControl</code></p>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.__init__","title":"__init__","text":"<pre><code>__init__(items: list[str | tuple] = None, *, empty_prompt: str = '---', ellipses: str = '\u2026', highlight_style: str = 'reverse')\n</code></pre> <p>Creates a single line of selectable items. This allows to have menus without needing more space for drawing submenus. Items that don't fit into the line will be \"scrolled\" to as the selection shifts.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>list[str | tuple[str]]</code> <p>The items to display. Items can be strings or tuples but don't have to be consistent. If an item is a tuple, the first element is the style of the item and the second element its label. Additional elements are ignored and can be used to store additional information with each item.</p> <code>None</code> <code>empty_prompt</code> <code>str</code> <p>What to show when there are no items.</p> <code>'---'</code> <code>ellipses</code> <code>str</code> <p>A character or string to signify that there are additional items.</p> <code>'\u2026'</code> <code>highlight_style</code> <code>str</code> <p>The style to use for showing that an item is selected.</p> <code>'reverse'</code>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.get_selected_item","title":"get_selected_item","text":"<pre><code>get_selected_item() -&gt; str | tuple\n</code></pre> <p>Return the currently selected item data. If the item is a tuple it will have formatting in the first slot, its string representation in the second, and any additional information in subsequent slots.</p> <p>Returns:</p> Type Description <code>str | tuple</code> <p>The item as it was passed to this menu.</p>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.select","title":"select","text":"<pre><code>select(idx: int) -&gt; None\n</code></pre> <p>Select the specified existing menu item. No modulo operation is performed on the passed index, so it must be valid.</p> <p>Parameters:</p> Name Type Description Default <code>idx</code> <code>int</code> <p>The index of the item to be selected.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the provided index is outside the range of items in this menu.</p>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.select_next","title":"select_next","text":"<pre><code>select_next() -&gt; None\n</code></pre> <p>Select the next menu item, wrapping around.</p>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.select_prev","title":"select_prev","text":"<pre><code>select_prev() -&gt; None\n</code></pre> <p>Select the previous menu item, wrapping around.</p>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.set_items","title":"set_items","text":"<pre><code>set_items(items: list[str | tuple[str, str]], default: int = 0) -&gt; None\n</code></pre> <p>Replace the items currently in this menu.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>list[str | tuple[str, str]]</code> <p>The new items to use. If an item is a tuple, its first slot must be a formatting string and the second its string information. Any subsequent slots are kept, but ignored.</p> required <code>default</code> <code>int</code> <p>Index of the item to select.</p> <code>0</code>"},{"location":"reference/better_launch/tui/footer_menu/#better_launch.tui.footer_menu.FooterMenu.update_items","title":"update_items","text":"<pre><code>update_items(items: list[str | tuple[str, str]]) -&gt; None\n</code></pre> <p>Like FooterMenu.set_items, except that it keeps the currently selected index intact if it is contained within the new list of items.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>list[str | tuple[str, str]]</code> <p>The new items to use. If an item is a tuple, its first slot must be a formatting string and the second its string information. Any subsequent slots are kept, but ignored.</p> required"},{"location":"reference/better_launch/utils/","title":"utils","text":""},{"location":"reference/better_launch/utils/#better_launch.utils","title":"better_launch.utils","text":""},{"location":"reference/better_launch/utils/better_logging/","title":"better_logging","text":""},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging","title":"better_launch.utils.better_logging","text":""},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging.PrettyLogFormatter","title":"PrettyLogFormatter","text":"<p>               Bases: <code>Formatter</code></p>"},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging.PrettyLogFormatter.__init__","title":"__init__","text":"<pre><code>__init__(format: str, timestamp_format: str = '%Y-%m-%d %H:%M:%S.%f', *, defaults: dict[str, Any] = None, roslog_pattern: str = None, source_colors: str | int | Iterable[int] | dict[str, Any] = default_source_color, log_colors: str | int | Iterable[int] | dict[str, Any] = None, no_colors: bool = False, max_message_length: int = 0)\n</code></pre> <p>A specialized formatter that will try to extract various details from messages logged by ROS2 nodes and reformat them.</p> <p>The following additional keys can be used for formatting messages: * {sourcecolor_start}: colors subsequent characters based on the message's source until a {sourcecolor_end} is encountered. * {levelcolor_start}: colors subsequent characters based on the message's severity until a {levelcolor_end} is encountered.</p> <p>Colors can be specified in 3 ways: * an ANSI/VT100 escape sequence, typically starting with <code>\\e</code> or <code>\\x1b</code>. * an integer, referring to a color from the vte 256 colors table. * a tuple of 3 integers representing an RGB value.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>str</code> <p>The format this instance will use when formatting messages.</p> required <code>timestamp_format</code> <code>_type_</code> <p>The format to use for timestamps (we like human readable here).</p> <code>'%Y-%m-%d %H:%M:%S.%f'</code> <code>defaults</code> <code>dict[str, Any]</code> <p>Defaults the formatter may use when formatting strings.</p> <code>None</code> <code>roslog_pattern</code> <code>str</code> <p>The pattern used for matching incoming log messages. The pattern should define named groups that will be matched to logging.Record attributes via their names.</p> <code>None</code> <code>source_colors</code> <code>str | int | Iterable[int] | dict[str, Any]</code> <p>Colors to use when formatting <code>sourcecolor_start</code> tags based on the source of the log report. If a string, integer or iterable, use this as the color for all sources. If None, use a different color for every source. Pass a dict to specify custom colors for a set of sources.</p> <code>default_source_color</code> <code>log_colors</code> <code>str | int | Iterable[int] | dict[str, Any]</code> <p>Colors to use when formatting <code>levelcolor_start</code> tags based on the log report's severity. If a string, integer or iterable, use this as the color for all sources. If None, use the default log colors from <code>default_log_colormap</code>. Pass a dict to override the colors for select log levels identified by name.</p> <code>None</code> <code>no_colors</code> <code>bool</code> <p>If True, all colors will be disabled.</p> <code>False</code> <code>max_message_length</code> <code>int</code> <p>Limit the length of the message content formatted by this logger. No limits are imposed if &lt;= 0.</p> <code>0</code>"},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging.PrettyLogFormatter.get_source_color","title":"get_source_color","text":"<pre><code>get_source_color(source: str) -&gt; str\n</code></pre> <p>Return the color associated with the provided source.</p>"},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging.StubbornHandler","title":"StubbornHandler","text":"<p>               Bases: <code>Handler</code></p> <p>This handler resists the common changes ROS2 attempts to make for its logging so that our formatters can work properly.</p> <p>The ROS2 launch system assigns the same formatter to many sources using special wrappers. However, we don't want our log forwarders and formatters to be replaced just like that.</p> <p>Parameters:</p> Name Type Description Default <code>actual_handler</code> <code>Handler</code> <p>The handler which will actually handle any incoming log records.</p> required"},{"location":"reference/better_launch/utils/better_logging/#better_launch.utils.better_logging.configure_logger","title":"configure_logger","text":"<pre><code>configure_logger(logger: Logger, output: LogSink | Iterable[LogSink] | Iterable[str] | str = None, screen_formatter: Formatter = None, file_formatter: Formatter = None) -&gt; None\n</code></pre> <p>Initialize the logging framework.</p>"},{"location":"reference/better_launch/utils/click/","title":"click","text":""},{"location":"reference/better_launch/utils/click/#better_launch.utils.click","title":"better_launch.utils.click","text":""},{"location":"reference/better_launch/utils/click/#better_launch.utils.click.get_click_bl_options","title":"get_click_bl_options","text":"<pre><code>get_click_bl_options(expose: bool = False) -&gt; list[click.Option]\n</code></pre> <p>Get the click options specific to better_launch itself.</p> <p>Parameters:</p> Name Type Description Default <code>expose</code> <code>bool</code> <p>If True, click will forward these options to the command callback.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[Option]</code> <p>description</p>"},{"location":"reference/better_launch/utils/colors/","title":"colors","text":""},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors","title":"better_launch.utils.colors","text":""},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors.get_contrast_color","title":"get_contrast_color  <code>module-attribute</code>","text":"<pre><code>get_contrast_color = HighContrastColorGenerator()\n</code></pre> <p>A global instance to generate sequences of visually distinct colors.</p>"},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors.HighContrastColorGenerator","title":"HighContrastColorGenerator","text":"<p>Generates RGB colors with a certain distance apart so that subsequent colors are visually distinct.</p>"},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors.HighContrastColorGenerator.__call__","title":"__call__","text":"<pre><code>__call__()\n</code></pre> <p>Allows calling the instance directly to get the next color.</p>"},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors.HighContrastColorGenerator.__iter__","title":"__iter__","text":"<pre><code>__iter__()\n</code></pre> <p>Allows the class to be used as an iterable.</p>"},{"location":"reference/better_launch/utils/colors/#better_launch.utils.colors.HighContrastColorGenerator.__next__","title":"__next__","text":"<pre><code>__next__()\n</code></pre> <p>Generates the next high-contrast color.</p>"},{"location":"reference/better_launch/utils/glob_dict/","title":"glob_dict","text":""},{"location":"reference/better_launch/utils/glob_dict/#better_launch.utils.glob_dict","title":"better_launch.utils.glob_dict","text":""},{"location":"reference/better_launch/utils/glob_dict/#better_launch.utils.glob_dict.deep_merge","title":"deep_merge","text":"<pre><code>deep_merge(base: dict, override: dict) -&gt; dict\n</code></pre> <p>Merge a (nested) override dict into a (nested) base dict. </p> <p>After this operation the entirety of override will be part of base. All values will be assigned without any copies. This operation will alter the provided base dict. Use copy.deepcopy if you don't want this.</p> <p>Parameters:</p> Name Type Description Default <code>base</code> <code>dict</code> <p>The dict into which the override will be merged.</p> required <code>override</code> <code>dict</code> <p>The override to merge into base.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>The merged dict, which is the modified base dict.</p>"},{"location":"reference/better_launch/utils/glob_dict/#better_launch.utils.glob_dict.glob_dict","title":"glob_dict","text":"<pre><code>glob_dict(data: dict, pattern: str, invert: bool = False, strip: bool = True) -&gt; dict\n</code></pre> <p>Filter a nested dict by a glob pattern.</p> <p>Keys may contain slashes which are treated as path separators.</p> <p>This function supports wildcards both in dict keys and the provided pattern. * will match a single key part or skip a single pattern part. ** will consider the entire remaining branch as matching.</p> <p>Note that this function is destructive and will prune parts of the provided dictionary not matching pattern. Use <code>copy.deepcopy</code> if you want to keep your input dict untouched.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>The input dictionary. Non-matching parts will be removed.</p> required <code>pattern</code> <code>str</code> <p>A glob-style pattern to select parts of the dictionary to keep.</p> required <code>invert</code> <code>bool</code> <p>If True, only retain branches that do not match the provided pattern.</p> <code>False</code> <code>strip</code> <code>bool</code> <p>If True, the matched qualifier prefix is removed from the keys of the returned dict.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict</code> <p>The pruned input dict.</p>"},{"location":"reference/better_launch/utils/glob_dict/#better_launch.utils.glob_dict.merge_and_explode","title":"merge_and_explode","text":"<pre><code>merge_and_explode(*dicts: dict) -&gt; dict\n</code></pre> <p>Recursively merge any number of nested dicts.</p> <p>All keys (including those of nested dicts) must be strings. Compound keys (e.g. \"a/b\") are exploded into nested dicts first. Later dicts win on key conflicts at leaf level.</p> Parameters: <p>dicts: dict     The dictionaries to merge.</p> <p>Returns:</p> Type Description <code>dict</code> <p>A dict which is the result of exploding and merging all input dicts.</p>"},{"location":"reference/better_launch/utils/introspection/","title":"introspection","text":""},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection","title":"better_launch.utils.introspection","text":""},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.find_calling_frame","title":"find_calling_frame","text":"<pre><code>find_calling_frame(func: Callable, thread_id: int = -1) -&gt; inspect.FrameInfo\n</code></pre> <p>Find the most recent stack frame the specified function is called in that is NOT in the same file as the function. This is useful to e.g. identify the python file a function is imported from and called in.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>A defined function.</p> required <code>thread_id</code> <code>int</code> <p>The ID of the thread to search in. Search the main thread if 0, or the current thread if -1.</p> <code>-1</code> <p>Returns:</p> Type Description <code>FrameInfo</code> <p>A frame which called the provided function but lives in a different file than the function itself.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no such frame could be found.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.find_decorated_function_args","title":"find_decorated_function_args","text":"<pre><code>find_decorated_function_args(decorator_func: Callable) -&gt; dict[str, Any]\n</code></pre> <p>Retrieve the arguments of the function that the specified decorator is wrapping.</p> <p>Note that the function must be part of the current stack frame that led to the invocation of this function.</p> <p>Parameters:</p> Name Type Description Default <code>decorator_func</code> <code>Callable</code> <p>A decorator function.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>The arguments that were used to invoke the function decorated by the provided decorator.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the function could not be extracted from the decorator or the function frame could not be identified.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.find_function_frame","title":"find_function_frame","text":"<pre><code>find_function_frame(func: Callable) -&gt; inspect.FrameInfo\n</code></pre> <p>Find the most recent stack frame the specified function is called in.</p> <p>Note that the function must be part of the current stack frame that led to the invocation of this function.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>A defined function.</p> required <p>Returns:</p> Type Description <code>FrameInfo</code> <p>The frame the specified function was called from.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no such frame could be found.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.find_launchthis_function","title":"find_launchthis_function","text":"<pre><code>find_launchthis_function(filepath: str) -&gt; ast.FunctionDef\n</code></pre> <p>Parses a source file into an AST tree and searches for a function decorated by better_launch.launch_this.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to a python source file.</p> required <p>Returns:</p> Type Description <code>FunctionDef</code> <p>A representation of the function decorated by launch_this, or <code>None</code> if it could not be found.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.get_bound_arguments","title":"get_bound_arguments","text":"<pre><code>get_bound_arguments(func: Callable, with_defaults: bool = True) -&gt; dict[str, Any]\n</code></pre> <p>Retrieve the arguments that were passed to the specified function.</p> <p>Note that the function must be part of the current stack frame that led to the invocation of this function.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>A defined function.</p> required <code>with_defaults</code> <code>bool</code> <p>If True the returned dict will include defaults according to the function's signature for arguments that were not passed to it.</p> <code>True</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>The arguments that were used to invoke the function.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the function frame could not be identified.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.get_launchfunc_signature_from_file","title":"get_launchfunc_signature_from_file","text":"<pre><code>get_launchfunc_signature_from_file(filepath: str) -&gt; tuple[str, inspect.Signature, str]\n</code></pre> <p>Searches for a launch function in the specified source file and returns its name, signature and docstring.</p> <p>.. seealso::</p> <pre><code>[find_launchthis_function][]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to a python source file</p> required <p>Returns:</p> Type Description <code>tuple[str, Signature]</code> <p>The name, signature and docstring of the function decorated by launch_this. If no docstring is defined for the function it will be <code>None</code>. Likewise, if no such function could be found all parts of the returned tuple will be <code>None</code>.</p>"},{"location":"reference/better_launch/utils/introspection/#better_launch.utils.introspection.get_stack","title":"get_stack","text":"<pre><code>get_stack(thread_id: int = -1) -&gt; list[inspect.FrameInfo]\n</code></pre> <p>Returns a stack of frame info for the specified thread_id.</p> <p>Parameters:</p> Name Type Description Default <code>thread_id</code> <code>int</code> <p>ID of the thread to get the frame info for. -1 will return for the caller's frame, -2 for the caller's caller, and so on. Return for the main thread if 0.</p> <code>-1</code> <p>Returns:</p> Type Description <code>list</code> <p>A list of <code>inspect.FrameInfo</code> objects.</p> <p>Raises:</p> Type Description <code>IndexError</code> <p>If the specified thread_id does not identify a valid frame.</p>"},{"location":"reference/better_launch/utils/random_names/","title":"random_names","text":""},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names","title":"better_launch.utils.random_names","text":""},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names.UniqueWordGenerator","title":"UniqueWordGenerator","text":""},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names.UniqueWordGenerator.get_unique_word","title":"get_unique_word","text":"<pre><code>get_unique_word() -&gt; str\n</code></pre> <p>Generate the next unique word</p>"},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names.UniqueWordGenerator.remaining","title":"remaining","text":"<pre><code>remaining() -&gt; int\n</code></pre> <p>Return number of words still available</p>"},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names.UniqueWordGenerator.reset","title":"reset","text":"<pre><code>reset()\n</code></pre> <p>Shuffle and reset the generator</p>"},{"location":"reference/better_launch/utils/random_names/#better_launch.utils.random_names.UniqueWordGenerator.total_available","title":"total_available","text":"<pre><code>total_available() -&gt; int\n</code></pre> <p>Return total number of unique words possible</p>"},{"location":"reference/better_launch/utils/settings/","title":"settings","text":""},{"location":"reference/better_launch/utils/settings/#better_launch.utils.settings","title":"better_launch.utils.settings","text":""},{"location":"reference/better_launch/utils/settings/#better_launch.utils.settings.Settings","title":"Settings","text":"<pre><code>Settings() -&gt; _Settings\n</code></pre> <p>Get the current settings.</p> <p>This function serves two purposes: 1. dissuades replacing the _SETTINGS object, and 2. ensures that importing modules always get the most recent state (instead of the state on import).</p> <p>Returns:</p> Type Description <code>Settings</code> <p>The current settings.</p>"},{"location":"reference/better_launch/wrapper/","title":"wrapper","text":""},{"location":"reference/better_launch/wrapper/#better_launch.wrapper","title":"better_launch.wrapper","text":""},{"location":"reference/better_launch/wrapper/#better_launch.wrapper.launch_this","title":"launch_this","text":"<pre><code>launch_this(launch_func: Callable = None, *, ui: bool = False, colormode: Colormode = None, print_limit: int = None, screen_log_level: str | int = None, screen_log_format: str = None, file_log_level: str | int = None, file_log_format: str = None, use_sim_time: bool = None, manage_foreign_nodes: bool = False, join: bool = True, keep_alive: bool = False)\n</code></pre> <p>Use this to decorate your launch function. The function will be run automatically. The function is allowed to block even when using the UI.</p> <p>NOTE: this decorator cannot be used more than once per module.</p> <p>Parameters:</p> Name Type Description Default <code>launch_func</code> <code>Callable</code> <p>Your launch function, typically using BetterLaunch to start ROS2 nodes.</p> <code>None</code> <code>ui</code> <code>bool</code> <p>Whether to start the better_launch TUI. Superseded by the <code>BL_UI</code> environment variable and the <code>--bl_ui_override</code> argument.</p> <code>False</code> <code>colormode</code> <code>Colormode</code> <p>Decides what colors will be used for: * default: one color per log severity level and a single color for all message sources * severity: one color per log severity, don't colorize message sources * source: one color per message source, don't colorize log severity * none: don't colorize anything * rainbow: colorize log severity and give each message source its own color Superseded by the <code>BL_COLORMODE</code> environment variable and the <code>--bl_colormode_override</code> argument.</p> <code>None</code> <code>print_limit</code> <code>int</code> <p>Limit the length of messages printed to the screen.</p> <code>None</code> <code>screen_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be printed to the terminal/screen. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>screen_log_format</code> <code>str</code> <p>Customize how log output will be formatted when printing it to the screen. Will be overridden by the <code>BL_SCREEN_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>file_log_level</code> <code>str | int</code> <p>The minimum level for log messages to be written to the lot file. Can be either  \"info\", \"warning\", \"error\", \"critical\", or an arbitrary integer (e.g. logging.WARNING).</p> <code>None</code> <code>file_log_format</code> <code>str</code> <p>Customize how log output will be formatted when writing it to a file. Will be overridden by the <code>BL_FILE_LOG_FORMAT</code> environment variable. See PrettyLogFormatter for details.</p> <code>None</code> <code>manage_foreign_nodes</code> <code>bool</code> <p>If True, the TUI will also include node processes not started by this process. Has no effect if the TUI is not started.</p> <code>False</code> <code>join</code> <code>bool</code> <p>If True, join the better_launch process. Has no effect when ui == True.</p> <code>True</code> <code>keep_alive</code> <code>bool</code> <p>If True, keep the process alive even when all nodes have stopped.</p> <code>False</code>"}]}